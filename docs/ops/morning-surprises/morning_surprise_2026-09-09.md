# Morning surprise — 2026-09-09

Overnight Track B (autoresearch) for Ralf.

## Shipped tonight

| Version | What |
| --- | --- |
| **v0.1.963** | Instant lane: **uploads directory age** (`uploads age` / `how old are uploads` / `uploads folder age`) — newest `~/.mac-stats/uploads/` mtime; no list; safe vs path / size / upload |
| **v0.1.962** | Instant lane: **tmp directory age** (`tmp age` / `how old is tmp` / `tmp folder age`) — newest `~/.mac-stats/tmp/` mtime; no list; safe vs path / size / prune / temperature |
| **v0.1.961** | Instant lane: **task directory age** (`task age` / `how old are tasks` / `task folder age`) — newest `~/.mac-stats/task/` mtime; no list; safe vs path / size / `/tasks` |
| **v0.1.960** | AI Chat filter **Clear** chip beside All · You · Assistant · Errors (Ops Clear parity; Cleared flash) |

## Night notes
- Digester open stayed empty; design review still in grace.
- Fuel = standing backlog p50 dir-age ladder (uploads after tmp).
- First keep of the 20:00–06:00 window: **v0.1.963** @ `64f11c32`.

## Next
- Traces / pdfs / quarantine / browser-downloads age lanes, or a product-owned debug.log fix (idle-thought Ollama timeout noise).
