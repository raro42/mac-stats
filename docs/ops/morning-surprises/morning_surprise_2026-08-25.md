# Morning surprise — 2026-08-25

Overnight Track B shipped **v0.1.627** through **v0.1.629** — CPU-window fixes, AI Chat operator instant lane, then Ollama settings keyboard polish.

## Latest keep (this tick)

**v0.1.629** — Ollama settings **header↔body toolbar chain** — at header Close, ← → / h l crosses into system prompt · Reset · Save; at first/last body control, ←/→ crosses back to header Close; at header title, ← crosses to Save (changelog parity).

## Tonight's keeps

| Version | What |
|---------|------|
| **v0.1.629** | Ollama settings header↔body toolbar chain |
| **v0.1.628** | Operator instant lane in AI Chat (Discord gateway parity) |
| **v0.1.627** | LPM toggle + GPU sparklines + section persistence + Agent Ops hide + changelog wrap |

## Notes

- Digester open empty (10 turns, 7 instant); design review not due (feature-ai-chat ~11.3d grace).
- `debug.log`: quiet (180m scan — no ERROR/WARN clusters).
- Installed release @ v0.1.629; Discord Ready after install.
