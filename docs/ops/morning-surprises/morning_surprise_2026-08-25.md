# Morning surprise — 2026-08-25

Overnight Track B shipped **v0.1.627** + **v0.1.628** — CPU-window fixes after a quiet gap, then a p50 latency win for operator asks in AI Chat.

## Latest keep (this tick)

**v0.1.628** — AI Chat **operator instant lane** (`/status`, `/insights`, `/schedules`, `/digest`, `scrub memory`, `/help`) answers without an LLM — same gateway formatters Discord already used.

## Tonight's keeps

| Version | What |
|---------|------|
| **v0.1.628** | Operator instant lane in AI Chat (Discord gateway parity) |
| **v0.1.627** | LPM toggle + GPU sparklines + section persistence + Agent Ops hide + changelog wrap |

## Notes

- Digester open empty (10 turns, 7 instant); design review not due (feature-ai-chat ~11.3d grace).
- `debug.log`: quiet (180m scan — disk-91% Discord lines only earlier).
- Installed release @ v0.1.628; Discord Ready after install.
