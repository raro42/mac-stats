# Morning surprise — 2026-09-04

Overnight Track B shipped real product keeps (not digester-empty quiet).

## Shipped tonight

| Version | What |
| --- | --- |
| **v0.1.847** | Instant lane: **session_reset_phrases.md path** — `session reset phrases path` / `where is session_reset_phrases.md` / `reset phrases path` → `~/.mac-stats/agents/session_reset_phrases.md` without Ollama (path only; no list/clear; does not steal escalation / agents / session dir path). |
| **v0.1.846** | Instant lane: **escalation_patterns.md path** — `escalation patterns path` / `where is escalation_patterns.md` / `escalation file path` → `~/.mac-stats/agents/escalation_patterns.md` without Ollama (path only; no list/append; does not steal session-reset / agents path). |
| **v0.1.845** | Instant lane: **credential_accounts.json path** — path-only Keychain account-list file ask (no dump). |
| **v0.1.844** | Disk Cleanup **Filter** attention glance (Reclaim / Big / Clean). |
| **v0.1.843** | Monitors **Filter** attention glance (Up / Down / Slow). |
| **v0.1.842** | Top Processes **Filter** attention glance (Pinned / Hot). |

## Tick notes (~03:05)

- Digester open: empty (Elmasnow weather already stale/shipped).
- Design review due=false (grace); recommended `feature-ai-chat` — took standing-backlog p50 path (`session_reset_phrases.md`).
- Debug log: no ERROR/WARN/panic clusters in the scan window.
- Ratchet keep @ 6903930; install/kickstart after ship.

## Still open / next

- Digester open / product-owned `debug.log` errors when present.
- More p50 path lanes — e.g. `cookie_reject_patterns.md`, downloads-organizer rules.
- Design review screenshot when TCC allows (`feature-ai-chat` / Agent Ops still aged; grace active).
- Sibling ports (Hermes insights / session UX).
