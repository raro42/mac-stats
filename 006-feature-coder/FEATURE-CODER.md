# Feature coder — FEAT backlog

Agent-facing backlog for mac-stats. Pick an open row, implement, run `cargo check` / `cargo test` / `cargo clippy`, then mark done and note in `CHANGELOG.md` under the current version section (or `[Unreleased]` if not yet released) when behaviour changes.

## Recently closed

| ID | Item | Notes |
|------|------|--------|
| FEAT-D383 | Ollama / router: `is_context_overflow_error` — tighten `prompt too long`, `prompt is too long`, `prompt has more tokens than`, and `prompt exceeds the context` with ident-boundary (FEAT-D382 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for those phrases so embedded `prompt too long` inside `microprompt too long`, `prompt is too long` inside `microprompt is too long`, `prompt has more tokens than` inside `microprompt has more tokens than`, and `prompt exceeds the context` inside `microprompt exceeds the context` no longer match. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D382 | Ollama / router: `is_context_overflow_error` — tighten `input sequence is too long` and `prompt length exceeds` with ident-boundary (FEAT-D381 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for `input sequence is too long` and `prompt length exceeds` so embedded `input sequence is too long` inside `microinput sequence is too long` and `prompt length exceeds` inside `microprompt length exceeds` no longer match. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D381 | Ollama / router: `is_context_overflow_error` — tighten sequence-length phrases with ident-boundary | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for `exceeds maximum sequence length`, `maximum sequence length exceeded`, and `sequence length exceeds` so embedded `sequence length exceeds` inside `subsequence length exceeds` and prefixed forms like `microexceeds maximum sequence length` / `micromaximum sequence length exceeded` no longer match. Extends `does_not_match_unrelated_errors`. User-facing overflow message unchanged. |
| FEAT-D380 | Ollama / router: `is_context_overflow_error` — tighten FEAT-D308 `queries exceed` / `query exceed` with ident-boundary (FEAT-D353 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for plural `queries exceed` / `queries exceeded` and singular `query exceed` so embedded `queries exceed` inside `microqueries exceed` / `metaqueries exceed` no longer matches with a context slot; `query exceed` inside `subquery exceed` / `prequery exceed` / `requery exceed` likewise. Context-slot guard unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D379 | Ollama / router: `is_context_overflow_error` — tighten FEAT-D303 `contents exceed` / `content exceed` with ident-boundary (FEAT-D378 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for plural `contents exceed` / `contents exceeded` and singular `content exceed` so embedded `contents exceed` inside `microcontents exceed` / `metacontents exceed` no longer matches with a context slot; `content exceed` inside `subcontent exceed` / `precontent exceed` / `recontent exceed` likewise. Context-slot guard unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D378 | Ollama / router: `is_context_overflow_error` — tighten FEAT-D306 `responses exceed` / `response exceed` with ident-boundary (FEAT-D376 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for plural `responses exceed` / `responses exceeded` and singular `response exceed` so embedded `responses exceed` inside `microresponses exceed` / `metaresponses exceed` no longer matches with a context slot; `response exceed` inside `subresponse exceed` / `preresponse exceed` / `reresponse exceed` likewise. Context-slot guard unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D377 | Ollama / router: `is_context_overflow_error` — token-count `exceed` phrases with ident-boundary (FEAT-D376 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for `total prompt tokens exceed`, `requested tokens exceed`, guarded `total tokens exceed`, and guarded `input tokens exceed`. `prompt tokens exceed` uses ident-boundary plus `preceding_ascii_ident_token` to skip when the prior token is a `*total` compound (not bare `total`). FEAT-D311 `tokens exceed` / `tokens exceeded` use `contains_tokens_exceed_subphrase_at_boundary`, skipping when the prior token ends with `prompt`, `input`, `total`, or `requested`. Context-slot guards unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D376 | Ollama / router: `is_context_overflow_error` — tighten FEAT-D305 `outputs exceed` / `output exceed` with ident-boundary (FEAT-D375 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for plural `outputs exceed` / `outputs exceeded` and singular `output exceed` so embedded `outputs exceed` inside `microoutputs exceed` / `metaoutputs exceed` no longer matches with a context slot; `output exceed` inside `suboutput exceed` / `preoutput exceed` / `reoutput exceed` likewise. Context-slot guard unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D375 | Ollama / router: `is_context_overflow_error` — tighten FEAT-D300 `inputs exceed` / `input exceed` with ident-boundary (FEAT-D358 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for plural `inputs exceed` / `inputs exceeded` and singular `input exceed` so embedded `inputs exceed` inside `microinputs exceed` / `metainputs exceed` no longer matches with a context slot; `input exceed` inside `subinput exceed` / `preinput exceed` / `reinput exceed` likewise. Context-slot guard unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D374 | Ollama / router: `is_context_overflow_error` — `logprobs exceed` / `logprob exceed` + same context-slot guard as FEAT-D295 | Recognizes log-probability / incremental-decode or batch API wording where the subject is `logprobs` (or singular `logprob` via substring `logprob exceed`) with `exceed` / `exceeded`, parallel to `probabilities exceed` / `probability exceed`. `logprob exceed` matches `logprob exceeds` / `logprob exceeded` and does not substring-match plural `logprobs exceed` (the `s` after `logprob` breaks `logprob` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `micrologprobs exceed` / `metalogprobs exceed` / `sublogprob exceed` do not false-positive; `prelogprob exceed` and `relogprob exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `logprobs exceed` rate limits, top-k / return-n caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D373 | Ollama / router: `is_context_overflow_error` — `probabilities exceed` / `probability exceed` + same context-slot guard as FEAT-D295 | Recognizes softmax / sampling or batch API wording where the subject is `probabilities` (or singular `probability` via substring `probability exceed`) with `exceed` / `exceeded`, parallel to `logits exceed` / `logit exceed`. `probability exceed` matches `probability exceeds` / `probability exceeded` and does not substring-match plural `probabilities exceed` (after `probability` the plural has `ies`, not space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microprobabilities exceed` / `metaprobabilities exceed` / `subprobability exceed` do not false-positive; `preprobability exceed` and `reprobability exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `probabilities exceed` rate limits, softmax / distribution caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D372 | Ollama / router: `is_context_overflow_error` — `logits exceed` / `logit exceed` + same context-slot guard as FEAT-D295 | Recognizes batched-inference / LM-head or batch API wording where the subject is `logits` (or singular `logit` via substring `logit exceed`) with `exceed` / `exceeded`, parallel to `embeddings exceed` / `embedding exceed`. `logit exceed` matches `logit exceeds` / `logit exceeded` and does not substring-match plural `logits exceed` (the `s` after `logit` breaks `logit` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `micrologits exceed` / `metalogits exceed` / `sublogit exceed` do not false-positive; `prelogit exceed` and `relogit exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `logits exceed` rate limits, vocabulary / batch-dimension caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D371 | Ollama / router: `is_context_overflow_error` — `embeddings exceed` / `embedding exceed` + same context-slot guard as FEAT-D295 | Recognizes embedding-table / vector-ingest or batch API wording where the subject is `embeddings` (or singular `embedding` via substring `embedding exceed`) with `exceed` / `exceeded`, parallel to `positions exceed` / `position exceed`. `embedding exceed` matches `embedding exceeds` / `embedding exceeded` and does not substring-match plural `embeddings exceed` (the `s` after `embedding` breaks `embedding` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microembeddings exceed` / `metaembeddings exceed` / `subembedding exceed` do not false-positive; `preembedding exceed` and `reembedding exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `embeddings exceed` rate limits, vector-dimension / batch-width caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D370 | Ollama / router: `is_context_overflow_error` — `positions exceed` / `position exceed` + same context-slot guard as FEAT-D295 | Recognizes transformer / sequence-index or batch API wording where the subject is `positions` (or singular `position` via substring `position exceed`) with `exceed` / `exceeded`, parallel to `heads exceed` / `head exceed`. `position exceed` matches `position exceeds` / `position exceeded` and does not substring-match plural `positions exceed` (the `s` after `position` breaks `position` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `micropositions exceed` / `metapositions exceed` / `subposition exceed` do not false-positive; `preposition exceed` and `reposition exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `positions exceed` rate limits, max-index / KV-slot caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D369 | Ollama / router: `is_context_overflow_error` — `heads exceed` / `head exceed` + same context-slot guard as FEAT-D295 | Recognizes multi-head attention / transformer or batch API wording where the subject is `heads` (or singular `head` via substring `head exceed`) with `exceed` / `exceeded`, parallel to `layers exceed` / `layer exceed`. `head exceed` matches `head exceeds` / `head exceeded` and does not substring-match plural `heads exceed` (the `s` after `head` breaks `head` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microheads exceed` / `metaheads exceed` / `subhead exceed` do not false-positive; `prehead exceed` and `rehead exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `heads exceed` rate limits, per-head dimension caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D368 | Ollama / router: `is_context_overflow_error` — `layers exceed` / `layer exceed` + same context-slot guard as FEAT-D295 | Recognizes NN / transformer or batch API wording where the subject is `layers` (or singular `layer` via substring `layer exceed`) with `exceed` / `exceeded`, parallel to `biases exceed` / `bias exceed`. `layer exceed` matches `layer exceeds` / `layer exceeded` and does not substring-match plural `layers exceed` (the `s` after `layer` breaks `layer` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microlayers exceed` / `metalayers exceed` / `sublayer exceed` do not false-positive; `prelayer exceed` and `relayer exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `layers exceed` rate limits, depth/module caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D367 | Ollama / router: `is_context_overflow_error` — `biases exceed` / `bias exceed` + same context-slot guard as FEAT-D295 | Recognizes ML / layer-norm or batch API wording where the subject is `biases` (or singular `bias` via substring `bias exceed`) with `exceed` / `exceeded`, parallel to `weights exceed` / `weight exceed`. `bias exceed` matches `bias exceeds` / `bias exceeded` and does not substring-match plural `biases exceed` (the `s` after `bias` breaks `bias` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microbiases exceed` / `metabiases exceed` / `subbias exceed` do not false-positive; `prebias exceed` and `rebias exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `biases exceed` rate limits, initializer / offset caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D366 | Ollama / router: `is_context_overflow_error` — `weights exceed` / `weight exceed` + same context-slot guard as FEAT-D295 | Recognizes ML / optimizer or batch API wording where the subject is `weights` (or singular `weight` via substring `weight exceed`) with `exceed` / `exceeded`, parallel to `gradients exceed` / `gradient exceed`. `weight exceed` matches `weight exceeds` / `weight exceeded` and does not substring-match plural `weights exceed` (the `s` after `weight` breaks `weight` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microweights exceed` / `metaweights exceed` / `subweight exceed` do not false-positive; `preweight exceed` and `reweight exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `weights exceed` rate limits, L2 / clip / parameter-count caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D365 | Ollama / router: `is_context_overflow_error` — `gradients exceed` / `gradient exceed` + same context-slot guard as FEAT-D295 | Recognizes ML / autograd or batch API wording where the subject is `gradients` (or singular `gradient` via substring `gradient exceed`) with `exceed` / `exceeded`, parallel to `activations exceed` / `activation exceed`. `gradient exceed` matches `gradient exceeds` / `gradient exceeded` and does not substring-match plural `gradients exceed` (the `s` after `gradient` breaks `gradient` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microgradients exceed` / `metagradients exceed` / `subgradient exceed` do not false-positive; `pregradient exceed` and `regradient exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `gradients exceed` rate limits, clip / Jacobian caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D364 | Ollama / router: `is_context_overflow_error` — `activations exceed` / `activation exceed` + same context-slot guard as FEAT-D295 | Recognizes ML / forward-pass or batch API wording where the subject is `activations` (or singular `activation` via substring `activation exceed`) with `exceed` / `exceeded`, parallel to `tensors exceed` / `tensor exceed`. `activation exceed` matches `activation exceeds` / `activation exceeded` and does not substring-match plural `activations exceed` (the `s` after `activation` breaks `activation` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microactivations exceed` / `metaactivations exceed` / `subactivation exceed` do not false-positive; `preactivation exceed` and `reactivation exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `activations exceed` rate limits, layer / feature-map caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D363 | Ollama / router: `is_context_overflow_error` — `tensors exceed` / `tensor exceed` + same context-slot guard as FEAT-D295 | Recognizes ML / graph / batch API wording where the subject is `tensors` (or singular `tensor` via substring `tensor exceed`) with `exceed` / `exceeded`, parallel to `dimensions exceed` / `dimension exceed`. `tensor exceed` matches `tensor exceeds` / `tensor exceeded` and does not substring-match plural `tensors exceed` (the `s` after `tensor` breaks `tensor` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microtensors exceed` / `metatensors exceed` / `subtensor exceed` do not false-positive; `pretensor exceed` and `retensor exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `tensors exceed` rate limits, graph node / operand caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D362 | Ollama / router: `is_context_overflow_error` — `dimensions exceed` / `dimension exceed` + same context-slot guard as FEAT-D295 | Recognizes tensor / multi-axis or batch API wording where the subject is `dimensions` (or singular `dimension` via substring `dimension exceed`) with `exceed` / `exceeded`, parallel to `metrics exceed` / `metric exceed`. `dimension exceed` matches `dimension exceeds` / `dimension exceeded` and does not substring-match plural `dimensions exceed` (the `s` after `dimension` breaks `dimension` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microdimensions exceed` / `metadimensions exceed` / `subdimension exceed` do not false-positive; `predimension exceed` and `redimension exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `dimensions exceed` rate limits, tensor rank / axis caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D361 | Ollama / router: `is_context_overflow_error` — `metrics exceed` / `metric exceed` + same context-slot guard as FEAT-D295 | Recognizes observability / metrics-ingest or batch API wording where the subject is `metrics` (or singular `metric` via substring `metric exceed`) with `exceed` / `exceeded`, parallel to `resources exceed` / `resource exceed`. `metric exceed` matches `metric exceeds` / `metric exceeded` and does not substring-match plural `metrics exceed` (the `s` after `metric` breaks `metric` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `micrometrics exceed` / `metametrics exceed` / `submetric exceed` do not false-positive; `premetric exceed` and `remetric exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `metrics exceed` rate limits, cardinality/series caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D360 | Ollama / router: `is_context_overflow_error` — `resources exceed` / `resource exceed` + same context-slot guard as FEAT-D295 | Recognizes OpenTelemetry Resource / batch API wording where the subject is `resources` (or singular `resource` via substring `resource exceed`) with `exceed` / `exceeded`, parallel to `scopes exceed` / `scope exceed`. `resource exceed` matches `resource exceeds` / `resource exceeded` and does not substring-match plural `resources exceed` (the `s` after `resource` breaks `resource` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microresources exceed` / `metaresources exceed` / `subresource exceed` do not false-positive; `preresource exceed` and `reresource exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `resources exceed` rate limits, per-resource attribute caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D359 | Ollama / router: `is_context_overflow_error` — `scopes exceed` / `scope exceed` + same context-slot guard as FEAT-D295 | Recognizes OpenTelemetry instrumentation-scope / batch API wording where the subject is `scopes` (or singular `scope` via substring `scope exceed`) with `exceed` / `exceeded`, parallel to `links exceed` / `link exceed`. `scope exceed` matches `scope exceeds` / `scope exceeded` and does not substring-match plural `scopes exceed` (the `s` after `scope` breaks `scope` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microscopes exceed` / `metascopes exceed` / `subscope exceed` do not false-positive; `prescope exceed` and `rescope exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `scopes exceed` rate limits, per-scope caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D358 | Ollama / router: `is_context_overflow_error` — tighten FEAT-D295 `messages exceed` / `message exceed` with ident-boundary (FEAT-D353 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for plural `messages exceed` / `messages exceeded` and singular `message exceed` (covers `message exceeds` / `message exceeded` via the `exceed` prefix) so embedded `messages exceed` inside `micromessages exceed` / `metamessages exceed` no longer matches with a context slot; `message exceed` inside `submessage exceed` / `premessage exceed` / `remessage exceed` likewise. Context-slot guard unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D357 | Ollama / router: `is_context_overflow_error` — `links exceed` / `link exceed` + same context-slot guard as FEAT-D295 | Recognizes OpenTelemetry / tracing API wording where the subject is `links` (or singular `link` via substring `link exceed`) with `exceed` / `exceeded`, parallel to `attributes exceed` / `attribute exceed`. `link exceed` matches `link exceeds` / `link exceeded` and does not substring-match plural `links exceed` (the `s` after `link` breaks `link` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microlinks exceed` / `metalinks exceed` / `sublink exceed` do not false-positive; `prelink exceed` and `relink exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `links exceed` rate limits, per-span link caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D356 | Ollama / router: `is_context_overflow_error` — `attributes exceed` / `attribute exceed` + same context-slot guard as FEAT-D295 | Recognizes OpenTelemetry / structured-payload API wording where the subject is `attributes` (or singular `attribute` via substring `attribute exceed`) with `exceed` / `exceeded`, parallel to `spans exceed` / `span exceed`. `attribute exceed` matches `attribute exceeds` / `attribute exceeded` and does not substring-match plural `attributes exceed` (the `s` after `attribute` breaks `attribute` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microattributes exceed` / `metaattributes exceed` / `subattribute exceed` do not false-positive; `preattribute exceed` and `reattribute exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `attributes exceed` rate limits, schema key-count caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D355 | Ollama / router: `is_context_overflow_error` — `spans exceed` / `span exceed` + same context-slot guard as FEAT-D295 | Recognizes OpenTelemetry / distributed-tracing API wording where the subject is `spans` (or singular `span` via substring `span exceed`) with `exceed` / `exceeded`, parallel to `traces exceed` / `trace exceed`. `span exceed` matches `span exceeds` / `span exceeded` and does not substring-match plural `spans exceed` (the `s` after `span` breaks `span` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microspans exceed` / `metaspans exceed` / `subspan exceed` do not false-positive; `prespan exceed` and `respan exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `spans exceed` rate limits, attribute/link caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D354 | Ollama / router: `is_context_overflow_error` — `traces exceed` / `trace exceed` + same context-slot guard as FEAT-D295 | Recognizes OpenTelemetry / distributed-tracing API wording where the subject is `traces` (or singular `trace` via substring `trace exceed`) with `exceed` / `exceeded`, parallel to `events exceed` / `event exceed`. `trace exceed` matches `trace exceeds` / `trace exceeded` and does not substring-match plural `traces exceed` (the `s` after `trace` breaks `trace` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microtraces exceed` / `metatraces exceed` / `subtrace exceed` do not false-positive; `pretrace exceed` and `retrace exceed` are rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `traces exceed` rate limits, span-depth caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D353 | Ollama / router: `is_context_overflow_error` — tighten FEAT-D307 `requests exceed` / `request exceed` with ident-boundary (FEAT-D295 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for the three FEAT-D307 phrases so embedded `requests exceed` inside `microrequests exceed` / `metarequests exceed` no longer matches with a context slot; `request exceed` inside `subrequest exceed` / `prerequest exceed` likewise. Context-slot guard unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D352 | Ollama / router: `is_context_overflow_error` — `events exceed` / `event exceed` + same context-slot guard as FEAT-D295 | Recognizes event-stream / CQRS / analytics API wording where the subject is `events` (or singular `event` via substring `event exceed`) with `exceed` / `exceeded`, parallel to `observations exceed` / `observation exceed`. `event exceed` matches `event exceeds` / `event exceeded` and does not substring-match plural `events exceed` (the `s` after `event` breaks `event` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microevents exceed` / `metaevents exceed` / `subevent exceed` do not false-positive; `preevent exceed` is rejected the same way; embedded `event exceed` inside `prevent exceed` is rejected by the boundary. Same explicit context-slot phrases. Negatives: HTTP `events exceed` rate limits, batch caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D351 | Ollama / router: `is_context_overflow_error` — tighten FEAT-D314 `records exceed` / `record exceed` with ident-boundary (FEAT-D295 style) | Replaces plain `contains` with `contains_phrase_after_ident_boundary` for the three FEAT-D314 phrases so embedded `records exceed` inside `microrecords exceed` / `metarecords exceed` no longer matches with a context slot; `record exceed` inside `subrecord exceed` / `rerecord exceed` / `prerecord exceed` likewise. Context-slot guard unchanged. Extends `does_not_match_unrelated_errors` tests. User-facing overflow message unchanged. |
| FEAT-D350 | Ollama / router: `is_context_overflow_error` — `observations exceed` / `observation exceed` + same context-slot guard as FEAT-D295 | Recognizes analytics / time-series / batch API wording where the subject is `observations` (or singular `observation` via substring `observation exceed`) with `exceed` / `exceeded`, parallel to `samples exceed` / `sample exceed`. `observation exceed` matches `observation exceeds` / `observation exceeded` and does not substring-match plural `observations exceed` (the `s` after `observation` breaks `observation` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microobservations exceed` / `metaobservations exceed` / `subobservation exceed` do not false-positive; `preobservation exceed` is rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `observations exceed` rate limits, batch-size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D349 | Ollama / router: `is_context_overflow_error` — `samples exceed` / `sample exceed` + same context-slot guard as FEAT-D295 | Recognizes audio / time-series / batch API wording where the subject is `samples` (or singular `sample` via substring `sample exceed`) with `exceed` / `exceeded`, parallel to `frames exceed` / `frame exceed`. `sample exceed` matches `sample exceeds` / `sample exceeded` and does not substring-match plural `samples exceed` (the `s` after `sample` breaks `sample` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microsamples exceed` / `metasamples exceed` / `subsample exceed` do not false-positive; `resample exceed` is rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `samples exceed` rate limits, batch-size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D348 | Ollama / router: `is_context_overflow_error` — `frames exceed` / `frame exceed` + same context-slot guard as FEAT-D295 | Recognizes video / streaming / batch API wording where the subject is `frames` (or singular `frame` via substring `frame exceed`) with `exceed` / `exceeded`, parallel to `packets exceed` / `packet exceed`. `frame exceed` matches `frame exceeds` / `frame exceeded` and does not substring-match plural `frames exceed` (the `s` after `frame` breaks `frame` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microframes exceed` / `metaframes exceed` / `subframe exceed` do not false-positive; `reframe exceed` is rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `frames exceed` rate limits, GOP/frame count caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D347 | Ollama / router: `is_context_overflow_error` — `packets exceed` / `packet exceed` + same context-slot guard as FEAT-D295 | Recognizes streaming / datagram API wording where the subject is `packets` (or singular `packet` via substring `packet exceed`) with `exceed` / `exceeded`, parallel to `fragments exceed` / `fragment exceed`. `packet exceed` matches `packet exceeds` / `packet exceeded` and does not substring-match plural `packets exceed` (the `s` after `packet` breaks `packet` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `micropackets exceed` / `metapackets exceed` / `subpacket exceed` do not false-positive; `repacket exceed` is rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `packets exceed` rate limits, MTU/packet count caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D346 | Ollama / router: `is_context_overflow_error` — `fragments exceed` / `fragment exceed` + same context-slot guard as FEAT-D295 | Recognizes streaming / reassembly API wording where the subject is `fragments` (or singular `fragment` via substring `fragment exceed`) with `exceed` / `exceeded`, parallel to `shards exceed` / `shard exceed`. `fragment exceed` matches `fragment exceeds` / `fragment exceeded` and does not substring-match plural `fragments exceed` (the `s` after `fragment` breaks `fragment` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microfragments exceed` / `metafragments exceed` / `subfragment exceed` do not false-positive; `refragment exceed` is rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `fragments exceed` rate limits, packet/fragment count caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D345 | Ollama / router: `is_context_overflow_error` — `shards exceed` / `shard exceed` + same context-slot guard as FEAT-D295 | Recognizes sharded / distributed-context API wording where the subject is `shards` (or singular `shard` via substring `shard exceed`) with `exceed` / `exceeded`, parallel to `pieces exceed` / `piece exceed`. `shard exceed` matches `shard exceeds` / `shard exceeded` and does not substring-match plural `shards exceed` (the `s` after `shard` breaks `shard` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microshards exceed` / `metashards exceed` / `subshard exceed` do not false-positive; `reshard exceed` is rejected the same way. Same explicit context-slot phrases. Negatives: HTTP `shards exceed` rate limits, replica/shard count caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D344 | Ollama / router: `is_context_overflow_error` — `pieces exceed` / `piece exceed` + same context-slot guard as FEAT-D295 | Recognizes batch / decomposed-payload API wording where the subject is `pieces` (or singular `piece` via substring `piece exceed`) with `exceed` / `exceeded`, parallel to `parts exceed` / `part exceed`. `piece exceed` matches `piece exceeds` / `piece exceeded` and does not substring-match plural `pieces exceed` (the `s` after `piece` breaks `piece` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `micropieces exceed` / `metapieces exceed` / `subpiece exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `pieces exceed` rate limits, puzzle/shard count caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D343 | Ollama / router: `is_context_overflow_error` — `parts exceed` / `part exceed` + same context-slot guard as FEAT-D295 | Recognizes multipart / batch API wording where the subject is `parts` (or singular `part` via substring `part exceed`) with `exceed` / `exceeded`, parallel to `bodies exceed` / `body exceed`. `part exceed` matches `part exceeds` / `part exceeded` and does not substring-match plural `parts exceed` (the `s` after `part` breaks `part` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microparts exceed` / `metaparts exceed` / `subpart exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `parts exceed` rate limits, multipart count/size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D342 | Ollama / router: `is_context_overflow_error` — `bodies exceed` / `body exceed` + same context-slot guard as FEAT-D295 | Recognizes HTTP / gateway / batch API wording where the subject is `bodies` (or singular `body` via substring `body exceed`) with `exceed` / `exceeded`, parallel to `cookies exceed` / `cookie exceed`. `body exceed` matches `body exceeds` / `body exceeded` and does not substring-match plural `bodies exceed` (the `s` after `body` breaks `body` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microbodies exceed` / `metabodies exceed` / `subbody exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `bodies exceed` rate limits, upload/body size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D341 | Ollama / router: `is_context_overflow_error` — `cookies exceed` / `cookie exceed` + same context-slot guard as FEAT-D295 | Recognizes HTTP / gateway / batch API wording where the subject is `cookies` (or singular `cookie` via substring `cookie exceed`) with `exceed` / `exceeded`, parallel to `headers exceed` / `header exceed`. `cookie exceed` matches `cookie exceeds` / `cookie exceeded` and does not substring-match plural `cookies exceed` (the `s` after `cookie` breaks `cookie` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microcookies exceed` / `metacookies exceed` / `subcookie exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `cookies exceed` rate limits, cookie count/size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D340 | Ollama / router: `is_context_overflow_error` — `headers exceed` / `header exceed` + same context-slot guard as FEAT-D295 | Recognizes HTTP / gateway / batch API wording where the subject is `headers` (or singular `header` via substring `header exceed`) with `exceed` / `exceeded`, parallel to `variables exceed` / `variable exceed`. `header exceed` matches `header exceeds` / `header exceeded` and does not substring-match plural `headers exceed` (the `s` after `header` breaks `header` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microheaders exceed` / `metaheaders exceed` / `subheader exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `headers exceed` rate limits, header name/size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D339 | Ollama / router: `is_context_overflow_error` — `variables exceed` / `variable exceed` + same context-slot guard as FEAT-D295 | Recognizes GraphQL / template / batch API wording where the subject is `variables` (or singular `variable` via substring `variable exceed`) with `exceed` / `exceeded`, parallel to `arguments exceed` / `argument exceed`. `variable exceed` matches `variable exceeds` / `variable exceeded` and does not substring-match plural `variables exceed` (the `s` after `variable` breaks `variable` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `metavariables exceed` / `hypervariables exceed` / `multivariable exceed` / `subvariable exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `variables exceed` rate limits, template substitution caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D338 | Ollama / router: `is_context_overflow_error` — `arguments exceed` / `argument exceed` + same context-slot guard as FEAT-D295 | Recognizes RPC / GraphQL style wording where the subject is `arguments` (or singular `argument` via substring `argument exceed`) with `exceed` / `exceeded`, parallel to `parameters exceed` / `parameter exceed`. `argument exceed` matches `argument exceeds` / `argument exceeded` and does not substring-match plural `arguments exceed` (the `s` after `argument` breaks `argument` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microarguments exceed` / `metaarguments exceed` / `subargument exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `arguments exceed` rate limits, per-call argument caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D337 | Ollama / router: `is_context_overflow_error` — `parameters exceed` / `parameter exceed` + same context-slot guard as FEAT-D295 | Recognizes REST / OpenAPI style wording where the subject is `parameters` (or singular `parameter` via substring `parameter exceed`) with `exceed` / `exceeded`, parallel to `schemas exceed` / `schema exceed`. `parameter exceed` matches `parameter exceeds` / `parameter exceeded` and does not substring-match plural `parameters exceed` (the `s` after `parameter` breaks `parameter` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microparameters exceed` / `metaparameters exceed` / `subparameter exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `parameters exceed` rate limits, per-route parameter caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D336 | Ollama / router: `is_context_overflow_error` — `schemas exceed` / `schema exceed` + same context-slot guard as FEAT-D295 | Recognizes JSON Schema / OpenAPI style wording where the subject is `schemas` (or singular `schema` via substring `schema exceed`) with `exceed` / `exceeded`, parallel to `properties exceed` / `property exceed`. `schema exceed` matches `schema exceeds` / `schema exceeded` and does not substring-match plural `schemas exceed` (the `s` after `schema` breaks `schema` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microschemas exceed` / `holoschemas exceed` / `subschema exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `schemas exceed` rate limits, `$ref` depth caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D335 | Ollama / router: `is_context_overflow_error` — `properties exceed` / `property exceed` + same context-slot guard as FEAT-D295 | Recognizes structured-payload / JSON-schema API wording where the subject is `properties` (or singular `property` via substring `property exceed`) with `exceed` / `exceeded`, parallel to `keys exceed` / `key exceed`. `property exceed` matches `property exceeds` / `property exceeded` and does not substring-match plural `properties exceed` (the `s` after `property` breaks `property` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microproperties exceed` / `subproperty exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `properties exceed` rate limits, schema nesting caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D334 | Ollama / router: `is_context_overflow_error` — `keys exceed` / `key exceed` + same context-slot guard as FEAT-D295 | Recognizes structured-payload / key-value API wording where the subject is `keys` (or singular `key` via substring `key exceed`) with `exceed` / `exceeded`, parallel to `values exceed` / `value exceed`. `key exceed` matches `key exceeds` / `key exceeded` and does not substring-match plural `keys exceed` (the `s` after `key` breaks `key` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `hotkeys exceed` / `turnkeys exceed` / `monkey exceed` / `passkey exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `keys exceed` rate limits, schema key-name caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D333 | Ollama / router: `is_context_overflow_error` — `values exceed` / `value exceed` + same context-slot guard as FEAT-D295 | Recognizes structured-payload / key-value API wording where the subject is `values` (or singular `value` via substring `value exceed`) with `exceed` / `exceeded`, parallel to `fields exceed` / `field exceed`. `value exceed` matches `value exceeds` / `value exceeded` and does not substring-match plural `values exceed` (the `s` after `value` breaks `value` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `eigenvalues exceed` / `meanvalues exceed` / `devalue exceed` / `overvalue exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `values exceed` rate limits, schema numeric caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D332 | Ollama / router: `is_context_overflow_error` — `fields exceed` / `field exceed` + same context-slot guard as FEAT-D295 | Recognizes structured-payload / form-field API wording where the subject is `fields` (or singular `field` via substring `field exceed`) with `exceed` / `exceeded`, parallel to `bits exceed` / `bit exceed`. `field exceed` matches `field exceeds` / `field exceeded` and does not substring-match plural `fields exceed` (the `s` after `field` breaks `field` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `battlefields exceed` / `cornfields exceed` / `afield exceed` / `subfield exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `fields exceed` rate limits, schema string caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D331 | Ollama / router: `is_context_overflow_error` — `bits exceed` / `bit exceed` + same context-slot guard as FEAT-D295 | Recognizes binary / bitmask / payload-size API wording where the subject is `bits` (or singular `bit` via substring `bit exceed`) with `exceed` / `exceeded`, parallel to `bytes exceed` / `byte exceed`. `bit exceed` matches `bit exceeds` / `bit exceeded` and does not substring-match plural `bits exceed` (the `s` after `bit` breaks `bit` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `megabits exceed` / `kilobits exceed` / `kilobit exceed` do not false-positive, and embedded `bit exceed` inside `rabbit exceed` is rejected. Same explicit context-slot phrases. Negatives: HTTP `bits exceed` rate limits, codec frame-size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D330 | Ollama / router: `is_context_overflow_error` — `bytes exceed` / `byte exceed` + same context-slot guard as FEAT-D295 | Recognizes binary / payload-size API wording where the subject is `bytes` (or singular `byte` via substring `byte exceed`) with `exceed` / `exceeded`, parallel to `characters exceed` / `character exceed`. `byte exceed` matches `byte exceeds` / `byte exceeded` and does not substring-match plural `bytes exceed` (the `s` after `byte` breaks `byte` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `megabytes exceed` / `kilobytes exceed` / `kilobyte exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `bytes exceed` rate limits, storage object-size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D329 | Ollama / router: `is_context_overflow_error` — `characters exceed` / `character exceed` + same context-slot guard as FEAT-D295 | Recognizes string-length / character-budget API wording where the subject is `characters` (or singular `character` via substring `character exceed`) with `exceed` / `exceeded`, parallel to `words exceed` / `word exceed`. `character exceed` matches `character exceeds` / `character exceeded` and does not substring-match plural `characters exceed` (the `s` after `character` breaks `character` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `megacharacters exceed` / `metacharacters exceed` / `noncharacter exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `characters exceed` rate limits, UI field-width caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D328 | Ollama / router: `is_context_overflow_error` — `words exceed` / `word exceed` + same context-slot guard as FEAT-D295 | Recognizes tokenization / vocabulary API wording where the subject is `words` (or singular `word` via substring `word exceed`) with `exceed` / `exceeded`, parallel to `sentences exceed` / `sentence exceed`. `word exceed` matches `word exceeds` / `word exceeded` and does not substring-match plural `words exceed` (the `s` after `word` breaks `word` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `buzzwords exceed` / `keywords exceed` / `crosswords exceed` / `buzzword exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `words exceed` rate limits, per-word syllable caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D327 | Ollama / router: `is_context_overflow_error` — `sentences exceed` / `sentence exceed` + same context-slot guard as FEAT-D295 | Recognizes long-document / NLP-ingest API wording where the subject is `sentences` (or singular `sentence` via substring `sentence exceed`) with `exceed` / `exceeded`, parallel to `paragraphs exceed` / `paragraph exceed`. `sentence exceed` matches `sentence exceeds` / `sentence exceeded` and does not substring-match plural `sentences exceed` (the `s` after `sentence` breaks `sentence` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microsentences exceed` / `microsentence exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `sentences exceed` rate limits, per-sentence token caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D326 | Ollama / router: `is_context_overflow_error` — `paragraphs exceed` / `paragraph exceed` + same context-slot guard as FEAT-D295 | Recognizes long-document / ingest API wording where the subject is `paragraphs` (or singular `paragraph` via substring `paragraph exceed`) with `exceed` / `exceeded`, parallel to `sections exceed` / `section exceed`. `paragraph exceed` matches `paragraph exceeds` / `paragraph exceeded` and does not substring-match plural `paragraphs exceed` (the `s` after `paragraph` breaks `paragraph` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `counterparagraphs exceed` / `counterparagraph exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `paragraphs exceed` rate limits, layout `paragraph exceed` width caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D325 | Ollama / router: `is_context_overflow_error` — `sections exceed` / `section exceed` + same context-slot guard as FEAT-D295 | Recognizes long-document / outline API wording where the subject is `sections` (or singular `section` via substring `section exceed`) with `exceed` / `exceeded`, parallel to `segments exceed` / `segment exceed`. `section exceed` matches `section exceeds` / `section exceeded` and does not substring-match plural `sections exceed` (the `s` after `section` breaks `section` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `subsections exceed` / `intersection exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `sections exceed` rate limits, outline `section exceed` depth caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D324 | Ollama / router: `is_context_overflow_error` — `segments exceed` / `segment exceed` + same context-slot guard as FEAT-D295 | Recognizes streaming / long-context API wording where the subject is `segments` (or singular `segment` via substring `segment exceed`) with `exceed` / `exceeded`, parallel to `blocks exceed` / `block exceed`. `segment exceed` matches `segment exceeds` / `segment exceeded` and does not substring-match plural `segments exceed` (the `s` after `segment` breaks `segment` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microsegments exceed` / `multisegment exceeds` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `segments exceed` rate limits, video `segment exceed` duration caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D323 | Ollama / router: `is_context_overflow_error` — `blocks exceed` / `block exceed` + same context-slot guard as FEAT-D295 | Recognizes RAG / chunk-ingest API wording where the subject is `blocks` (or singular `block` via substring `block exceed`) with `exceed` / `exceeded`, parallel to `tables exceed` / `table exceed`. `block exceed` matches `block exceeds` / `block exceeded` and does not substring-match plural `blocks exceed` (the `s` after `block` breaks `block` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `roadblocks exceed` / `roadblock exceed` / `sunblock exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `blocks exceed` rate limits, storage `block exceed` size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D322 | Ollama / router: `is_context_overflow_error` — `tables exceed` / `table exceed` + same context-slot guard as FEAT-D295 | Recognizes batch / multi-table or structured-data API wording where the subject is `tables` (or singular `table` via substring `table exceed`) with `exceed` / `exceeded`, parallel to `columns exceed` / `column exceed`. `table exceed` matches `table exceeds` / `table exceeded` and does not substring-match plural `tables exceed` (the `s` after `table` breaks `table` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `constables exceed` / `stable exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `tables exceed` rate limits, quota caps, layout `table exceed` sizing, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D321 | Ollama / router: `is_context_overflow_error` — `columns exceed` / `column exceed` + same context-slot guard as FEAT-D295 | Recognizes spreadsheet / table-ingest API wording where the subject is `columns` (or singular `column` via substring `column exceed`) with `exceed` / `exceeded`, parallel to `rows exceed` / `row exceed`. `column exceed` matches `column exceeds` / `column exceeded` and does not substring-match plural `columns exceed` (the `s` after `column` breaks `column` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `microcolumns exceed` / `multicolumn exceeds` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `columns exceed` rate limits, layout or display `column exceed` width caps, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D320 | Ollama / router: `is_context_overflow_error` — `rows exceed` / `row exceed` + same context-slot guard as FEAT-D295 | Recognizes spreadsheet / table-ingest API wording where the subject is `rows` (or singular `row` via substring `row exceed`) with `exceed` / `exceeded`, parallel to `cells exceed` / `cell exceed`. `row exceed` matches `row exceeds` / `row exceeded` and does not substring-match plural `rows exceed` (the `s` after `row` breaks `row` + space + `exceed`). Phrases use an ASCII alnum/`_` left boundary so `arrows exceed` / `throws exceed` / `arrow exceed` do not false-positive. Same explicit context-slot phrases. Negatives: HTTP `rows exceed` rate limits, layout or display `row exceed` size caps, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D319 | Ollama / router: `is_context_overflow_error` — `cells exceed` / `cell exceed` + same context-slot guard as FEAT-D295 | Recognizes spreadsheet / table-ingest API wording where the subject is `cells` (or singular `cell` via substring `cell exceed`) with `exceed` / `exceeded`, parallel to `lines exceed` / `line exceed`. `cell exceed` matches `cell exceeds` / `cell exceeded` and does not substring-match plural `cells exceed` (the `s` after `cell` breaks `cell` + space + `exceed`). Same explicit context-slot phrases. Negatives: HTTP `cells exceed` rate limits, formula or display `cell exceed` size caps, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D318 | Ollama / router: `is_context_overflow_error` — `lines exceed` / `line exceed` + same context-slot guard as FEAT-D295 | Recognizes batch / streaming / log-ingest API wording where the subject is `lines` (or singular `line` via substring `line exceed`) with `exceed` / `exceeded`, parallel to `files exceed` / `file exceed`. `line exceed` matches `line exceeds` / `line exceeded` and does not substring-match plural `lines exceed` (the `s` after `line` breaks `line` + space + `exceed`). Same explicit context-slot phrases. Negatives: HTTP `lines exceed` rate limits, log `line exceed` length caps, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D317 | Ollama / router: `is_context_overflow_error` — `files exceed` / `file exceed` + same context-slot guard as FEAT-D295 | Recognizes upload / RAG / corpus API wording where the subject is `files` (or singular `file` via substring `file exceed`) with `exceed` / `exceeded`, parallel to `documents exceed` / `document exceed`. `file exceed` matches `file exceeds` / `file exceeded` and does not substring-match plural `files exceed` (the `s` after `file` breaks `file` + space + `exceed`). Same explicit context-slot phrases. Negatives: HTTP `files exceed` rate limits, storage or upload `file exceed` size caps, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D316 | Ollama / router: `is_context_overflow_error` — `documents exceed` / `document exceed` + same context-slot guard as FEAT-D295 | Recognizes RAG / corpus API wording where the subject is `documents` (or singular `document` via substring `document exceed`) with `exceed` / `exceeded`, parallel to `chunks exceed` / `chunk exceed`. `document exceed` matches `document exceeds` / `document exceeded` and does not substring-match plural `documents exceed` (the `s` after `document` breaks `document` + space + `exceed`). Same explicit context-slot phrases. Negatives: HTTP `documents exceed` rate limits, storage or upload `document exceed` size caps, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D315 | Ollama / router: `is_context_overflow_error` — `chunks exceed` / `chunk exceed` + same context-slot guard as FEAT-D295 | Recognizes RAG / document-split API wording where the subject is `chunks` (or singular `chunk` via substring `chunk exceed`) with `exceed` / `exceeded`, parallel to `records exceed` / `record exceed`. `chunk exceed` matches `chunk exceeds` / `chunk exceeded` and does not substring-match plural `chunks exceed` (the `s` after `chunk` breaks `chunk` + space + `exceed`). Same explicit context-slot phrases. Negatives: HTTP `chunks exceed` rate limits, storage or upload `chunk exceed` size caps, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D314 | Ollama / router: `is_context_overflow_error` — `records exceed` / `record exceed` + same context-slot guard as FEAT-D295 | Recognizes batch/API wording where the subject is `records` (or singular `record` via substring `record exceed`) with `exceed` / `exceeded`, parallel to `entries exceed` / `entry exceed`. `record exceed` matches `record exceeds` / `record exceeded` and does not substring-match plural `records exceed` (the `s` after `record` breaks `record` + space + `exceed`). Same explicit context-slot phrases. Negatives: HTTP `records exceed` rate limits, audit `record exceed` field limits, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D313 | Ollama / router: `is_context_overflow_error` — `entries exceed` / `entry exceed` + same context-slot guard as FEAT-D295 | Recognizes batch/log API wording where the subject is `entries` (or singular `entry` via substring `entry exceed`) with `exceed` / `exceeded`, parallel to `items exceed` / `item exceed`. `entry exceed` matches `entry exceeds` / `entry exceeded` and does not substring-match plural `entries exceed` (`entr` + `ies` vs `entry` before space + `exceed`). Same explicit context-slot phrases. Negatives: HTTP `entries exceed` rate limits, catalog `entry exceed` field limits, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D312 | Ollama / router: `is_context_overflow_error` — `items exceed` / `item exceed` + same context-slot guard as FEAT-D295 | Recognizes batch/list API wording where the subject is `items` (or singular `item` via substring `item exceed`) with `exceed` / `exceeded`, parallel to `tokens exceed` / `token exceed`. `item exceed` matches `item exceeds` / `item exceeded` and does not substring-match plural `items exceed` (the `s` after `item` breaks `item` + space + `exceed`). Same explicit context-slot phrases. Negatives: cart or queue `items exceed` limits, HTTP rate limits, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D311 | Ollama / router: `is_context_overflow_error` — `tokens exceed` / `token exceed` + same context-slot guard as FEAT-D295 | Recognizes token-budget / API wording where the subject is `tokens` (or singular `token` via substring `token exceed`) with `exceed` / `exceeded`, parallel to `batches exceed` / `batch exceed`. `token exceed` matches `token exceeds` / `token exceeded` and does not substring-match plural `tokens exceed` (the `s` after `token` breaks `token` + space + `exceed`). Same explicit context-slot phrases. Negatives: billing `tokens exceed` caps, HTTP rate limits, daily quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D310 | Ollama / router: `is_context_overflow_error` — `batches exceed` / `batch exceed` + same context-slot guard as FEAT-D295 | Recognizes batch-API wording where the subject is `batches` (or singular `batch` via substring `batch exceed`) with `exceed` / `exceeded`, parallel to `calls exceed` / `queries exceed`. `batch exceed` matches `batch exceeds` / `batch exceeded` and does not substring-match plural `batches exceed` (the `es` after `batch` breaks `batch` + space + `exceed`). Same explicit context-slot phrases. Negatives: HTTP `batches exceed` rate limits, job-queue size caps, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D309 | Ollama / router: `is_context_overflow_error` — `calls exceed` / `call exceed` + same context-slot guard as FEAT-D295 | Recognizes tool-calling / batch API wording where the subject is `calls` (or singular `call` via substring `call exceed`) with `exceed` / `exceeded`, parallel to `queries exceed` / `requests exceed`. `call exceed` matches `call exceeds` / `call exceeded` and does not false-positive on plural `calls exceed` (the substring `call exceed` does not occur inside `calls exceed`). Same explicit context-slot phrases. Negatives: HTTP `calls exceed` rate limits, quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D308 | Ollama / router: `is_context_overflow_error` — `queries exceed` / `query exceed` + same context-slot guard as FEAT-D295 | Recognizes batch/API wording where the subject is `queries` (or singular `query` via substring `query exceed`) with `exceed` / `exceeded`, parallel to `requests exceed` / `responses exceed`. `query exceed` matches `query exceeds` / `query exceeded` and does not false-positive on plural `queries exceed` (the substring `query exceed` does not occur inside `queries exceed`). Same explicit context-slot phrases. Negatives: database or HTTP `queries exceed` rate limits, execution time, quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D307 | Ollama / router: `is_context_overflow_error` — `requests exceed` / `request exceed` + same context-slot guard as FEAT-D295 | Recognizes batch/API wording where the subject is `requests` (or singular `request` via substring `request exceed`) with `exceed` / `exceeded`, parallel to `responses exceed` / `outputs exceed`. `request exceed` matches `request exceeds` / `request exceeded` and does not substring-match plural `requests exceed`. Same explicit context-slot phrases. Negatives: HTTP `requests exceed` rate limits, quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D306 | Ollama / router: `is_context_overflow_error` — `responses exceed` / `response exceed` + same context-slot guard as FEAT-D295 | Recognizes batch/API wording where the subject is `responses` (or singular `response` via substring `response exceed`) with `exceed` / `exceeded`, parallel to `outputs exceed` / `inputs exceed`. `response exceed` matches `response exceeds` / `response exceeded` and does not substring-match plural `responses exceed`. Same explicit context-slot phrases. Negatives: HTTP `responses exceed` rate limits, quotas, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D305 | Ollama / router: `is_context_overflow_error` — `outputs exceed` / `output exceed` + same context-slot guard as FEAT-D295 | Recognizes batch/API wording where the subject is `outputs` (or singular `output` via substring `output exceed`) with `exceed` / `exceeded`, parallel to `inputs exceed` / `contents exceed`. `output exceed` matches `output exceeds` / `output exceeded` and does not substring-match plural `outputs exceed`. Same explicit context-slot phrases. Negatives: pipeline/GPU `outputs exceed` limits without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D304 | Ollama / router: `is_context_overflow_error` — `beyond` / `over` / `outside` + `context window` without intervening `the` (extends FEAT-D291) | Recognizes compact API phrasing `beyond context window`, `over context window`, `outside context window`, and `outside of context window` where `beyond the context window` / `over the context window` / `outside of the context window` do not substring-match (e.g. `prompt extends beyond context window`). Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D303 | Ollama / router: `is_context_overflow_error` — `contents exceed` / `content exceed` + same context-slot guard as FEAT-D295 | Recognizes batch/API wording where the subject is `contents` with `exceed` / `exceeded` (e.g. `contents exceed the model's context window`), which does not substring-match `exceeds the context window`. Singular `content exceed` matches `content exceeds` / `content exceeded` and does not substring-match plural `contents exceed` (the `s` after `content` breaks the substring). Same explicit context-slot phrases as `messages exceed` / `inputs exceed`. Negatives: `contents exceed` attachment size or rate limits without slot wording (including incidental `no model context configured`). Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D302 | Ollama / router: `is_context_overflow_error` — plural `inputs are too long` / `inputs were too long` + same context-slot guard as FEAT-D296 | Recognizes batch-style wording where the subject is plural `inputs` with `are` / `were too long` (parallel to `messages are too long` / `messages were too long`). Same explicit context-slot phrases as guarded `message(s)`/`input` + `too long`. Does not substring-match singular `input is` / `input was`. Negatives: form/UI `inputs are too long` without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D301 | Ollama / router: `is_context_overflow_error` — possessive `model's context` as context-slot phrase (FEAT-D295-style guards) | Extends the explicit slot list for `messages exceed` / `message exceed(s/ed)` / `inputs exceed` / `input exceed` / guarded `message(s)`/`input` + `too long` with substring `model's context`, so wording like `your message is too long for this model's context` matches without requiring `context window` / `max context` / etc. Does not substring-match bare `model context` in lines such as `(no model context configured)`. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D300 | Ollama / router: `is_context_overflow_error` — singular `input exceed` / `exceeds` / `exceeded` + same context-slot guard as FEAT-D295 | Recognizes one-shot / single-payload API errors where the subject is singular `input` with `exceed` / `exceeds` / `exceeded` (matched via substring `input exceed`, which covers `exceeds` and `exceeded` but does **not** match plural `inputs exceed`). Same explicit context-slot phrases as `inputs exceed` / `messages exceed`. Fills gaps such as `input exceeded available context` where `exceeded available context` alone is not a prior arm. Negatives: `input exceed` rate limits, attachment size, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D299 | Ollama / router: `is_context_overflow_error` — plural `inputs exceed` / `inputs exceeded` + same context-slot guard as FEAT-D295 | Recognizes batch / multi-part API errors where the subject is plural `inputs` with present `exceed` or past `exceeded` (e.g. `inputs exceed the model's context window`), which do not substring-match `exceeds the context window`. Same explicit context-slot phrases as `messages exceed` / singular `message exceed(s/ed)` so incidental `model context` in unrelated lines does not match. Negatives: `inputs exceed` rate limits, attachment size, etc. without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D298 | Ollama / router: `is_context_overflow_error` — singular `message exceeds` / `message exceeded` + same context-slot guard as FEAT-D295 | Recognizes chat/API errors where a single user message is the subject (e.g. `message exceeds the model's context window`, `your message exceeded maximum context`) with the same explicit context-slot phrases as plural `messages exceed`. Distinct from `message is too long` (FEAT-D296) and does not substring-match `messages exceed`. Negatives: generic `message exceeds` sizing/UI limits without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D297 | Ollama / router: `is_context_overflow_error` — `maximum allowed context` / `max allowed context`; guarded `context length limit`; bounded `context_budget_exceeded` | Recognizes `maximum allowed context` and `max allowed context` with the same overflow guards as contiguous `maximum context` / `max context` (the word `allowed` breaks those substrings). Recognizes combined `context length limit` with `exceed` / `overflow` / `too long` / `too large` / `larger` / `greater` (covers `… limit exceeded` via the `exceed` substring). Recognizes bounded API-style token `context_budget_exceeded`. Negatives: informational sizing without overflow signals; `old_context_budget_exceeded`-style prefixed identifiers. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D296 | Ollama / router: `is_context_overflow_error` — `message(s)` / `input` + `too long` + same context-slot guard as FEAT-D295 | Recognizes `message is too long`, `messages are too long`, past-tense `message was` / `messages were`, and `input is` / `input was too long` when paired with the same explicit context-slot phrases as `messages exceed` (`context window`, `context length`, `context limit`, `context size`, `max context`, `maximum context`, `available context`). Distinct from unguarded `prompt too long` / `prompt is too long`. Negatives: platform character limits (`message is too long (max 2000 characters)`), generic form validation without slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D295 | Ollama / router: `is_context_overflow_error` — `messages exceed` / `messages exceeded` + explicit context-slot phrases | Recognizes chat-style errors where the plural `messages` is the subject (e.g. `messages exceed the model's context window`, `messages exceeded maximum context`, `messages exceed available context`). Requires one of `context window`, `context length`, `context limit`, `context size`, `max context`, `maximum context`, or `available context` so lines like `status messages exceed rate limits (no model context configured)` do not false-positive on bare `model context`. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D294 | Ollama / router: `is_context_overflow_error` — bounded API tokens + demonstrative `this model's context`; bounded snake/camel option keys with overflow guards | Recognizes OpenAI-style / JSON `code` values `context_length_exceeded`, `max_context_length_exceeded`, `context_window_exceeded`, `max_context_exceeded` via `contains_bounded_token` (avoids `old_*` column/flag names embedding those substrings). Recognizes `exceeds` / `exceeded` / `exceed this model's context`. Tightens combined arms for `max_context`, `context_length`, `context_window`, `context_limit`, `maxcontext`, `contextlength`, `contextlimit`, `n_ctx_per_seq`, `n_ctx`, and `num_ctx` to the same boundaries so `exceeded` / `exceed` substrings on suffixed identifiers do not false-positive. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D293 | Ollama / router: `is_context_overflow_error` — `running out of context`; guarded `context budget`; `conversation` + `too long` + `context` | Recognizes progressive `running out of context` (distinct from past-tense `ran out of context`). Recognizes combined `context budget` with `exceed` / `overflow` / `full` (avoids `exhaust` substring false positives on words like `inexhaustible`). Recognizes `conversation` with `too long` and `context` (e.g. `conversation is too long for the model context window`). Negatives: `running out of` non-context idioms, informational `context budget` lines without exceed/overflow/full, long conversation UI copy without `context`. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D292 | Ollama / router: `is_context_overflow_error` — guarded `maximum context` / `max context` when tokens sit between `exceed` and `maximum` | Recognizes combined `maximum context` with `exceed` / `overflow` / `too long` / `too large` / `larger` / `greater` (e.g. `exceeds model maximum context`, `allowed maximum context`, `too large for max context`) where contiguous `exceeds maximum context` is not a substring. Same guards for `max context` (distinct substring from `maximum context`). Negatives: informational lines with `maximum context` / `max context` but no overflow signals. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D291 | Ollama / router: `is_context_overflow_error` — possessive `model's maximum context`; `beyond` / `over` / `outside of` the context window | Recognizes `exceeds the model's maximum context`, `exceeded the model's maximum context`, `exceed the model's maximum context` (fills gap where `exceeds maximum context` is not a substring—e.g. `the model's` between tokens). Recognizes `beyond the context window`, `over the context window`, `outside of the context window` (extends `outside the context window` / `beyond the context`). Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D290 | Ollama / router: `is_context_overflow_error` — `context_limit` / `contextLimit`; `exceed the maximum context` / `exceeded maximum context` / `maximum context is exceeded`; `context token limit` phrasing | Recognizes snake_case `context_limit` and camelCase `contextlimit` (after lowercasing) with the same overflow guards as `context_length` (`exceed` / `overflow` / `too long` / `too large`). Recognizes `exceed the maximum context`, `exceeded maximum context`, `maximum context is exceeded`, and `context token limit exceeded` / `exceeds the context token limit` / `exceeded the context token limit` (distinct from bare API “token limit” without `context token`). Negatives: bare `context_limit=…` / schema field lists, bare `contextLimit=…` sizing, `API token limit exceeded` without context-slot wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D289 | Ollama / router: `is_context_overflow_error` — `model context` limit phrasing + snake_case `context_window` | Recognizes `exceeds the model context`, `exceeded the model context`, `exceed the model context`, `model context exceeded`, `model context limit exceeded` (wording where `exceeds the model's context window` does not substring-match, e.g. `exceeds the model context limit`). Recognizes combined `context_window` with `exceed` / `overflow` / `too long` / `too large` / `larger` / `greater` / `too small` (JSON/config field distinct from prose `context window`). Negatives: bare `context_window=…` sizing, schema-only `context_window` prose, neutral “model context” tuning notes without exceed/limit-exceeded phrasing. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D288 | Ollama / router: `is_context_overflow_error` — camelCase `maxContext` / `contextLength` + `n_ctx_per_seq` | Recognizes `maxcontext` / `contextlength` (JSON camelCase after lowercasing) with the same overflow guards as snake_case `max_context` / `context_length`, and `n_ctx_per_seq` with `exceed` / `overflow` / `too long` / `too large` / `larger` / `greater` / `too small`. Negatives: bare camelCase sizing lines, schema prose mentioning `contextLength` without overflow signals, diagnostic `n_ctx_per_seq=…` without overflow wording. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D287 | Ollama / router: `is_context_overflow_error` — API-style `max_context` + `context_length` | Recognizes combined `max_context` with `exceed` / `overflow` / `too long` / `too large` / `larger` / `greater`, and combined `context_length` with `exceed` / `overflow` / `too long` / `too large` (snake_case / JSON-style field names). Negatives: bare `max_context=…` sizing, field lists mentioning `context_length` without overflow signals. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D286 | Ollama / router: `is_context_overflow_error` — `context size` phrasing, `context window` + `too small`, `context capacity` / `allocated context`, guarded `prefill` + `context` | Recognizes `exceeds the context size`, `context size exceeds`, `exceeded the context size`, combined `context window` with `too small`, combined `context capacity` with `exceed` / `overflow` / `full`, combined `allocated context` with `exceed` / `overflow` / `full`, and `prefill` with `context` and `exceed` / `overflow` / `too long`. Negatives: `prefill` + `context` without overflow signals; `allocated context` sizing prose without exceed/overflow/full. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D285 | Ollama / router: `is_context_overflow_error` — exhaustion + guarded KV-cache-full phrasing | Recognizes `context exhausted`, combined `context` with `fully exhausted` / `completely exhausted`, `insufficient remaining context`, and combined `kv cache` / `kv-cache` with `is full` and `context`. Does not use bare `context is full` (substring of `context is fully …`). Negatives: marketing-style “fully supported … context”, GPU `kv cache` full without `context`. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D284 | Ollama / router: `is_context_overflow_error` — `exceeds the context limit` / `exceed the context limit` | Recognizes present-tense and infinitive phrasing distinct from existing `exceeded the context limit` and `context limit exceeded` (e.g. `exceeds the context limit`, `cannot exceed the context limit`). Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D283 | Ollama / router: `is_context_overflow_error` — configured context, limits, truncation phrasing | Recognizes `exceeds the configured context`, combined `configured context` with `overflow` / `exceed` / `full`, combined `would exceed` … `context`, `past the context`, `reached the context limit`, `hit the context limit`, `over the context limit`, and `truncated` with `context window` / `context length` / `context limit` / `max context`. Negatives: plain `truncated` without those context phrases (e.g. display truncation, “trace for context”). Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D282 | Ollama / router: `is_context_overflow_error` — `max context` / `beyond the context` / `not enough context`; `context buffer` + overflow/exceed/full; guarded `prompt tokens exceed` / `input tokens exceed` | Recognizes `max context exceeded`, `exceeds max context`, `beyond the context`, `not enough context`, combined `context buffer` with `overflow` / `exceed` / `full`, and `prompt tokens exceed` or `input tokens exceed` when `context` / `model` / `maximum` / `sequence` / `n_ctx` / `window` is present (parity with `total tokens exceed` guard). Negatives: bare `prompt tokens exceed` billing cap, non-context `buffer full`. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D281 | Ollama / router: `is_context_overflow_error` — sequence-length / prompt-length / token-count phrasing | Recognizes `exceeds maximum sequence length`, `maximum sequence length exceeded`, `sequence length exceeds`, `input sequence is too long`, `prompt length exceeds`, combined `total tokens exceed` with `context` / `model` / `maximum` / `sequence` / `n_ctx`, and `too many tokens` + `context`. Negative: bare `too many tokens` (rate limit), `total tokens exceed` + quota (no model/context keywords). Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D280 | Ollama / router: `is_context_overflow_error` — `greater than` + `context`; `n_ctx` + `exceed` / `overflow` / `too small`; `num_ctx` + `exceed` / `larger` / `greater` / `longer` | Extends FEAT-D279-style coverage for wording that names llama `n_ctx` / Ollama `num_ctx` without repeating “context window”, and “greater than … context” (distinct from existing `longer than` … `context`). Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D279 | Ollama / router: `is_context_overflow_error` — `prompt is too long`, `exceeds maximum context`, `maximum context exceeded`, `insufficient context`, `prompt exceeds the context`, `doesn't fit` / `unable to fit` / `won't fit` + `context` | Extends FEAT-D278: fixes gap where `the prompt is too long` did not match `prompt too long`; contraction and “unable/won’t fit … context” combos. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D278 | Ollama / router: `is_context_overflow_error` — `context limit exceeded`, `exceeds context length`, `requested tokens exceed`, `too large` + `context`, `cannot fit` + `context`, `does not fit` + `context` | Extends FEAT-D277-style coverage for alternate word order and batch/prompt sizing wording from llama-style backends. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D277 | Ollama / router: `is_context_overflow_error` — `context size exceeded`, `exceeded context size`, `prompt has more tokens than`, `exceeds available context`, `longer than` + `context` | Extends FEAT-D276-style coverage for n_ctx / allocation-style and “prompt longer than context length” wording from llama-style backends. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D276 | Ollama / router: `is_context_overflow_error` — `fit in the context`, `larger than the context`, `outside the context window`, `ran out of context` | Extends FEAT-D275-style coverage for slot/prompt sizing and generation-boundary wording from llama-style backends. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D275 | Ollama / router: `is_context_overflow_error` + `sanitize_ollama_error_for_user` — extra llama-runner / context phrases | Extends FEAT-D28-style coverage: `exceeds the context window` (no possessive), `context window exceeded`, `exceeded the context limit`, `requested more tokens than`, `total prompt tokens exceed`. Unit tests in `content_reduction.rs`. User-facing overflow message unchanged. |
| FEAT-D274 | HTML `clean_html`: CJK Symbols—official `UnicodeData.txt` names for **IDEOGRAPHIC ITERATION MARK** U+3005 and **VERTICAL IDEOGRAPHIC ITERATION MARK** U+303B (`Lm`) in comments | Narrative used shorthand “iteration marks U+3005 / U+303B”. `collapse_whitespace()` “Omitted on purpose” block + `cjk_symbols_brackets_ditto_wave_vertical_repeat_masu_half_fill_separate_words` test doc note now spell official scalar names (both `Lm`). No tokenizer change. |
| FEAT-D273 | HTML `clean_html`: CJK Symbols—document **IDEOGRAPHIC SPACE** U+3000 (`Zs`): Rust whitespace, not on `match` arm; split+join vs NBSP `=> ' '` | **IDEOGRAPHIC SPACE** is `Zs` and `is_whitespace`; `collapse_whitespace` does not map it on the CJK punctuation arm (contrast **NO-BREAK SPACE** U+00A0 explicitly mapped to U+0020 for plain-space–only output). Block comment before U+3001/U+3002 explains per-line `split_whitespace()` already splits on U+3000; between Latin tokens, join emits ASCII spaces only. Test `ideographic_space_zs_u3000_separates_words_plain_ascii_between_tokens`. No tokenizer change. |
| FEAT-D272 | HTML `clean_html`: CJK Symbols—official `UnicodeData.txt` scalar names for U+3008–U+301B brackets, POSTAL MARK, GETA MARK in comments | Narrative used only “CJK brackets and postal/geta marks (U+3008–U+301B, Ps/Pe/So)”. `collapse_whitespace()` block comment now spells LEFT/RIGHT ANGLE / DOUBLE ANGLE / CORNER / WHITE CORNER / BLACK LENTICULAR / TORTOISE SHELL / WHITE LENTICULAR / WHITE TORTOISE SHELL / WHITE SQUARE BRACKET pairs with categories, plus POSTAL MARK and GETA MARK. `cjk_symbols_brackets_ditto_wave_vertical_repeat_masu_half_fill_separate_words` test doc note: bracket Ps/Pe summary + U+3012/U+3013 names; references FEAT-D272. No tokenizer change. |
| FEAT-D271 | HTML `clean_html`: CJK Symbols—official `UnicodeData.txt` scalar names for U+3003 DITTO MARK, U+301C WAVE DASH, U+301D–U+301F prime quotation marks, U+3020 POSTAL MARK FACE, U+303C MASU MARK, U+303E–U+303F variation / half fill, U+3030 WAVY DASH, U+3037 IDEOGRAPHIC TELEGRAPH LINE FEED SEPARATOR SYMBOL, U+303D PART ALTERNATION MARK, U+30A0 KATAKANA-HIRAGANA DOUBLE HYPHEN, U+FF3F FULLWIDTH LOW LINE in comments | Narrative used informal lowercase (“ditto mark”, “wave dash”, “postal mark face”, “masu mark”, shortened prime names, “ideographic variation indicator / half fill space”, “Wavy dash”, “ideographic telegraph line-feed separator”, “part alternation mark”, “Katakana-Hiragana double hyphen”, “fullwidth low line”). `collapse_whitespace()` block comment + `cjk_symbols_brackets_ditto_wave_vertical_repeat_masu_half_fill_separate_words` test doc note updated to official names. No tokenizer change. |
| FEAT-D270 | HTML `clean_html`: CJK Symbols—U+3001/U+3002 official names **IDEOGRAPHIC COMMA** / **IDEOGRAPHIC FULL STOP** (`Po`) in comments | Narrative used colloquial “ideographic comma / full stop” only; official `UnicodeData.txt` names are **IDEOGRAPHIC COMMA** (U+3001) and **IDEOGRAPHIC FULL STOP** (U+3002). `collapse_whitespace()` block comment + `cjk_fullwidth_and_vertical_forms_punctuation_separate_words` test doc note updated (colloquial gloss retained). No tokenizer change. |
| FEAT-D269 | HTML `clean_html`: CJK Symbols—U+3007 official name **IDEOGRAPHIC NUMBER ZERO** (`Nl`) in comments | Narrative used shorthand “ideographic zero”; official `UnicodeData.txt` name is **IDEOGRAPHIC NUMBER ZERO** (`Nl`). `collapse_whitespace()` block comment + `cjk_symbols_brackets_ditto_wave_vertical_repeat_masu_half_fill_separate_words` test doc note (U+3007 stays unmapped with Hangzhou `Nl` numerics). Same pass spells **IDEOGRAPHIC CLOSING MARK** for U+3006 in both places (was “ideographic closing mark” / “3006 is” in the test). No tokenizer change. |
| FEAT-D268 | HTML `clean_html`: CJK Symbols—U+3004 official name **JAPANESE INDUSTRIAL STANDARD SYMBOL** (`So`) in comments | Narrative used shorthand “JIS symbol” only; official `UnicodeData.txt` name is **JAPANESE INDUSTRIAL STANDARD SYMBOL**. `collapse_whitespace()` block comment + `cjk_symbols_brackets_ditto_wave_vertical_repeat_masu_half_fill_separate_words` test doc note updated (JIS kept as colloquial gloss). No tokenizer change. |
| FEAT-D267 | CHANGELOG `[0.1.56]`: remove duplicate FEAT-D257 bullet contradicting FEAT-D266 on U+3006 | **FEAT-D266** already states U+3006 is `Lo` and FEAT-D257 had categories backwards. A second **Changed** bullet still claimed U+3006 was `Po` “not `Lo`” with a footnote—duplicate and confusing. Deleted that row; appended one **Changelog** bullet documenting the cleanup (FEAT-D267). No code change. |
| FEAT-D266 | HTML `clean_html`: CJK Symbols—U+3006 IDEOGRAPHIC CLOSING MARK is `Lo` in UnicodeData (FEAT-D257 was wrong) | Official `UnicodeData.txt` assigns **IDEOGRAPHIC CLOSING MARK** U+3006 as **`Lo`**, not `Po`. **FEAT-D257** had reversed the categories. `collapse_whitespace()` block comment + `cjk_symbols_brackets_ditto_wave_vertical_repeat_masu_half_fill_separate_words` doc note corrected; match arms unchanged (〆 still maps—typography / FETCH_URL Latin token boundaries). |
| FEAT-D265 | HTML `clean_html`: Number Forms comment—U+218A–U+218B TURNED DIGIT TWO/THREE are `So` in UnicodeData, not `No`/`Nl` | Unicode 16 `UnicodeData.txt` assigns **TURNED DIGIT TWO** U+218A and **TURNED DIGIT THREE** U+218B as `So` (symbol digits). **FEAT-D263** had claimed every mapped scalar in the Number Forms arms (except U+2183/U+2184) was `No` or `Nl`. `collapse_whitespace()` match arms unchanged—still word separators; corrected block comment + `number_forms_fractions_romans_and_turned_digits_separate_words` doc note in `commands/html_cleaning.rs`. No tokenizer change. |
| FEAT-D264 | HTML `clean_html`: CJK Symbols comment—U+301D `Ps`, U+301E–U+301F both `Pe` | UnicodeData assigns **REVERSED DOUBLE PRIME QUOTATION MARK** U+301D as `Ps` and **DOUBLE PRIME QUOTATION MARK** U+301E plus **LOW DOUBLE PRIME QUOTATION MARK** U+301F as `Pe`; the `collapse_whitespace()` narrative listed “reversed/double-prime quotes (U+301D–U+301F)” without categories, which could mislead edits expecting a strict ascending Ps/Pe pair. Corrected block comment + `cjk_symbols_brackets_ditto_wave_vertical_repeat_masu_half_fill_separate_words` doc note in `commands/html_cleaning.rs`. No tokenizer change. |
| FEAT-D263 | HTML `clean_html`: Number Forms comment—mapped U+2150–U+2182 + U+2185–U+218B are `No`/`Nl` only, not `So` | **Superseded for U+218A–U+218B:** UnicodeData assigns those as `So` (**FEAT-D265**). Historical row text claimed every mapped scalar (except U+2183/U+2184) was `No` or `Nl`. Intent was to drop vague “No / Nl / So” lumping vs Letterlike arms. Corrected block comment + test doc note in `commands/html_cleaning.rs` (then refined again in D265). No tokenizer change. |
| FEAT-D262 | HTML `clean_html`: Letterlike tail—document U+214A–U+214D + U+214F before Number Forms | Match arms already map PROPERTY LINE / TURNED AMPERSAND / PER SIGN / AKTIESELSKAB (U+214A–U+214D) and SYMBOL FOR SAMARITAN SOURCE (U+214F); the long `collapse_whitespace()` Letterlike narrative jumped from U+2140–U+2144 to Number Forms without naming this tail, which could mislead range edits. Added block-comment lines (Unicode scalar names + categories); U+214E TURNED SMALL F (`Ll`) explicitly stays unmapped. Test doc note in `letterlike_symbol_subranges_separate_words`; `letterlike_mathematical_letters_stay_unmapped` includes U+214E. No tokenizer change. |
| FEAT-D261 | HTML `clean_html`: Letterlike comment—U+2140–U+2144 are `Sm`, not `So` | UnicodeData assigns DOUBLE-STRUCK N-ARY SUMMATION through TURNED SANS-SERIF CAPITAL G U+2140–U+2144 as `Sm`; the `collapse_whitespace()` narrative line after FEAT-D246/FEAT-D251 called that contiguous run `So` (“double-struck Pi / empty-set”), which could mislead edits vs Mathematical Operators `Sm` arms. Corrected comment + test doc note in `letterlike_symbol_subranges_separate_words` in `commands/html_cleaning.rs`. No tokenizer change. |
| FEAT-D260 | HTML `clean_html`: CJK Symbols comment—U+303C MASU MARK is `So`, not `Lo` | UnicodeData classifies **MASU MARK** U+303C as `So`; the `collapse_whitespace()` block comment called it `Lo`, which could mislead future range edits (iteration marks U+3005 / U+303B are `Lm`). Corrected comment + test doc note in `commands/html_cleaning.rs`. No tokenizer change. |
| FEAT-D259 | HTML `clean_html`: CJK Symbols comment—U+3036 CIRCLED POSTAL MARK is `So`, not `Lm` | UnicodeData classifies **CIRCLED POSTAL MARK** U+3036 as `So`; the `collapse_whitespace()` block comment grouped U+3031–U+3036 as all `Lm`, which could mislead future edits (U+3031–U+3035 are `Lm`). Corrected comment + test doc note in `commands/html_cleaning.rs`. No tokenizer change. |
| FEAT-D258 | HTML `clean_html`: NBSP (`U+00A0`) documentation—Rust `split_whitespace` vs ASCII-only output | **NO-BREAK SPACE** is Unicode `Zs`; current Rust `char::is_whitespace` / `split_whitespace` treat it as a separator, so the old `nbsp_separates_words` note (“not Unicode whitespace”, “one token”) was misleading. `collapse_whitespace` still maps U+00A0 → U+0020 for ASCII-space–only cleaned text. Updated comments in `collapse_whitespace()` and `nbsp_separates_words` in `commands/html_cleaning.rs`. No tokenizer change. |
| FEAT-D257 | HTML `clean_html`: CJK Symbols comment—U+3006 IDEOGRAPHIC CLOSING MARK is `Po`, not `Lo` | **Erroneous:** `UnicodeData.txt` assigns U+3006 as **`Lo`**, not `Po`. Historical row text had the swap backwards. **FEAT-D266** restores accurate General_Category in comments; tokenizer was always unchanged. |
| FEAT-D256 | HTML `clean_html`: Supplemental Punctuation block tail U+2E5E–U+2E7F (`Cn`) stay unmapped—comment + regression test | Unicode 17 leaves the block’s scalars after **OBLIQUE HYPHEN** U+2E5D unassigned (`Cn`); `collapse_whitespace()` must not widen `U+2E30..=U+2E5D` to the block end so future assignments get an explicit review. Comment in `collapse_whitespace()`; test `supplemental_punctuation_reserved_tail_u2e5e_through_u2e7f_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D255 | HTML `clean_html`: drop erroneous Supplemental Punctuation U+2E2F word-separator mapping | UnicodeData assigns **VERTICAL TILDE** U+2E2F as `Lm`, not `Po`; FEAT-D60’s `U+2E00..=U+2E5D` arm treated the whole block as clause punctuation. `collapse_whitespace()` maps `U+2E00..=U+2E2E` and `U+2E30..=U+2E5D` only. Tests: `supplemental_punctuation_u2e00_through_u2e5d_separate_words`, `supplemental_punctuation_vertical_tilde_lm_u2e2f_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D254 | HTML `clean_html`: drop obsolete Hanifi Rohingya U+10D29–U+10D2F word-separator arm | Current UnicodeData: gap **U+10D28–U+10D2F** (`Cn`) between U+10D27 SIGN TASSI (`Mn`) and digit numerics U+10D30+ (`Nd`); historical FEAT-D111 matched U+10D29–U+10D2D as sentence `Po` (stale assignment). `collapse_whitespace()` no longer matches that range. Tests: `hanifi_rohingya_unassigned_gap_u10d28_through_u10d2f_stays_unmapped`, `hanifi_rohingya_combining_signs_mn_u10d24_through_u10d27_stay_unmapped`, `hanifi_rohingya_digit_nd_u10d30_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D253 | HTML `clean_html`: drop erroneous Philippine U+1734 word-separator mapping | UnicodeData assigns **HANUNOO SIGN PAMUDPOD** U+1734 as `Mn`, not `Po`; FEAT-D83’s `U+1734..=U+1736` arm treated the range as sentence punctuation. `collapse_whitespace()` maps `U+1735..=U+1736` only (PHILIPPINE SINGLE / DOUBLE PUNCTUATION, `Po`). Tests: `philippine_single_double_punctuation_separate_words`, `hanunoo_sign_pamudpod_mn_u1734_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D252 | HTML `clean_html`: drop erroneous Letterlike U+2107 word-separator mapping | UnicodeData assigns **EULER CONSTANT** U+2107 as `Lu`, not `So`; FEAT-D230 merged it into `U+2103..=U+2109` on a stale category assumption. `collapse_whitespace()` again uses `U+2103..=U+2106` and `U+2108..=U+2109` only (FEAT-D147 spirit). Tests: `letterlike_symbol_subranges_separate_words`, `letterlike_mathematical_letters_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D251 | HTML `clean_html`: Letterlike Symbols U+2139 INFORMATION SOURCE (`So`) as word separator | Not Rust whitespace; UI / accessibility or Unicode-sample HTML can glue Latin tokens without ASCII space—fills gap before turned sans-serif `So` U+213A–U+213B (FEAT-D147 arm). Historical `collapse_whitespace` comment listed U+2139 among Lu/Ll/Lo “stay unmapped” by mistake. `collapse_whitespace()` arm `U+2139..=U+213B`; extends `letterlike_symbol_subranges_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D250 | HTML `clean_html`: Halfwidth and Fullwidth U+FF02 FULLWIDTH QUOTATION MARK (`Po`) as word separator | Not Rust whitespace; CJK or mixed-layout HTML can glue Latin tokens without ASCII space—fills gap between mapped U+FF01 and U+FF03 (FEAT-D125 arm). U+FF07 FULLWIDTH APOSTROPHE (`Po`) stays unmapped. `collapse_whitespace()` + tests `fullwidth_quotation_mark_po_ff02_separate_words`, `fullwidth_apostrophe_po_ff07_stays_unmapped`; extends `cjk_fullwidth_and_vertical_forms_punctuation_separate_words` and `fullwidth_delimiters_operators_and_brackets_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D249 | HTML `clean_html`: Halfwidth and Fullwidth fullwidth spacing `Sk` U+FF3E FULLWIDTH CIRCUMFLEX ACCENT as word separator | Not Rust whitespace; JIS / fullwidth typography can glue Latin tokens without ASCII space—extends FEAT-D247 (ASCII U+005E `Sk`). U+FF40 FULLWIDTH GRAVE ACCENT (`Sk`) stays unmapped (FEAT-D125 modifier-like / word-internal risk). U+FFE3 FULLWIDTH MACRON (`Sk`) stays unmapped. `collapse_whitespace()` + tests `fullwidth_circumflex_sk_ff3e_separate_words`, `fullwidth_grave_sk_ff40_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D248 | HTML `clean_html`: Hiragana U+309B VOICED + U+309C SEMI-VOICED sound marks (`Sk`) as word separators | Not Rust whitespace; romaji–kana or Unicode-sample HTML can glue Latin tokens without ASCII space. Extends middle-dot arm spirit (U+30FB Po). Hiragana letter e.g. U+3042 (`Lo`) stays unmapped. `collapse_whitespace()` + tests `katakana_hiragana_voiced_semi_voiced_sound_marks_sk_separate_words`, `hiragana_letter_lo_u3042_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D247 | HTML `clean_html`: Basic Latin spacing `Sk` U+005E CIRCUMFLEX ACCENT + U+0060 GRAVE ACCENT as word separators | Not Rust whitespace; ASCII markup or transliteration in HTML can glue Latin tokens without ASCII space. U+005F LOW LINE (`Pc`) stays unmapped—underscore / word-internal risk. Distinct from `Lm` U+02C6/U+02C7 (FEAT-D240). Comment: U+00A6 BROKEN BAR is `So` in UnicodeData (legacy copy called it `Sm`). `collapse_whitespace()` + tests `basic_latin_spacing_circumflex_and_grave_sk_u005e_u0060_separate_words`, `basic_latin_low_line_pc_u005f_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D246 | HTML `clean_html`: drop erroneous Letterlike U+213F word-separator mapping | Current UnicodeData: U+213F DOUBLE-STRUCK CAPITAL PI (`Lu`); DOUBLE-STRUCK N-ARY SUMMATION is U+2140 (`Sm`). Historical FEAT-D231 treated U+213F as `Sm`. `collapse_whitespace()` no longer matches U+213F; U+2140..=U+2144 unchanged. Tests: `letterlike_symbol_subranges_separate_words`, `letterlike_mathematical_letters_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D245 | HTML `clean_html`: Latin-1 Supplement spacing `Sk` U+00AF MACRON + U+00B4 ACUTE ACCENT + U+00B8 CEDILLA as word separators | Not Rust whitespace; legacy ISO 8859-1 or linguistics typography in HTML can glue Latin tokens without ASCII space. U+00A8 DIAERESIS (`Sk`) stays unmapped—spacing-mark / combining-like risk (FEAT-D223 / FEAT-D226). `collapse_whitespace()` + `latin1_macron_acute_and_cedilla_sk_u00af_u00b4_u00b8_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D244 | HTML `clean_html`: Greek Extended spacing polytonic `Sk` U+1FBD U+1FBF U+1FC0–U+1FC1 U+1FCD–U+1FCF U+1FDD–U+1FDF U+1FED–U+1FEF U+1FFD–U+1FFE as word separators | Unicode block U+1F00–U+1FFF: fifteen assigned spacing `Sk` (koronis, psili, perispomeni, dialytika combinations, varia/oxia/dasia marks); not Rust whitespace; extends FEAT-D235 (U+0384/U+0385). Precomposed Greek letters (`Ll`/`Lu`/`Lt`, e.g. U+1FB6) stay unmapped—`greek_extended_letter_alpha_with_perispomeni_ll_u1fb6_stays_unmapped`. Clarifies comment vs supplementary-plane U+1FB00+ Legacy Computing. `collapse_whitespace()` + `greek_extended_spacing_sk_polytonic_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D243 | HTML `clean_html`: Modifier Tone Letters `Sk` U+A700–U+A716 as word separators | Unicode block U+A700–U+A71F: contiguous `Sk` U+A700–U+A716 (Chinese tone marks through extra-low left-stem tone bar); not Rust whitespace. Tail U+A717–U+A71F are `Lm`—spot-check `modifier_tone_letters_lm_u_a717_through_u_a71f_stay_unmapped`. `collapse_whitespace()` + test `modifier_tone_letters_sk_u_a700_through_u_a716_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D242 | HTML `clean_html`: Latin Extended-E `Sk` U+AB5B U+AB6A U+AB6B as word separators | Unicode block U+AB30–U+AB6F: three assigned `Sk` scalars—MODIFIER BREVE WITH INVERTED BREVE, MODIFIER LETTER LEFT TACK, MODIFIER LETTER RIGHT TACK; not Rust whitespace. U+AB5A (`Ll`) and U+AB5C (`Lm`) spot-check stay unmapped; tail U+AB6C–U+AB6F unassigned (`Cn`). `collapse_whitespace()` + tests `latin_extended_e_modifier_letters_sk_ab5b_ab6a_ab6b_separate_words`, `latin_extended_e_letter_ll_ab5a_and_modifier_lm_ab5c_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D241 | HTML `clean_html`: Latin Extended-D `Sk` U+A720 U+A721 U+A789 U+A78A as word separators | Unicode block U+A720–U+A7FF: only four assigned `Sk` scalars—MODIFIER LETTER STRESS AND HIGH/LOW TONE, MODIFIER LETTER COLON, MODIFIER LETTER SHORT EQUALS SIGN; not Rust whitespace. Lu/Ll/Lm neighbors stay unmapped. `collapse_whitespace()` + tests `latin_extended_d_modifier_letters_sk_a720_a721_a789_a78a_separate_words`, `latin_extended_d_modifier_letter_lm_a788_and_letter_ll_a723_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D240 | HTML `clean_html`: drop erroneous Spacing Modifier Letters U+02C6/U+02C7 word-separator mapping | UnicodeData: U+02C6 MODIFIER LETTER CIRCUMFLEX ACCENT and U+02C7 CARON are `Lm`, not `Sk`; FEAT-D239’s `U+02C2..=U+02C7` arm split IPA-style superscript diacritics incorrectly. `collapse_whitespace()` maps `U+02C2..=U+02C5` + `U+02D2..=U+02D5` only. Tests `spacing_modifier_arrowheads_sk_u02c2_through_u02c5_and_tacks_u02d2_through_u02d5_separate_words`, `spacing_modifier_circumflex_and_caron_lm_u02c6_u02c7_stay_unmapped`, `spacing_modifier_stress_marks_lm_u02c8_and_u02d0_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D239 | HTML `clean_html`: Spacing Modifier Letters `Sk` U+02C2–U+02C5 (arrowheads) + U+02D2–U+02D5 (centred half-rings, up/down tack) as word separators | Not Rust whitespace; IPA or Unicode-sample HTML can glue Latin tokens without ASCII space. **U+02C6/U+02C7:** Unicode assigns `Lm`; FEAT-D240 removed them from the D239 arm (historical row text once called them `Sk`). U+02C8 VERTICAL LINE through U+02D1 HALF TRIANGULAR COLON (`Lm`) stay unmapped—stress / length, word-internal risk. Extends FEAT-D237 (U+02D6/U+02D7); U+02D8 BREVE+ (`Sk`) still unmapped. `collapse_whitespace()` + tests above in `commands/html_cleaning.rs`. |
| FEAT-D238 | HTML `clean_html`: Spacing Modifier Letters tone / contour `Sk` U+02E5–U+02EB, U+02ED, U+02EF–U+02FF as word separators | Not Rust whitespace; IPA / Chin Wu tone bars, departing marks, unaspirated, arrowheads, shelves, or Unicode-sample HTML can glue Latin tokens without ASCII space. U+02EC VOICING (`Lm`) and U+02EE DOUBLE APOSTROPHE (`Lm`) stay unmapped—letter- / apostrophe-like risk. U+02E0–U+02E4 superscript letters (`Lm`) stay unmapped. U+02DE–U+02DF and U+02D8–U+02DD (`Sk`) stay unmapped (spacing hook / accent risk, FEAT-D237 neighbor). `collapse_whitespace()` + tests `spacing_modifier_tone_contour_sk_u02e5_through_u02eb_u02ed_u02ef_through_u02ff_separate_words`, `spacing_modifier_voicing_lm_u02ec_and_double_apostrophe_lm_u02ee_stay_unmapped`, `spacing_modifier_superscript_letter_lm_u02e1_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D237 | HTML `clean_html`: Spacing Modifier Letters U+02D6 MODIFIER LETTER PLUS SIGN + U+02D7 MODIFIER LETTER MINUS SIGN (`Sk`) as word separators | Not Rust whitespace; IPA / linguistic notation or Unicode-sample HTML can glue Latin tokens without ASCII space (spirit of Latin-1 PLUS-MINUS U+00B1 `Sm`). U+02D8 BREVE through U+02DD DOUBLE ACUTE ACCENT (`Sk`) stay unmapped—spacing-accent / word-internal risk. `collapse_whitespace()` + tests `modifier_letter_plus_and_minus_sk_u02d6_u02d7_separate_words`, `modifier_letter_breve_sk_u02d8_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D236 | HTML `clean_html`: Greek and Coptic U+03D6 GREEK PI SYMBOL (`Sm`) as word separator | Not Rust whitespace; MathML or Greek math typography can sit π between Latin tokens without ASCII space (extends FEAT-D199 U+03F6). U+03D5 GREEK PHI SYMBOL (`Lu`) stays unmapped—letter-like, word-internal risk. `collapse_whitespace()` + tests extend `latin1_greek_and_arabic_script_punctuation_separate_words`; `greek_phi_symbol_lu_u03d5_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D235 | HTML `clean_html`: Greek and Coptic U+0384 GREEK TONOS + U+0385 GREEK DIALYTIKA TONOS (`Sk`) as word separators | Spacing polytonic marks are not Rust whitespace; mixed Greek–Latin or Unicode-sample HTML can glue Latin tokens without ASCII space. U+0374 GREEK NUMERAL SIGN (`Lm`) and U+0375 GREEK LOWER NUMERAL SIGN (`Sk`) stay unmapped—numeral / thousands notation, word-internal risk. `collapse_whitespace()` + tests extend `latin1_greek_and_arabic_script_punctuation_separate_words`; `greek_lower_numeral_sign_sk_u0375_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D234 | HTML `clean_html`: Arabic Extended-B U+0888 ARABIC RAISED ROUND DOT (`Sk`) as word separator | Not Rust whitespace; Arabic typography or Unicode-sample HTML can glue Latin tokens without ASCII space. U+08C9 ARABIC SMALL FARSI YEH (`Lm`) stays unmapped—modifier-like, word-internal risk. `collapse_whitespace()` + tests extend `latin1_greek_and_arabic_script_punctuation_separate_words`, `tifinagh_separator_and_arabic_supplementary_punctuation_separate_words`; `arabic_small_farsi_yeh_lm_u08c9_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D233 | HTML `clean_html`: Myanmar U+109E SHAN ONE + U+109F SHAN EXCLAMATION (`So`) as word separators | Block tail after Shan digits U+1090–U+1099 (`Nd`) and Khamti/Aiton marks U+109A–U+109D (`Mc`/`Mn`); only U+109E–U+109F are `So`—not Rust whitespace. Extends FEAT-D120 (U+104C–U+104F). Myanmar LETTER KA U+1000 (`Lo`) and Shan digit U+1090 (`Nd`) stay unmapped. `collapse_whitespace()` + tests extend `thai_lao_myanmar_sentence_punctuation_separate_words`; `myanmar_letter_ka_lo_u1000_stays_unmapped`, `myanmar_shan_digit_nd_u1090_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D232 | HTML `clean_html`: Alphabetic Presentation Forms U+FB29 HEBREW LETTER ALTERNATIVE PLUS SIGN (`Sm`) as word separator | Sole `Sm` in block U+FB00–U+FB4F; not Rust whitespace—Hebrew or Unicode-sample HTML can glue Latin tokens. Hebrew letters (`Lo`) stay unmapped. `collapse_whitespace()` + test `hebrew_alternative_plus_sign_sm_fb29_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D231 | HTML `clean_html`: Letterlike Symbols U+213F DOUBLE-STRUCK N-ARY SUMMATION (`Sm`) as word separator | Historical row text assumed U+213F was `Sm`; Unicode assigns **DOUBLE-STRUCK CAPITAL PI** (`Lu`). **DOUBLE-STRUCK N-ARY SUMMATION** is U+2140 (`Sm`), mapped with U+2141–U+2144. **FEAT-D246** removed the erroneous U+213F arm. Lu/Ll neighbors U+213C–U+213E stay unmapped. `collapse_whitespace()` + test `letterlike_symbol_subranges_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D230 | HTML `clean_html`: Letterlike Symbols U+2107 EULER CONSTANT (`So`) as word separator | Row text assumed `So`; Unicode assigns **EULER CONSTANT** as `Lu`. **FEAT-D252** removed U+2107 from the merged arm; `U+2103..=U+2106` and `U+2108..=U+2109` only. Historical: FEAT-D147 had split ranges; D230 merged `U+2103..=U+2109`; test `letterlike_symbol_subranges_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D229 | HTML `clean_html`: Latin-1 vulgar fractions U+00BC U+00BD U+00BE (`No`) as word separators | Vulgar fraction one quarter / one half / three quarters are not Rust whitespace; recipe, price, or legacy Latin-1 HTML can glue Latin tokens without ASCII space (same spirit as Number Forms FEAT-D146). Superscript digit numerics U+00B2 / U+00B3 / U+00B9 (`No`) and U+00BA MASCULINE ORDINAL (`Ll`) stay unmapped (FEAT-D225). `collapse_whitespace()` + test `latin1_vulgar_fractions_no_u00bc_u00bd_u00be_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D228 | HTML `clean_html`: C0 controls U+0001–U+0008 U+000E–U+001F U+007F (`Cc`) as word separators | Legacy device controls are not Unicode White_Space (Rust `is_whitespace` false), unlike TAB and LF–CR; binary-pasted HTML can glue Latin tokens. `collapse_whitespace()` maps those scalars to ASCII space; U+0000 omitted (HTML5 drops NUL in text nodes); U+0009–U+000D omitted. Test `c0_controls_u0001_through_u0008_u000e_through_u001f_and_del_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D227 | HTML `clean_html`: C1 controls U+0080–U+009F (`Cc`) as word separators | ISO 8859-1 C1 / binary noise in text nodes is not Rust whitespace; `split_whitespace()` would glue Latin tokens. `collapse_whitespace()` maps the full range to ASCII space. Test `c1_controls_u0080_through_u009f_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D226 | HTML `clean_html`: Latin-1 U+00A6 BROKEN BAR (`Sm`) as word separator | Legacy ISO 8859-1 / entity-encoded HTML often uses ¦ as a tall pipe between Latin tokens without ASCII space; not Rust whitespace; distinct from ASCII VERTICAL LINE U+007C. Revisits FEAT-D223 omission for U+00A6 only; U+00A8 DIAERESIS (`Sk`) stays unmapped. `collapse_whitespace()` + tests `latin1_broken_bar_sm_u00a6_separate_words`, `latin1_diaeresis_sk_u00a8_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D225 | HTML `clean_html`: Latin-1 U+00B0 DEGREE SIGN (`So`) + U+00AC NOT SIGN, U+00B1 PLUS-MINUS, U+00D7 MULTIPLICATION, U+00F7 DIVISION (`Sm`) as word separators | Scientific or pasted math / weather HTML can place degree or Latin-1 math symbols between Latin tokens without ASCII space; not Rust whitespace; outside U+2200–U+22FF Mathematical Operators arm. Superscript digit numerics U+00B2 / U+00B3 / U+00B9 (`No`) and U+00BA MASCULINE ORDINAL (`Ll`) stay unmapped. `collapse_whitespace()` + tests `latin1_degree_sign_so_u00b0_and_math_sm_u00ac_u00b1_u00d7_u00f7_separate_words`, `latin1_superscript_digits_no_and_masculine_ordinal_ll_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D224 | HTML `clean_html`: Latin-1 U+00A9 COPYRIGHT SIGN + U+00AE REGISTERED SIGN (`So`) as word separators | Legal / trademark HTML often places © and ® between Latin tokens without ASCII space; not Rust whitespace. U+00AA FEMININE ORDINAL INDICATOR (`Ll`) stays unmapped—letter-like, word-internal risk. `collapse_whitespace()` + tests `latin1_copyright_and_registered_signs_so_u00a9_u00ae_separate_words`, `latin1_feminine_ordinal_ll_u00aa_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D223 | HTML `clean_html`: Latin-1 U+00A2–U+00A5 cent / pound / currency / yen (`Sc`) as word separators | Contiguous Latin-1 Supplement currency scalars; not Rust whitespace. At close, U+00A6 BROKEN BAR (`Sm`) and U+00A8 DIAERESIS (`Sk`) stayed unmapped; **FEAT-D226** maps U+00A6; U+00A8 still unmapped. Distinct from Currency Symbols block U+20A0+ (FEAT-D141 / FEAT-D222). `collapse_whitespace()` + tests `latin1_currency_signs_u00a2_through_u00a5_separate_words`, `latin1_diaeresis_sk_u00a8_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D222 | HTML `clean_html`: Currency Symbols U+20C0 SOM SIGN + U+20C1 SAUDI RIYAL SIGN (`Sc`) as word separators | Extends FEAT-D141 contiguous `Sc` arm from `U+20A0..=U+20BF` to `U+20A0..=U+20C1`; Unicode 14 / 17 assignments; not Rust whitespace. U+20C2–U+20CF unassigned (`Cn`) stay unmapped. `collapse_whitespace()` + tests `currency_symbols_u20a0_through_u20c1_separate_words`, `currency_symbols_unassigned_tail_u20c2_through_u20cf_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D221 | HTML `clean_html`: drop erroneous Meetei Mayek Extensions U+AAF2 word-separator arm | Current UnicodeData: U+AAF2 MEETEI MAYEK ANJI (`Lo`); historical FEAT-D218 treated it as `Po`. `collapse_whitespace()` no longer matches `U+AAF2`; U+AAF0–U+AAF1 (`Po`) unchanged. Test `meetei_mayek_anji_lo_u_aaf2_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D220 | HTML `clean_html`: drop erroneous Vithkuqi U+1057B–U+1057F word-separator arm | Current UnicodeData: gap U+1057B (`Cn`), U+1057C–U+1057F Vithkuqi capital letters (`Lu`); historical FEAT-D117 treated the range as `Po`. `collapse_whitespace()` no longer matches `U+1057B..=U+1057F`; U+1056F Caucasian Albanian citation mark (`Po`) unchanged. Test `vithkuqi_capital_letters_and_gap_u1057b_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D219 | HTML `clean_html`: clarify U+1806 — **MONGOLIAN TODO SOFT HYPHEN** (`Pd`) in comments | No tokenizer change. Unicode character name includes the substring `TODO`; comments in `collapse_whitespace()` and `mongolian_punctuation_separates_words` spell the full name so it is not mistaken for a developer TODO. `commands/html_cleaning.rs`. |
| FEAT-D218 | HTML `clean_html`: Meetei Mayek Extensions U+AAF2 ANII (`Po`) as word separator | **Superseded for U+AAF2:** Unicode assigns MEETEI MAYEK ANJI as `Lo`; FEAT-D221 removed that arm. U+AAF0–U+AAF1 (`Po`) coverage unchanged. Historical row text assumed `Po` for U+AAF2. |
| FEAT-D217 | HTML `clean_html`: Superscripts and Subscripts super/sub plus–minus–equals and parens U+207A–U+207E + U+208A–U+208E as word separators | Unicode block tail: `Sm`/`Ps`/`Pe` only; not Rust whitespace. Superscript / subscript digit numerics `No` U+2070–U+2079 and U+2080–U+2089, superscript letters `Lm` U+2071 / U+207F, and gap / unassigned U+208F stay unmapped. `collapse_whitespace()` maps `U+207A..=U+207E` and `U+208A..=U+208E`; tests `superscripts_and_subscripts_math_delimiters_separate_words`, `superscripts_subscripts_digit_numerics_and_letter_marks_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D216 | HTML `clean_html`: Arabic Presentation Forms-A honorific `So` ligatures U+FBC3–U+FBD2 as word separators | Extends FEAT-D210 (FD40–FD4F, FDCF, FDFE, FDFF); not Rust whitespace. Quranic annotation `Sk` U+FBB2–U+FBC1 and U+FBC2 WASLA ABOVE stay unmapped. Letter `Lo` U+FBD3+ unchanged. `collapse_whitespace()` maps `U+FBC3..=U+FBD2`; tests `arabic_presentation_forms_honorific_so_ligatures_fbc3_fbd2_separate_words`, `arabic_presentation_forms_sk_fbc2_and_lo_fbd3_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D215 | HTML `clean_html`: Javanese U+A9DE–U+A9DF tail pada (`Po`) as word separators | Extends FEAT-D78 contiguous pada U+A9C1–U+A9CF; not Rust whitespace. Javanese digits U+A9D0–U+A9D9 (`Nd`) sit between the two `Po` runs—cannot widen one range; discrete `U+A9DE` + `U+A9DF` arms. U+A9C0 PANGKON (`Cf`) stays unmapped. `collapse_whitespace()` + tests extend `khmer_sentence_signs_and_javanese_pada_separate_words`; `javanese_digit_nd_u_a9d0_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D214 | HTML `clean_html`: Wancho arm — U+1E2FF NGUN SIGN (`Sc`) only | UnicodeData assigns Wancho digits U+1E2F0–U+1E2F9 (`Nd`), unassigned gap U+1E2FA–U+1E2FE (`Cn`), and **WANCHO NGUN SIGN** U+1E2FF (`Sc`); the historical FEAT-D91 “comma / full stop (`Po`) U+1E2FE–U+1E2FF” arm was stale. `collapse_whitespace()` maps `U+1E2FF` only; `wancho_comma_full_stop_and_nyiakeng_sentence_punctuation_separate_words` + new `wancho_digit_nd_and_unassigned_gap_before_ngun_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D213 | HTML `clean_html`: drop erroneous Elymaic U+10FF5 word-separator arm | Unicode assigns U+10FF5 as Elymaic **LETTER TAW** (`Lo`), not `Po`; mapping it to ASCII space split script text incorrectly (historical FEAT-D110 mismatch). `collapse_whitespace()` no longer matches `U+10FF5`; `nabataean_hatran_old_north_arabian_word_separators_separate_words` covers Nabataean / Hatran / Old North Arabian only; `elymaic_letter_taw_lo_u10ff5_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D212 | HTML `clean_html`: drop erroneous Warang Citi U+118C8–U+118CF word-separator arm | Unicode 14+ assigns those scalars as Warang Citi **small letters** (`Ll`), not `Po`; mapping them to ASCII space split script text incorrectly (historical FEAT-D111 mismatch). `collapse_whitespace()` no longer matches `U+118C8..=U+118CF`; Hanifi Rohingya gap / Mn / Nd tests after **FEAT-D254**; `warang_citi_small_letters_u118c8_through_u118cf_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D211 | HTML `clean_html`: Canadian Syllabics U+1400 HYPHEN (`Pd`) as word separator | Extends FEAT-D88 full stop / Bamum / Mro / New Tai Lue; not Rust whitespace (Unicode dash punctuation spirit, FEAT-D63). Sole `Pd` in Unified Canadian Aboriginal Syllabics U+1400–U+167F; U+166D CHI SIGN (`So`) stays unmapped. `collapse_whitespace()` maps `U+1400`; test extends `canadian_syllabics_full_stop_bamum_mro_and_new_tai_lue_signs_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D210 | HTML `clean_html`: Arabic Presentation Forms-A honorific `So` ligatures U+FD40–U+FD4F + U+FDCF + U+FDFE + U+FDFF as word separators | Extends FEAT-D183 ornate parens / rial / Bismillah; not Rust whitespace. Letter ligatures U+FD50+ (`Lo`), Koranic stop signs U+FDF0–U+FDFB (`Lo`), and Quranic annotation `Sk` U+FBB2–U+FBC1 stay unmapped. `collapse_whitespace()` maps `U+FD40..=U+FD4F`, `U+FDCF`, `U+FDFE`, `U+FDFF`; tests `arabic_presentation_forms_honorific_so_ligatures_fd40_fd4f_fdcf_fdfe_fdff_separate_words`, `arabic_presentation_forms_koranic_stop_lo_and_triple_ligatures_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D209 | HTML `clean_html`: Arabic U+060E POETIC VERSE SIGN + U+060F SIGN MISRA (`So`) as word separators | Between U+060D (`Po`) and U+0610+ (`Mn` honorific stack); not Rust whitespace. Arabic-indic cube / fourth root / ray U+0606–U+0608 (`Sm`) stay unmapped (FEAT-D124 spirit). `collapse_whitespace()` maps `U+060E` and `U+060F`; tests extend `latin1_greek_and_arabic_script_punctuation_separate_words` and `tifinagh_separator_and_arabic_supplementary_punctuation_separate_words`; `arabic_indic_cube_root_sm_u0606_and_sallallahou_mn_u0610_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D208 | HTML `clean_html`: Arabic U+061D END OF TEXT MARK (`Po`) as word separator | Fills gap between U+061B and U+061E in the Arabic punctuation arm; not Rust whitespace. U+061C (`Cf` letter mark) stays unmapped. `collapse_whitespace()` maps `U+061D`; tests extend `latin1_greek_and_arabic_script_punctuation_separate_words` and `tifinagh_separator_and_arabic_supplementary_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D207 | HTML `clean_html`: Ahom U+1173F SYMBOL VI (`So`) as word separator | Extends FEAT-D96 Ahom `Po` U+1173C–U+1173E; not Rust whitespace. Sole `So` in Ahom block tail; Ahom letters (`Lo`, e.g. U+11700) excluded. `collapse_whitespace()` maps `U+1173C..=U+1173F`; tests extend `ahom_dogra_dives_akuru_nandinagari_bhaiksuki_sentence_punctuation_separate_words`; `ahom_letter_ka_lo_u11700_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D206 | HTML `clean_html`: Ethiopic U+1360 SECTION MARK + U+1363–U+1368 clause / sentence `Po` as word separators | Extends FEAT-D48 (wordspace U+1361, full stop U+1362); not Rust whitespace. Single arm `U+1360..=U+1368` (all `Po` before digit numerics). Ethiopic digit numerics U+1369+ (`No`) excluded—spot-check U+1369, U+1372. `collapse_whitespace()` maps the range; tests `ethiopic_section_wordspace_and_sentence_punctuation_u1360_through_u1368_separate_words`, `ethiopic_digit_one_no_u1369_and_number_ten_no_u1372_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D205 | HTML `clean_html`: Armenian U+058D–U+058F eternity signs (`So`) + dram (`Sc`) as word separators | Extends FEAT-D72 sentence / full-stop arm; not Rust whitespace. Unassigned gap U+058B–U+058C (`Cn`) excluded. U+0559 MODIFIER LETTER LEFT HALF RING (`Lm`) stays unmapped—word-internal risk. `collapse_whitespace()` maps `U+058D..=U+058F`; tests extend `armenian_script_punctuation_separates_words`; `armenian_modifier_letter_half_ring_lm_u0559_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D204 | HTML `clean_html`: Medefaidrin U+16E99 SYMBOL AIVA (`Po`) as word separator | Extends FEAT-D92 contiguous `Po` run to `U+16E97..=U+16E9A` (comma, full stop, symbol aiva, exclamation oh); UnicodeData category `Po` (historical backlog text called it `So`). Medefaidrin letters e.g. U+16E6D (`Ll`) excluded. `collapse_whitespace()` one range arm; tests extend `adlam_initial_punctuation_and_medefaidrin_sentence_marks_separate_words`; `medefaidrin_letter_ll_u16e6d_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D203 | HTML `clean_html`: Cyrillic U+0482 THOUSANDS SIGN (`So`) as word separator | Sole `So` in Cyrillic block U+0400–U+04FF; not Rust whitespace. Cyrillic letters (`Lu`/`Ll`) and combining titlo stack U+0483–U+0489 (`Mn`/`Me`) excluded. Extends FEAT-D178 Slavonic/Cyrillic theme (Extended-B `Po` U+A673/U+A67E). `collapse_whitespace()` maps `U+0482`; tests `cyrillic_thousands_sign_so_u0482_separate_words`, `cyrillic_combining_titlo_mn_u0483_and_capital_letter_lo_u0410_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D202 | HTML `clean_html`: Unicode 17 CJK Strokes U+31E4–U+31E5 (`So`) + IDC SUBTRACTION U+31EF (`So`) as word separators | Extends FEAT-D176 IDC arm (`U+2FF0..=U+2FFF`) with `U+31EF`; extends FEAT-D173 CJK Strokes `U+31C0..=31E3` to `U+31E5`. Inner gap `U+31E6..=U+31EE` (`Cn`) excluded. `collapse_whitespace()` match arms + comments; tests extend `kanbun_so_marks_and_cjk_strokes_separate_words`, `ideographic_description_characters_separate_words`; `kanbun_no_numerics_bopomofo_ext_and_strokes_gap_stay_unmapped` spot-checks gap; `cjk_strokes_inner_gap_u31e6_through_u31ee_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D201 | HTML `clean_html`: Nyiakeng Puachue Hmong U+1E14F CIRCLED CA (`So`) as word separator | Extends FEAT-D91 Wancho **NGUN SIGN** U+1E2FF (`Sc`; FEAT-D214) and Ideographic Symbols U+16FE2–U+16FE3; not Rust whitespace. Digits U+1E140–U+1E149 (`Nd`) and Lm U+1E137–U+1E13D (category signs / syllable lengthener) excluded. `collapse_whitespace()` maps `U+1E14F`; tests extend `wancho_comma_full_stop_and_nyiakeng_sentence_punctuation_separate_words`; `nyiakeng_puachue_hmong_digit_nd_and_category_lm_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D200 | HTML `clean_html`: Aegean Numbers U+10137–U+1013F weight and measure signs (`So`) as word separators | Extends FEAT-D108 Aegean word-separator `Po` U+10100–U+10102; not Rust whitespace. Aegean number forms U+10107–U+10133 (`No`) and unassigned gap U+10134–U+10136 (`Cn`) excluded. Distinct from FEAT-D180 Ancient Greek Numbers metrological `So` U+10179+. `collapse_whitespace()` maps `U+10137..=U+1013F`; tests extend `aegean_and_phoenician_word_separator_marks_separate_words`; `aegean_number_no_and_unassigned_gap_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D199 | HTML `clean_html`: Greek and Coptic U+03F6 REVERSED LUNATE EPSILON (`Sm`) as word separator | Extends FEAT-D69 Greek question mark / FEAT-D55 ano teleia; not Rust whitespace. Outside U+2200–U+22FF Mathematical Operators arm. Neighbor U+03F5 GREEK LUNATE EPSILON SYMBOL (`Ll`) excluded—letter-like, word-internal risk. `collapse_whitespace()` maps `U+03F6`; tests extend `latin1_greek_and_arabic_script_punctuation_separate_words`; `greek_lunate_epsilon_letter_ll_u03f5_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D198 | HTML `clean_html`: Hebrew U+05C6 NUN HAFUKHA (`Po`) as word separator | Extends FEAT-D73 Hebrew sof pasuq / Georgian paragraph arm and FEAT-D62 maqaf/paseq; not Rust whitespace. U+05F3 GERESH and U+05F4 GERSHAYIM (`Po`) stay unmapped—apostrophe / abbreviation risk (like U+2019). Hebrew letter U+05D0 (`Lo`) spot-check stays unmapped. `collapse_whitespace()` maps `U+05C6`; tests extend `hebrew_maqaf_paseq_tibetan_tsheg_ethiopic_stop_separate_words`; `hebrew_geresh_gershayim_po_u05f3_u05f4_stay_unmapped`, `hebrew_letter_aleph_lo_u05d0_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D197 | HTML `clean_html`: Khmer Symbols U+19E0–U+19FF (`So`) as word separators | Unicode block: all 32 assigned scalars are `So` (lunar-abbreviation symbols); not Rust whitespace. Khmer digits U+17E0–U+17E9 (`Nd`) and LEK ATTAK U+17F0–U+17F9 (`No`) excluded (spot-check U+17E0, U+17F0). Distinct from Khmer script / FEAT-D196 Riel `Sc`. `collapse_whitespace()` maps `U+19E0..=U+19FF`; tests `khmer_symbols_so_u19e0_through_u19ff_separate_words`, `khmer_digit_nd_and_lek_attak_no_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D196 | HTML `clean_html`: Khmer U+17DB RIEL SIGN (`Sc`) as word separator | Extends FEAT-D78 Khmer sentence `Po` U+17D4–U+17DA; not Rust whitespace. Khmer SIGN AVAKRAHASANYA U+17DC (`Lo`) and SIGN ATTHACAN U+17DD (`Mn`) excluded. `collapse_whitespace()` maps `U+17DB`; tests `khmer_riel_sign_sc_u17db_separate_words`, `khmer_sign_avakrahasanya_lo_and_atthacan_mn_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D195 | HTML `clean_html`: Lao U+0EDF YAMAK (`Po`) as word separator | Extends FEAT-D75 Lao ellipsis U+0EAF; not Rust whitespace. Lao LETTER KHMU GO U+0EDE (`Lo`) and Lao digits U+0ED0–U+0ED9 (`Nd`) excluded (tests spot-check U+0EDE, U+0ED0). `collapse_whitespace()` maps `U+0EDF`; tests `lao_yamak_po_u0edf_separate_words`, `lao_letter_khmu_go_u0ede_and_lao_digit_nd_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D194 | HTML `clean_html`: Thai U+0E3F CURRENCY SYMBOL BAHT (`Sc`) as word separator | Extends FEAT-D75 Thai sentence `Po` marks; not Rust whitespace. Thai letters (`Lo`) and digits U+0E50–U+0E59 (`Nd`) excluded (tests spot-check U+0E01, U+0E50). `collapse_whitespace()` maps `U+0E3F`; tests `thai_baht_currency_sc_u0e3f_separate_words`, `thai_letter_lo_and_digit_nd_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D193 | HTML `clean_html`: Gujarati U+0AF1 RUPEE SIGN (`Sc`) as word separator | Extends FEAT-D118 Gujarati U+0AF0 abbreviation; not Rust whitespace. Gujarati letters (`Lo`) and digits U+0AE6–U+0AEF (`Nd`) excluded. `collapse_whitespace()` maps `U+0AF1`; tests `gujarati_rupee_sign_sc_u0af1_separate_words`, `gujarati_letter_lo_and_digit_nd_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D192 | HTML `clean_html`: Telugu U+0C7F SIGN TUUMU (`So`) as word separator | Extends FEAT-D118 Gurmukhi / Gujarati / Telugu siddham / Kannada siddham; not Rust whitespace. Telugu letters (`Lo`) and digits U+0C66–U+0C6F (`Nd`) excluded. `collapse_whitespace()` maps `U+0C7F`; tests `telugu_sign_tuumu_so_u0c7f_separate_words`, `telugu_letter_lo_and_digit_nd_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D191 | HTML `clean_html`: Bengali U+09F2 RUPEE MARK + U+09F3 RUPEE SIGN + U+09FB GANDA MARK (`Sc`) as word separators | Extends FEAT-D119 Bengali `So`/`Po` (iss-har / abbreviation); not Rust whitespace. Currency numerators U+09F4–U+09F9 (`No`) and U+09FC LETTER KHANDA TA (`Lo`) excluded. `collapse_whitespace()` maps `U+09F2`, `U+09F3`, and `U+09FB`; tests `bengali_currency_sc_u09f2_u09f3_u09fb_separate_words`, `bengali_currency_numerator_no_and_khanda_ta_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D190 | HTML `clean_html`: Devanagari U+0970 ABBREVIATION SIGN (`Po`) + Mandaic U+085E PUNCTUATION (`Po`) as word separators | Extends FEAT-D74 danda arm with abbreviation sign; not Rust whitespace. Devanagari digits U+0966–U+096F (`Nd`) and U+0971 HIGH SPACING DOT (`Lm`) excluded. After Samaritan U+0830–U+083E; Mandaic combining U+0859–U+085B (`Mn`) excluded. Syriac Supplement U+0860+ (`Lo`) excluded. `collapse_whitespace()` maps `U+0970` and `U+085E`; tests `devanagari_abbreviation_sign_u0970_and_mandaic_punctuation_u085e_separate_words`, `devanagari_high_spacing_dot_u0971_and_mandaic_combining_marks_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D189 | HTML `clean_html`: NKo U+07F6 SYMBOL OO DENNEN (`So`) + U+07FE–U+07FF DOROME / TAMAN SIGNS (`Sc`) as word separators | Extends FEAT-D90 NKo `Po` U+07F7–U+07F9; not Rust whitespace. U+07FA (`Lm`), U+07F4–U+07F5 (`Lm`), U+07F0–U+07F3 / U+07FD (`Mn`), unassigned U+07FB–U+07FC excluded. `collapse_whitespace()` maps `U+07F6` and `U+07FE..=U+07FF`; tests `nko_sentence_punctuation_and_samaritan_punctuation_separate_words`, `nko_lajanyalan_tone_apostrophe_and_dantayalan_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D188 | HTML `clean_html`: Ethiopic Supplement U+1390–U+1399 tonal marks (`So`) as word separators | Unicode block U+1380–U+139F: contiguous `So` U+1390–U+1399 (YIZET through KURT); not Rust whitespace. Ethiopic syllables U+1380–U+138F (`Lo`) excluded—word-internal risk. Unassigned tail U+139A–U+139F (`Cn`) excluded. Distinct from Ethiopic `Po` U+1361–U+1362 (FEAT-D48 / full stop arm). `collapse_whitespace()` maps `U+1390..=U+1399`; tests `ethiopic_supplement_tonal_marks_so_u1390_through_u1399_separate_words`, `ethiopic_supplement_syllables_lo_and_unassigned_tail_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D187 | HTML `clean_html`: Vedic Extensions U+1CD3 VEDIC SIGN NIHSHVASA (`Po`) as word separator | Sole `Po` in block U+1CD0–U+1CFF; not Rust whitespace. Vedic tone marks U+1CD0–U+1CD2, U+1CD4–U+1CDF (`Mn`) excluded—combining / word-internal risk. Distinct from Devanagari Extended U+A8F8+ (FEAT-D121). `collapse_whitespace()` maps `U+1CD3`; tests `vedic_extensions_nihshvasa_u1cd3_separate_words`, `vedic_tone_marks_mn_u1cd0_u1cd1_u1cd4_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D186 | HTML `clean_html`: CJK Compatibility Forms U+FE30–U+FE4F as word separators | Vertical presentation for two-dot leader, em/en dash, underscore variants, brackets, sesame dots, dashed/wavy overlines and low lines (`Po`/`Pd`/`Ps`/`Pe`/`Pc`); not Rust whitespace. Combining Half Marks U+FE20–U+FE2F (`Mn`) excluded—word-internal risk. Distinct from Vertical Forms U+FE10–U+FE19 and Small Form Variants U+FE50+ (FEAT-D68). `collapse_whitespace()` maps `U+FE30..=U+FE4F`; tests `cjk_compatibility_forms_fe30_through_fe4f_separate_words`, `combining_half_marks_fe20_through_fe2f_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D185 | HTML `clean_html`: Ottoman Siyaq Numbers U+1ED2E MARRATAN (`So`) as word separator | Unicode block U+1ED01–U+1ED3D: sole `So` is MARRATAN U+1ED2E; not Rust whitespace. All other assigned scalars are `No` (Ottoman numerals / alternates / fractions)—excluded. Unassigned tail U+1ED3E+ (`Cn`) excluded. `collapse_whitespace()` maps `U+1ED2E`; tests `ottoman_siyaq_marratan_so_separate_words`, `ottoman_siyaq_number_forms_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D184 | HTML `clean_html`: Arabic Mathematical Alphabetic Symbols U+1EEF0–U+1EEF1 (`Sm`) as word separators | Unicode block U+1EE00–U+1EEFF: only two tail scalars are `Sm` (MEEM WITH HAH WITH TATWEEL, HAH WITH DAL); not Rust whitespace. Mathematical Arabic letters (`Lo`) and unassigned (`Cn`) gaps excluded. `collapse_whitespace()` maps `U+1EEF0` and `U+1EEF1`; tests `arabic_mathematical_alphabetic_operator_sm_separate_words`, `arabic_mathematical_alphabetic_letters_and_block_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D183 | HTML `clean_html`: Arabic Presentation Forms-A U+FD3E–U+FD3F ornate parens + U+FDFC RIAL + U+FDFD Bismillah ligature as word separators | Ornate left/right parentheses (`Pe`/`Ps`), `Sc` rial, `So` Bismillah ligature; not Rust whitespace. Quranic annotation `Sk` U+FBB2–U+FBC1 excluded (word-internal risk). Rest of block (`Lo`) unchanged. `collapse_whitespace()` maps four scalars; tests `arabic_presentation_forms_ornate_parens_rial_and_bismillah_ligature_separate_words`, `arabic_presentation_forms_quranic_symbol_dots_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D182 | HTML `clean_html`: Duployan SIGN O WITH CROSS U+1BC9C (`So`) as word separator | UnicodeData `So` before thick letter selectors; not Rust whitespace. Unassigned U+1BC9A–U+1BC9B (`Cn`) excluded. U+1BC9D–U+1BC9E (`Mn`), U+1BC9F CHINOOK FULL STOP (`Po`), U+1BCA0–U+1BCA3 (`Cf`) unchanged (FEAT-D52 / FEAT-D123). `collapse_whitespace()` maps `U+1BC9C`; tests `ogham_feather_marks_and_duployan_o_cross_and_chinook_full_stop_separate_words`, `duployan_unassigned_gap_u1bc9a_u1bc9b_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D181 | HTML `clean_html`: Balinese extended `Po` / `So` as word separators | Inverted carik `Po` U+1B4E–U+1B4F; sentence / section `Po` U+1B5A–U+1B60 (FEAT-D79) and U+1B7D–U+1B7F; pitch-name and hand-notation `So` U+1B61–U+1B6A and U+1B74–U+1B7C; not Rust whitespace. Digits `Nd` U+1B50–U+1B59 and combining musical `Mn` U+1B6B–U+1B73 excluded. Batak U+1BFC+ remains adjacent arm. `collapse_whitespace()` uses four arms; tests `balinese_inverted_carik_sentence_po_musical_so_and_lantang_separate_words`, `balinese_digits_and_combining_musical_marks_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D180 | HTML `clean_html`: Ancient Greek Numbers U+10179–U+10189 + U+1018C–U+1018E metrological / currency `So` as word separators | Unicode block U+10140–U+1018E: Greek year / talent / obol / measure signs and nomisma (`So`); not Rust whitespace. Acrophonic `Nl` U+10140–U+10174, fraction `No` U+10175–U+10178, `No` zero / quarter U+1018A–U+1018B, and unassigned U+1018F (`Cn`) excluded. Ancient Symbols U+10190+ (FEAT-D169) and Aegean U+10100–U+10102 remain adjacent arms. `collapse_whitespace()` uses `U+10179..=U+10189` and `U+1018C..=U+1018E`; tests `ancient_greek_numbers_metrological_so_subranges_separate_words`, `ancient_greek_numbers_nl_no_and_unassigned_gap_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D179 | HTML `clean_html`: Tamil Supplement U+11FD5–U+11FF1 metrological / currency `So`/`Sc` as word separators | Tamil Supplement land / cultivation / trade signs (`So`) and currency scalars (`Sc`); not Rust whitespace. Fraction numerics U+11FD0–U+11FD4 (`No`) and unassigned U+11FF2–U+11FFE (`Cn`) excluded. U+11FFF END OF TEXT (`Po`) remains on its existing arm (FEAT-D99). `collapse_whitespace()` uses `U+11FD5..=U+11FF1`; tests `tamil_supplement_metrological_and_currency_so_sc_separate_words`, `tamil_supplement_fraction_numerics_and_unassigned_gap_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D178 | HTML `clean_html`: Cyrillic Extended-B U+A673 U+A67E; Syloti Nagri poetry U+A828–U+A82B; North Indic U+A836–U+A839 as word separators | Cyrillic Ext-B Slavonic asterisk and kavyka (`Po`); not Rust whitespace. Combining U+A672 (`Me`), U+A674 / U+A67D (`Mn`), U+A67F (`Lm`) excluded. Syloti Nagri four poetry marks (`So`); letters / Mc / Mn before U+A828 excluded. North Indic quarter / placeholder / rupee / quantity (`So`/`Sc`); fraction numerics U+A830–U+A835 (`No`) and unassigned tail U+A83A–U+A83F excluded. `collapse_whitespace()` maps discrete `Po` + `U+A828..=U+A82B` + `U+A836..=U+A839`; tests `slavonic_asterisk_kavyka_syloti_poetry_north_indic_marks_separate_words`, `slavonic_kavyka_syloti_north_indic_fractions_and_letter_neighbors_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D177 | HTML `clean_html`: Yi Radicals U+A490–U+A4C6 as word separators | Unicode block: all 55 assigned scalars are `So`; not Rust whitespace. Unassigned tail U+A4C7–U+A4CF (`Cn`) and Lisu letters U+A4D0+ (`Lo`) excluded. Yijing hexagrams U+4DC0–U+4DFF (FEAT-D168) remain adjacent CJK-related `So` arm. `collapse_whitespace()` uses `U+A490..=U+A4C6`; tests `yi_radicals_u_a490_through_u_a4c6_all_so_separate_words`, `yi_radicals_unassigned_tail_and_neighbor_lisu_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D176 | HTML `clean_html`: Ideographic Description Characters extended U+2FFC–U+2FFF as word separators | Unicode block U+2FF0–U+2FFF: original IDCs U+2FF0–U+2FFB (FEAT-D53) plus overlap/surround/overlay/rotation U+2FFC–U+2FFF (all `So`); not Rust whitespace. Block fully assigned—no `Cn` tail. CJK Symbols U+3000+ and CJK Extension A U+3400+ (`Lo`) excluded elsewhere. `collapse_whitespace()` uses one arm `U+2FF0..=U+2FFF`; test `ideographic_description_characters_separate_words` loops the full block in `commands/html_cleaning.rs`. |
| FEAT-D175 | HTML `clean_html`: Kangxi Radicals assigned `So` U+2F00–U+2FD5 as word separators | Unicode block U+2F00–U+2FDF: all 214 assigned scalars are `So`; not Rust whitespace. Unassigned tail U+2FD6–U+2FDF (Cn) and inter-block gap U+2FE0–U+2FEF (Cn) excluded. Ideographic Description Characters U+2FF0–U+2FFF: FEAT-D176 extends FEAT-D53 arm. CJK Unified Ideographs Extension A U+3400+ (`Lo`) excluded. `collapse_whitespace()` maps to ASCII space; tests `kangxi_radicals_so_u2f00_through_u2fd5_separate_words`, `kangxi_radicals_unassigned_tail_and_inter_block_gap_stay_unmapped`, `kangxi_radicals_neighbor_cjk_ext_a_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D174 | HTML `clean_html`: CJK Radicals Supplement assigned `So` subranges as word separators | Block U+2E80–U+2EFF: contiguous `So` U+2E80–U+2E99 and U+2E9B–U+2EF3; not Rust whitespace. Unassigned U+2E9A and tail U+2EF4–U+2EFF (Cn) excluded. Kangxi Radicals U+2F00+ covered by FEAT-D175. Ideographic Description Characters U+2FF0–U+2FFF: FEAT-D176 / FEAT-D53 arm. `collapse_whitespace()` maps to ASCII space; tests `cjk_radicals_supplement_so_assigned_subranges_separate_words`, `cjk_radicals_supplement_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D173 | HTML `clean_html`: Kanbun assigned `So` + CJK Strokes U+31C0–U+31E3 as word separators | Kanbun block U+3190–U+319F: `So` U+3190–U+3191 (linking/reverse) and U+3196–U+319F (annotation position marks); not Rust whitespace. `No` ideographic annotation digit marks U+3192–U+3195 excluded. Bopomofo Extended U+31A0–U+31BF (`Lo`) excluded. CJK Strokes U+31C0–U+31E3 (all `So`); gap U+31E4–U+31EF (Cn) and Katakana Phonetic Extensions U+31F0–U+31FF (`Lo`) excluded. `collapse_whitespace()` maps to ASCII space; tests `kanbun_so_marks_and_cjk_strokes_separate_words`, `kanbun_no_numerics_bopomofo_ext_and_strokes_gap_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D172 | HTML `clean_html`: CJK Compatibility U+3300–U+33FF as word separators | Unicode block is fully assigned and all scalars are `So` (squared katakana words, telegraph-era labels, etc.); not Rust whitespace. Neighbor CJK Unified Ideographs Extension A U+3400+ (`Lo`) excluded. `collapse_whitespace()` maps contiguous `U+3300..=U+33FF` to ASCII space; tests `cjk_compatibility_u3300_through_u33ff_all_so_separate_words`, `cjk_compatibility_neighbor_cjk_ext_a_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D171 | HTML `clean_html`: Enclosed CJK Letters and Months U+3200–U+32FF assigned `So` as word separators | Block mixes `So` (parenthesized/circled Hangul, CJK-in-parens labels, ideographic telegraph months, circled katakana, squared Latin such as PTE/Hz/eV, Reiwa square) and `No` numerics; not Rust whitespace. All `No` in block excluded: U+3220–U+3229, U+3248–U+324F, U+3251–U+325F, U+3280–U+3289, U+32B1–U+32BF. Unassigned U+321F (Cn) excluded. Distinct from Enclosed Alphanumerics U+2460+ (FEAT-D131) and Enclosed Alphanumeric Supplement U+1F100+ (FEAT-D148). `collapse_whitespace()` maps to ASCII space; tests `enclosed_cjk_letters_and_months_so_assigned_subranges_separate_words`, `enclosed_cjk_letters_and_months_no_numerics_and_gap_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D170 | HTML `clean_html`: Manichaean SIGN UD U+10AC8 as word separator | UnicodeData `So` among Manichaean letters; not Rust whitespace. Abbreviation marks U+10AE5–U+10AE6 (`Mn`) and numbers U+10AEB–U+10AEF (`No`) excluded. Po U+10AF0–U+10AF6 remain on existing arm (extends FEAT-D108 ancient-dividers test). `collapse_whitespace()` maps to ASCII space; tests `ancient_word_dividers_and_manichaean_punctuation_separate_words`, `manichaean_abbreviation_marks_and_numbers_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D169 | HTML `clean_html`: Ancient Symbols assigned So + Phaistos Disc logograms as word separators | Ancient Symbols U+10190–U+101CF: So U+10190–U+1019C, U+101A0; gaps U+1019D–U+1019F, U+101A1–U+101CF (Cn) excluded. Phaistos Disc U+101D0–U+101FC (So); Mn U+101FD, tail U+101FE–U+101FF (Cn) excluded. Distinct from Aegean U+10100–U+10102 and Ancient Greek Numbers U+10140–U+1018E. `collapse_whitespace()` maps to ASCII space; tests `ancient_symbols_assigned_so_and_phaistos_disc_logograms_separate_words`, `ancient_symbols_block_gaps_phaistos_mn_and_tail_unassigned_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D168 | HTML `clean_html`: Yijing Hexagram Symbols U+4DC0–U+4DFF as word separators | Block (all So); not Rust whitespace. CJK Unified Ideographs Extension A U+3400–U+4DBF (Lo) and CJK Unified U+4E00+—excluded. `collapse_whitespace()` maps to ASCII space; tests `yijing_hexagram_symbols_u4dc0_through_u4dff_separate_words`, `yijing_neighbors_cjk_ext_a_and_unified_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D167 | HTML `clean_html`: Znamenny Musical Notation assigned So neumes as word separators | Block U+1CF00–U+1CFCF: contiguous So U+1CF50–U+1CFC3 (neumes); not Rust whitespace. Mn U+1CF00–U+1CF2D, U+1CF30–U+1CF46—excluded. Gaps U+1CF2E–U+1CF2F, U+1CF47–U+1CF4F, U+1CFC4–U+1CFCF unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `znamenny_neume_so_assigned_subrange_separate_words`, `znamenny_combining_mn_and_block_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D166 | HTML `clean_html`: Sutton SignWriting assigned So subranges as word separators | Block U+1D800–U+1DAAF: So U+1D800–U+1D9FF, U+1DA37–U+1DA3A, U+1DA6D–U+1DA74, U+1DA76–U+1DA83, U+1DA85–U+1DA86; not Rust whitespace. Mn U+1DA00–U+1DA36, U+1DA3B–U+1DA6C, U+1DA75, U+1DA84, U+1DA9B–U+1DA9F, U+1DAA1–U+1DAAF—excluded. Po U+1DA87–U+1DA8B on existing arm (FEAT-D115). Gap U+1DA8C–U+1DA9A unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `sutton_signwriting_so_assigned_subranges_separate_words`, `sutton_signwriting_mn_unassigned_gap_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D165 | HTML `clean_html`: Mathematical Alphanumeric Symbols — ten Sm nabla / partial-diff scalars as word separators | Block U+1D400–U+1D7FF is mostly Lu/Ll/Nd (styled letters/digits)—excluded. Only UnicodeData Sm MATHEMATICAL * NABLA / * PARTIAL DIFFERENTIAL in styled faces U+1D6C1 U+1D6DB U+1D6FB U+1D715 U+1D735 U+1D74F U+1D76F U+1D789 U+1D7A9 U+1D7C3 mapped; not Rust whitespace. BMP ∇/∂ remain U+2200–U+22FF. Unassigned Cn gaps and letters/digits stay unmapped. `collapse_whitespace()` maps to ASCII space; tests `mathematical_alphanumeric_nabla_partial_differential_sm_separate_words`, `mathematical_alphanumeric_letters_digits_and_block_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D164 | HTML `clean_html`: Tai Xuan Jing Symbols U+1D300–U+1D356 as word separators | Block U+1D300–U+1D356 (all So); not Rust whitespace. Unassigned before U+1D300 (e.g. U+1D246–U+1D2FF gap scalars), Mayan numerals U+1D2E0–U+1D2F3 (No), gap U+1D357–U+1D35F, and Counting Rod Numerals U+1D360–U+1D378 (No)—excluded. `collapse_whitespace()` maps to ASCII space; tests `tai_xuan_jing_symbols_assigned_subrange_separate_words`, `tai_xuan_jing_unassigned_gaps_and_numeric_neighbors_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D163 | HTML `clean_html`: Ancient Greek Musical Notation assigned subrange as word separators | Block U+1D200–U+1D24F: assigned contiguous U+1D200–U+1D245 (mostly So; U+1D242–U+1D244 Mn combining triseme/tetraseme/pentaseme); not Rust whitespace. Tail U+1D246–U+1D24F unassigned—excluded. Distinct from Phonetic Extensions U+1D20–U+1D2B (Ll) and Western Musical Symbols U+1D100+. `collapse_whitespace()` maps to ASCII space; tests `ancient_greek_musical_notation_assigned_subrange_separate_words`, `ancient_greek_musical_notation_unassigned_tail_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D162 | HTML `clean_html`: Western Musical Symbols assigned subranges as word separators | Block U+1D100–U+1D1FF: assigned U+1D100–U+1D126, U+1D129–U+1D172, U+1D17B–U+1D1EA (mostly So; Mc/Mn combining stems/flags/articulation per UnicodeData); not Rust whitespace. Gaps U+1D127–U+1D128 and U+1D1EB–U+1D1FF unassigned—excluded. U+1D173–U+1D17A (Cf) stays on the dedicated format arm (FEAT-D51). `collapse_whitespace()` maps to ASCII space; tests `western_musical_symbols_assigned_subranges_separate_words`, `western_musical_symbols_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D161 | HTML `clean_html`: Byzantine Musical Symbols U+1D000–U+1D0F5 as word separators | Assigned subrange (all So); not Rust whitespace. Tail U+1D0F6–U+1D0FF unassigned—excluded. Distinct from Western Musical Symbols U+1D100+ and Cf format controls U+1D173–U+1D17A. `collapse_whitespace()` maps to ASCII space; tests `byzantine_musical_symbols_assigned_subrange_separate_words`, `byzantine_musical_symbols_unassigned_tail_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D160 | HTML `clean_html`: Miscellaneous Symbols Supplement assigned subranges as word separators | Block U+1CEC0–U+1CEFF (Unicode 17): assigned So U+1CEC0–U+1CED0 (asteroid symbols), U+1CEE0–U+1CEEF (geomantic figures), U+1CEF0 (medium small white circle with horizontal bar); not Rust whitespace. Gaps U+1CED1–U+1CEDF and U+1CEF1–U+1CEFF unassigned—excluded. Distinct from Symbols for Legacy Computing Supplement U+1CC00+ (FEAT-D159). `collapse_whitespace()` maps to ASCII space; tests `miscellaneous_symbols_supplement_assigned_subranges_separate_words`, `miscellaneous_symbols_supplement_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D159 | HTML `clean_html`: Symbols for Legacy Computing Supplement assigned subranges as word separators | U+1CC00–U+1CCFC, U+1CD00–U+1CEB3, U+1CEBA–U+1CEBF (mostly So; Unicode 17 block); not Rust whitespace. Sundanese Supplement U+1CC0–U+1CC7 (Po) remains a separate arm—distinct from U+1CC00+. Gaps U+1CCFD–U+1CCFF and U+1CEB4–U+1CEB9 unassigned—excluded. U+1CEC0+ (Miscellaneous Symbols Supplement) covered by FEAT-D160. `collapse_whitespace()` maps to ASCII space; tests `symbols_for_legacy_computing_supplement_assigned_subranges_separate_words`, `symbols_for_legacy_computing_supplement_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D158 | HTML `clean_html`: Symbols for Legacy Computing assigned So subranges as word separators | U+1FB00–U+1FB92 and U+1FB94–U+1FBEF (mostly So); not Rust whitespace. U+1FB93 unassigned; U+1FBF0–U+1FBF9 Nd (segmented digits) and U+1FBFA–U+1FBFF unassigned—excluded. Distinct from Greek Extended U+1FB0–U+1FBF. `collapse_whitespace()` maps to ASCII space; tests `symbols_for_legacy_computing_assigned_so_subranges_separate_words`, `symbols_for_legacy_computing_unassigned_gap_nd_and_tail_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D157 | HTML `clean_html`: Chess Symbols + Symbols and Pictographs Extended-A assigned subranges as word separators | Chess: U+1FA00–U+1FA57, U+1FA60–U+1FA6D (mostly So); gaps U+1FA58–U+1FA5F, U+1FA6E–U+1FA6F unassigned—excluded. Extended-A: U+1FA70–U+1FA7C, U+1FA80–U+1FA8A, U+1FA8E–U+1FA8F, U+1FA90–U+1FAC6, U+1FAC8, U+1FACD–U+1FACF, U+1FAD0–U+1FADC, U+1FADF, U+1FAE0–U+1FAEA, U+1FAEF, U+1FAF0–U+1FAF8 (mostly So / emoji presentation); gaps per UnicodeData unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `chess_symbols_assigned_subranges_separate_words`, `chess_symbols_unassigned_gaps_stay_unmapped`, `symbols_and_pictographs_extended_a_assigned_subranges_separate_words`, `symbols_and_pictographs_extended_a_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D156 | HTML `clean_html`: Supplemental Symbols and Pictographs U+1F900–U+1F9FF as word separators | Contiguous block (mostly So / emoji presentation; fully assigned Unicode 14.0+); not Rust whitespace. `collapse_whitespace()` maps to ASCII space; test `supplemental_symbols_and_pictographs_u1f900_through_u1f9ff_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D155 | HTML `clean_html`: Supplemental Arrows-C U+1F800–U+1F8FF assigned subranges as word separators | Six assigned subranges (mostly Sm); not Rust whitespace. Gaps U+1F80C–U+1F80F, U+1F848–U+1F84F, U+1F85A–U+1F85F, U+1F888–U+1F88F, U+1F8AE–U+1F8AF, U+1F8B2–U+1F8FF unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `supplemental_arrows_c_assigned_subranges_separate_words`, `supplemental_arrows_c_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D154 | HTML `clean_html`: Geometric Shapes Extended U+1F780–U+1F7D8 + U+1F7E0–U+1F7EB as word separators | Assigned subranges (mostly So / emoji presentation); not Rust whitespace. Gaps U+1F7D9–U+1F7DF, U+1F7EC–U+1F7FF unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `geometric_shapes_extended_assigned_subranges_separate_words`, `geometric_shapes_extended_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D153 | HTML `clean_html`: Alchemical Symbols U+1F700–U+1F773 as word separators | Contiguous assigned subrange (all So); not Rust whitespace. Tail U+1F774–U+1F77F unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `alchemical_symbols_u1f700_through_u1f773_separate_words`, `alchemical_symbols_unassigned_tail_u1f774_through_u1f77f_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D152 | HTML `clean_html`: Transport and Map Symbols assigned subranges as word separators | U+1F680–U+1F6D7, U+1F6E0–U+1F6EC, U+1F6F0–U+1F6FC (mostly So / emoji presentation); not Rust whitespace. Gaps U+1F6D8–U+1F6DF, U+1F6ED–U+1F6EF, U+1F6FD–U+1F6FF unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `transport_and_map_symbols_assigned_subranges_separate_words`, `transport_and_map_symbols_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D151 | HTML `clean_html`: Miscellaneous Symbols and Pictographs + Emoticons U+1F300–U+1F67F as word separators | Contiguous U+1F300–U+1F5FF (Miscellaneous Symbols and Pictographs), U+1F600–U+1F64F (Emoticons), U+1F650–U+1F67F ornamental dingbats (mostly So); not Rust whitespace. Unassigned U+1F266–U+1F2FF before U+1F300 already excluded with Enclosed Ideographic Supplement gaps. `collapse_whitespace()` maps to ASCII space; test `miscellaneous_symbols_pictographs_and_emoticons_u1f300_through_u1f67f_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D150 | HTML `clean_html`: Mahjong / Domino / Playing Cards as word separators | U+1F000–U+1F02F Mahjong Tiles, U+1F030–U+1F093 Domino Tiles, U+1F0A0–U+1F0FF Playing Cards (all So); not Rust whitespace. Reserved U+1F094–U+1F09F—excluded. `collapse_whitespace()` maps to ASCII space; tests `mahjong_domino_and_playing_cards_separate_words`, `mahjong_domino_playing_cards_reserved_gap_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D149 | HTML `clean_html`: Enclosed Ideographic Supplement assigned subranges as word separators | U+1F200–U+1F202, U+1F210–U+1F23B, U+1F240–U+1F248, U+1F250–U+1F251, U+1F260–U+1F265 (squared katakana/CJK, tortoise-shell bracketed ideographs, circled advantage/accept, rounded fu/lu/shou/xi/shuangxi/cai; all So); not Rust whitespace. Gaps U+1F203–U+1F20F, U+1F23C–U+1F23F, U+1F249–U+1F24F, U+1F252–U+1F25F, U+1F266–U+1F2FF unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `enclosed_ideographic_supplement_assigned_separate_words`, `enclosed_ideographic_supplement_unassigned_gaps_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D148 | HTML `clean_html`: Enclosed Alphanumeric Supplement U+1F100–U+1F1AD + U+1F1E6–U+1F1FF as word separators | Parenthesized / circled / squared Latin and digits, dingbat circled sans-serif digits, mask work symbol, regional indicator letters (No / So); not Rust whitespace. Unicode 16 gap U+1F1AE–U+1F1E5 unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `enclosed_alphanumeric_supplement_assigned_separate_words`, `enclosed_alphanumeric_supplement_unassigned_gap_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D147 | HTML `clean_html`: Letterlike Symbols U+2100–U+214F — So/Sm subranges as word separators | Account-of, degree / care signs, numero, prescription, service / trade marks, sans-serif turned math symbols, per / aktieselskab, Samaritan source, etc.; not Rust whitespace. Lu/Ll/Lo mathematical letters (e.g. U+2102, U+210E, U+2115, U+2126, U+2135–U+2138, double-struck italics) and U+20D0–U+20FF combining marks stay unmapped. U+2139 INFORMATION SOURCE (`So`) maps with U+213A–U+213B (FEAT-D251). `collapse_whitespace()` maps to ASCII space; tests `letterlike_symbol_subranges_separate_words`, `letterlike_mathematical_letters_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D146 | HTML `clean_html`: Number Forms U+2150–U+2182 + U+2185–U+218B as word separators | Vulgar fractions, Roman numerals, late Roman forms, vulgar fraction zero thirds (U+2189, `No`), turned digit two/three U+218A–U+218B (`So` in UnicodeData; **FEAT-D265**); remaining mapped scalars `No`/`Nl`. Not Rust whitespace. Typography or Unicode-sample HTML can place ⅓, Ⅷ, etc. between Latin tokens without ASCII space. U+2183 (Lu) and U+2184 (Ll) omitted—letter-like, word-internal risk. U+218C–U+218F unassigned—excluded. `collapse_whitespace()` maps to ASCII space; tests `number_forms_fractions_romans_and_turned_digits_separate_words`, `number_forms_reversed_roman_c_and_reversed_c_letter_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D145 | HTML `clean_html`: Control Pictures U+2400–U+243F as word separators | Contiguous block (all So); not Rust whitespace. Terminal dumps, legacy copy, or Unicode-sample HTML can place control-picture glyphs between Latin tokens without ASCII space. U+2440+ OCR block covered separately (FEAT-D130). `collapse_whitespace()` maps to ASCII space; test `control_pictures_u2400_through_u243f_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D144 | HTML `clean_html`: Miscellaneous Technical U+2300–U+23FF as word separators | Contiguous block (mostly So / Sm / Po); not Rust whitespace. Technical, APL, or Unicode-sample HTML can place electrotechnical symbols, dice faces, etc. between Latin tokens without ASCII space. Control Pictures U+2400–U+243F added in FEAT-D145; U+2440+ OCR in FEAT-D130. `collapse_whitespace()` maps to ASCII space; test `miscellaneous_technical_u2300_through_u23ff_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D143 | HTML `clean_html`: Mathematical Operators U+2200–U+22FF as word separators | Contiguous block (mostly Sm; some Mn / Me); not Rust whitespace. MathML or Unicode-sample HTML can place ∀, ∈, integrals, etc. between Latin tokens without ASCII space. Subsumes prior explicit U+2215 / U+2219 / U+22C5 arms. U+2300+ Miscellaneous Technical covered by FEAT-D144. `collapse_whitespace()` maps to ASCII space; test `mathematical_operators_u2200_through_u22ff_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D142 | HTML `clean_html`: Arrows U+2190–U+21FF as word separators | Contiguous Arrows block (mostly Sm); not Rust whitespace. MathML, diagram, or Unicode-sample HTML can place arrows between Latin tokens without ASCII space. U+2200+ Mathematical Operators block not included. `collapse_whitespace()` maps to ASCII space; test `arrows_u2190_through_u21ff_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D141 | HTML `clean_html`: Currency Symbols U+20A0–U+20BF as word separators | Contiguous assigned Currency Symbols (all Sc); not Rust whitespace. **Tail extended in FEAT-D222:** U+20C0 SOM SIGN + U+20C1 SAUDI RIYAL SIGN (`Sc`); `U+20A0..=U+20C1`; U+20C2–U+20CF unassigned—excluded. Historical row text stopped at U+20BF; tests renamed `currency_symbols_u20a0_through_u20c1_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D140 | HTML `clean_html`: Coptic symbols U+2CE5–U+2CEA + Old Nubian punctuation U+2CF9–U+2CFC as word separators | Coptic MI RO through SHIMA SIMA (So); Old Nubian full stop / question marks / verse divider (Po); not Rust whitespace. U+2CFD FRACTION ONE HALF (No) stays unmapped. U+2CFE–U+2CFF remain on the same arm (FEAT-D122). `collapse_whitespace()` maps to ASCII space; test `coptic_full_stop_morphological_divider_and_linear_b_word_separators_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D139 | HTML `clean_html`: Miscellaneous Symbols and Arrows U+2B00–U+2BFF as word separators | Contiguous `U+2B00..=U+2BFF` (mostly So / Sm / Po); not Rust whitespace. Diagram, chess-piece, or Unicode-sample HTML can place arrows and symbols between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `miscellaneous_symbols_and_arrows_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D138 | HTML `clean_html`: Supplemental Mathematical Operators U+2A00–U+2AFF as word separators | Contiguous `U+2A00..=U+2AFF` (mostly Sm); not Rust whitespace. Advanced MathML or Unicode-sample HTML can place n-ary / large operators between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `supplemental_mathematical_operators_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D137 | HTML `clean_html`: Supplemental Arrows-B U+2900–U+297F + Miscellaneous Mathematical Symbols-B U+2980–U+29FF as word separators | Contiguous `U+2900..=U+29FF` (mostly Sm); not Rust whitespace. MathML / LaTeX-to-HTML or Unicode-sample pages can place supplemental arrows and math-B delimiters/operators between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `supplemental_arrows_b_and_miscellaneous_mathematical_symbols_b_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D136 | HTML `clean_html`: Braille Patterns U+2800–U+28FF as word separators | Contiguous `U+2800..=U+28FF` (all So); not Rust whitespace. Prior FEAT-D48 mapped only U+2800 blank; full block now so raised-dot cells in accessibility or Unicode-sample HTML do not glue Latin tokens. `collapse_whitespace()` maps to ASCII space; test `braille_patterns_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D135 | HTML `clean_html`: Miscellaneous Mathematical Symbols-A U+27C0–U+27EF + Supplemental Arrows-A U+27F0–U+27FF as word separators | Contiguous `U+27C0..=U+27FF` (mostly Sm); not Rust whitespace. MathML / LaTeX-to-HTML or Unicode-sample pages can place angles, set notation, long arrows, etc. between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `miscellaneous_mathematical_symbols_a_and_supplemental_arrows_a_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D134 | HTML `clean_html`: Dingbats U+2700–U+27BF as word separators | Contiguous `U+2700..=U+27BF` (mostly So / Sm / Po); not Rust whitespace. Zapf Dingbats–style or Unicode-sample HTML can place checkmarks, crosses, bullets, etc. between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `dingbats_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D133 | HTML `clean_html`: Miscellaneous Symbols U+2600–U+26FF as word separators | Contiguous `U+2600..=U+26FF` (mostly So / Sm / Po); not Rust whitespace. Dingbat / weather / chess / card-suit / warning-sign HTML or Unicode samples can place symbols between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `miscellaneous_symbols_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D132 | HTML `clean_html`: Box Drawing U+2500–U+257F + Block Elements U+2580–U+259F + Geometric Shapes U+25A0–U+25FF as word separators | Contiguous `U+2500..=U+25FF` (mostly So); not Rust whitespace. Table / ASCII-art / diagram HTML can place light horizontal, shaded blocks, or geometric symbols between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `box_drawing_block_elements_geometric_shapes_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D131 | HTML `clean_html`: Enclosed Alphanumerics U+2460–U+24FF as word separators | Full block: circled / parenthesized digits, digit full stops, enclosed Latin letters, negative circled digits, etc. (No / So); not Rust whitespace. List-style or Unicode-sample HTML can place them between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `enclosed_alphanumerics_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D130 | HTML `clean_html`: Optical Character Recognition U+2440–U+245F as word separators | Full OCR block (hook through double backslash; all So); not Rust whitespace. Scanned-PDF / OCR HTML exports can place zoning symbols between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `ocr_symbols_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D129 | HTML `clean_html`: Unicode REPLACEMENT CHARACTER U+FFFD as word separator | U+FFFD (So) in Halfwidth and Fullwidth Forms; not Rust whitespace. Extends the `U+FFF9..=U+FFFC` arm to `U+FFF9..=U+FFFD` so transcoding / mojibake HTML does not glue Latin tokens. U+FFFE–U+FFFF non-characters stay unmapped. Test `interlinear_annotation_object_replacement_and_replacement_char_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D128 | HTML `clean_html`: Halfwidth and Fullwidth Forms U+FFEF as word separator | U+FFEF HALFWIDTH BLACK SMALL SQUARE (So); extends FEAT-D127 arm to `U+FFE8..=U+FFEF`, completing the block’s last assigned scalar. U+FFE3 FULLWIDTH MACRON (Sk) still omitted. `collapse_whitespace()` maps to ASCII space; test `halfwidth_forms_light_vertical_arrows_square_circle_small_square_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D127 | HTML `clean_html`: Halfwidth and Fullwidth Forms U+FFE8–U+FFEE as word separators | Halfwidth forms light vertical (So), halfwidth arrows (Sm), halfwidth black square / white circle (So). Completes the block tail after U+FFE0–U+FFE6; U+FFE3 FULLWIDTH MACRON (Sk) still omitted. Range extended to U+FFEF in FEAT-D128; test renamed `halfwidth_forms_light_vertical_arrows_square_circle_small_square_separate_words`. |
| FEAT-D126 | HTML `clean_html`: Halfwidth and Fullwidth Forms U+FF5F–U+FF64 + U+FFE0–U+FFE2 U+FFE4–U+FFE6 as word separators | Fullwidth white parentheses (Ps/Pe); halfwidth ideographic full stop, corner brackets, ideographic comma (Po/Ps/Pe); U+FF65 middle dot remains on middle-dot arm. Fullwidth cent / pound / not / broken bar / yen / won (Sc/Sm/So). U+FFE3 FULLWIDTH MACRON (Sk) omitted—overline / word-internal risk. `collapse_whitespace()` maps to ASCII space; test `fullwidth_white_parens_halfwidth_cjk_punct_and_fullwidth_currency_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D125 | HTML `clean_html`: Halfwidth and Fullwidth Forms U+FF03–U+FF06 U+FF08–U+FF0B U+FF0D–U+FF0E U+FF1C–U+FF1E U+FF20 U+FF3B–U+FF3D U+FF5B–U+FF5D as word separators | Number / dollar / percent / ampersand; fullwidth parens; asterisk / plus; hyphen-minus / full stop; less / equals / greater; commercial at; square brackets / reverse solidus; curly braces / vertical line (Po/Ps/Pe/Sm/Pd/Sc as assigned). U+FF07 apostrophe, U+FF5E tilde, U+FF40 grave omitted (word-internal or range-notation risk). Extends FEAT-D62 fullwidth `,;:!?` arm. `collapse_whitespace()` maps to ASCII space; test `fullwidth_delimiters_operators_and_brackets_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D124 | HTML `clean_html`: Tifinagh U+2D70 + Arabic U+0609–U+060B U+060D U+061E + U+066D as word separators | Tifinagh SEPARATOR MARK (Po). Arabic-indic per mille / per ten thousand (Po), AFGHANI SIGN (Sc), DATE SEPARATOR (Po), TRIPLE DOT PUNCTUATION (Po), FIVE POINTED STAR (Po); extends U+066A–U+066C arm to U+066D. U+0606–U+0608 (Sm cube/fourth root, ray) omitted. `collapse_whitespace()` maps to ASCII space; tests `tifinagh_separator_and_arabic_supplementary_punctuation_separate_words`, `latin1_greek_and_arabic_script_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D123 | HTML `clean_html`: Ogham U+169B–U+169C + Duployan U+1BC9F as word separators | Ogham feather / reversed feather mark (Ps/Pe). Duployan CHINOOK FULL STOP (Po); extends U+1BC9D–U+1BC9E Mn + U+1BCA0–U+1BCA3 Cf arm. `collapse_whitespace()` maps to ASCII space; test `ogham_feather_marks_and_duployan_chinook_full_stop_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D122 | HTML `clean_html`: Coptic U+2CFE–U+2CFF + Linear B U+10079–U+1007C as word separators | Coptic full stop / morphological divider (Po). Linear B word separator dot through large (Po). `collapse_whitespace()` maps to ASCII space; test `coptic_full_stop_morphological_divider_and_linear_b_word_separators_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D121 | HTML `clean_html`: Phags-pa U+A874–U+A877 + Saurashtra U+A8CE–U+A8CF + Devanagari Ext U+A8F8–U+A8FA U+A8FC + Myanmar Ext-A U+AA77–U+AA79 + Meetei U+ABEB as word separators | Phags-pa head/shad (Po); Saurashtra dandas (Po); Devanagari Extended pushpika through siddham (Po); Aiton symbols (So); Meetei cheikhei (Po). U+A8FB (Lo) omitted. `collapse_whitespace()` maps to ASCII space; test `phags_pa_saurashtra_devanagari_ext_myanmar_aiton_meetei_cheikhei_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D120 | HTML `clean_html`: Myanmar U+104C–U+104F as word separators | Locative, exclamation, completed, aforementioned symbols (all So); extends U+104A–U+104B (Po). `collapse_whitespace()` maps to ASCII space; test `thai_lao_myanmar_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D119 | HTML `clean_html`: Bengali U+09FA U+09FD + Oriya U+0B70 + Tamil U+0BF3–U+0BFA + Malayalam U+0D4F U+0D79 as word separators | Isshar marks (So), Bengali abbreviation (Po), Tamil financial/day signs (So/Sc), Malayalam para/date (So). `collapse_whitespace()` maps to ASCII space; test `bengali_oriya_tamil_malayalam_issher_abbrev_and_financial_signs_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D118 | HTML `clean_html`: Gurmukhi U+0A76 + Gujarati U+0AF0 + Telugu U+0C77 + Kannada U+0C84 as word separators | Abbreviation signs (Gurmukhi, Gujarati) and sign siddham (Telugu, Kannada), all Po. `collapse_whitespace()` maps to ASCII space; test `gurmukhi_gujarati_telugu_kannada_abbreviation_siddham_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D117 | HTML `clean_html`: Vithkuqi U+1057B–U+1057F as word separators | Comma, full stop, colon, semicolon, question mark (all Po). U+10570–U+1057A (Lo) omitted. `collapse_whitespace()` maps to ASCII space; test `vithkuqi_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D116 | HTML `clean_html`: Yezidi U+10EAD + Indic Siyaq U+1ECAC U+1ECB0 as word separators | Yezidi hyphenation mark (Pd). Indic Siyaq placeholder (So) and rupee mark (Sc). `collapse_whitespace()` maps to ASCII space; test `yezidi_hyphenation_and_indic_siyaq_placeholder_rupee_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D115 | HTML `clean_html`: Arabic Extended-C U+10ED0 + SignWriting U+1DA87–U+1DA8B + Ol Onal U+1E5FF as word separators | Biblical end of verse (Po). Sutton SignWriting comma through parenthesis (Po). Ol Onal abbreviation sign (Po). U+10ED1–U+10ED8 honorific ligatures (So) omitted. `collapse_whitespace()` maps to ASCII space; test `arabic_biblical_end_of_verse_signwriting_and_ol_onal_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D114 | HTML `clean_html`: Garay U+10D6E + U+10D8E–U+10D8F as word separators | HYPHEN (Pd). PLUS / MINUS SIGN (Sm). `collapse_whitespace()` maps to ASCII space; test `garay_hyphen_plus_and_minus_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D113 | HTML `clean_html`: Devanagari Extended-A U+11B00–U+11B09 + Bassa Vah U+16AF5 as word separators | Head marks through SIGN MINDU (all Po). Bassa Vah FULL STOP (Po). `collapse_whitespace()` maps to ASCII space; test `devanagari_extended_a_and_bassa_vah_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D112 | HTML `clean_html`: Sunuwar U+11BE1 + Kirat Rai U+16D6D–U+16D6F as word separators | Sunuwar SIGN PVO (Po). Kirat Rai SIGN YUPI, DANDA, DOUBLE DANDA (all Po). U+16D6B VIRAMA / U+16D6C SAAT (Lm) omitted. `collapse_whitespace()` maps to ASCII space; test `sunuwar_pvo_and_kirat_rai_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D111 | HTML `clean_html`: Hanifi Rohingya U+10D29–U+10D2D + Warang Citi U+118C8–U+118CF as word separators | Historical Hanifi arm assumed sentence `Po` at U+10D29–U+10D2D. **Superseded for Hanifi:** UnicodeData now leaves U+10D28–U+10D2F unassigned (`Cn`); **FEAT-D254** removed that arm. **Superseded for Warang Citi:** Unicode 14+ assigns U+118C8–U+118CF as small letters (`Ll`); FEAT-D212 removed that arm. Tests: Hanifi gap / Mn / Nd after FEAT-D254; `warang_citi_small_letters_u118c8_through_u118cf_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D110 | HTML `clean_html`: Nabataean U+1089F + Hatran U+108FB + Old North Arabian U+10A9D + Elymaic U+10FF5 as word separators | Nabataean / Hatran / Old North Arabian: all `Po`. U+10A9E / U+10A9F (Old North Arabian numbers, Nl) omitted. **Superseded for Elymaic:** Unicode assigns U+10FF5 as LETTER TAW (`Lo`); FEAT-D213 removed that arm. `collapse_whitespace()` maps three epigraphic `Po` scalars; tests `nabataean_hatran_old_north_arabian_word_separators_separate_words`, `elymaic_letter_taw_lo_u10ff5_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D109 | HTML `clean_html`: Palmyrene U+10877–U+10878 + Pahawh Hmong U+16B37–U+16B3B U+16B44 as word separators | Palmyrene left/right fleuron (So). Pahawh Hmong VOS THOM through VOS FEEM + XAUS (Po). U+16B30–U+16B36 (Mn), U+16B40–U+16B43 (Lm), U+16B3C–U+16B3F / U+16B45 (So) omitted. `collapse_whitespace()` maps to ASCII space; test `palmyrene_fleurons_and_pahawh_hmong_clause_signs_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D108 | HTML `clean_html`: Aegean U+10102 + ancient word dividers U+1039F U+103D0 U+1056F U+10857 U+1093F U+10A7F + Manichaean U+10AF0–U+10AF6 as word separators | Aegean CHECK MARK extends U+10100–U+10101 arm. Ugaritic / Old Persian / Caucasian Albanian / Imperial Aramaic / Lydian / Old South Arabian + Manichaean punctuation (all Po). `collapse_whitespace()` maps to ASCII space; tests `aegean_and_phoenician_word_separator_marks_separate_words`, `ancient_word_dividers_and_manichaean_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D107 | HTML `clean_html`: Takri U+116B9–U+116BC as word separators | Abbreviation sign, double danda, danda, section mark (all Po). U+116B8 LETTER SSA (Lo) omitted. `collapse_whitespace()` maps to ASCII space; test `takri_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D106 | HTML `clean_html`: Mongolian Supplement U+11660–U+1166C as word separators | Ornamental birga marks BIRGA WITH ORNAMENT through TURNED SWIRL BIRGA WITH DOUBLE ORNAMENT (all Po). Distinct from Basic Mongolian U+1800–U+180E. `collapse_whitespace()` maps to ASCII space; test `mongolian_supplement_birga_ornaments_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D105 | HTML `clean_html`: Siddham U+115C1–U+115D7 as word separators | SIGN SIDDHAM through SECTION MARK WITH CIRCLES AND FOUR ENCLOSURES (all Po). U+115C0 SIGN NUKTA (Mn) omitted. `collapse_whitespace()` maps to ASCII space; test `siddham_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D104 | HTML `clean_html`: Sharada U+111DB + U+111DD–U+111DF as word separators | SIGN SIDDHAM + CONTINUATION SIGN + SECTION MARK-1/2 (all Po). U+111DA EKAM + U+111DC HEADSTROKE (Lo) omitted. `collapse_whitespace()` maps to ASCII space; `chakma_sharada_and_khojki_sentence_punctuation_separate_words` extended in `commands/html_cleaning.rs`. |
| FEAT-D103 | HTML `clean_html`: Old Uyghur U+10F86–U+10F89 as word separators | All Po (punctuation bar through four dots). U+10F82–U+10F85 combining dots (Mn) omitted. `collapse_whitespace()` maps to ASCII space; test `old_uyghur_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D102 | HTML `clean_html`: Psalter Pahlavi U+10B99–U+10B9C + Sogdian U+10F55–U+10F59 as word separators | All Po. `collapse_whitespace()` maps to ASCII space; test `psalter_pahlavi_and_sogdian_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D101 | HTML `clean_html`: Kharoshthi U+10A50–U+10A58 + Avestan U+10B39–U+10B3F as word separators | All Po. `collapse_whitespace()` maps to ASCII space; test `kharoshthi_and_avestan_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D100 | HTML `clean_html`: Cuneiform punctuation U+12470–U+12474 + Cypro-Minoan U+12FF1–U+12FF2 as word separators | All Po. `collapse_whitespace()` maps to ASCII space; test `cuneiform_punctuation_and_cypro_minoan_po_signs_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D99 | HTML `clean_html`: Kawi U+11F43–U+11F4F + Tamil Supplement U+11FFF (END OF TEXT) as word separators | All Po. `collapse_whitespace()` maps to ASCII space; test `kawi_sentence_punctuation_and_tamil_end_of_text_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D98 | HTML `clean_html`: Zanabazar Square U+11A3F–U+11A46 + Soyombo U+11A9A–U+11A9C / U+11A9E–U+11AA2 as word separators | All Po. U+11A9D MARK PLUTA (Lo) omitted. `collapse_whitespace()` maps to ASCII space; test `zanabazar_square_and_soyombo_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D97 | HTML `clean_html`: Marchen U+11C70–U+11C71 + Makasar U+11EF7–U+11EF8 as word separators | All Po. `collapse_whitespace()` maps to ASCII space; test `marchen_and_makasar_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D96 | HTML `clean_html`: Ahom U+1173C–U+1173E + Dogra U+1183B + Dives Akuru U+11944–U+11946 + Nandinagari U+119E2 + Bhaiksuki U+11C41–U+11C45 as word separators | All Po. `collapse_whitespace()` maps to ASCII space; test `ahom_dogra_dives_akuru_nandinagari_bhaiksuki_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D95 | HTML `clean_html`: Mahajani U+11174–U+11175 + Multani U+112A9 + Tulu-Tigalari U+113D4–U+113D5 U+113D7–U+113D8 + Newa U+1144B–U+1144F U+1145A U+1145B U+1145D + Tirhuta U+114C6 + Modi U+11641–U+11643 as word separators | All Po; Mahajani nukta (Mn), Tirhuta OM (Lo), Khudawadi virama/nukta (Mn), unassigned U+113D6 / U+1145C omitted. `collapse_whitespace()` maps to ASCII space; test `mahajani_multani_tulu_tigalari_newa_tirhuta_modi_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D94 | HTML `clean_html`: Chakma U+11140–U+11143 + Sharada U+111C5–U+111C8 U+111CD + Khojki U+11238–U+1123D as word separators | Chakma section mark through question mark (Po); Sharada danda / double danda / abbreviation / separator / sutra mark (Po); Khojki danda through abbreviation sign (Po). U+11144–U+11147 (Chakma Lo/Mc) omitted. `collapse_whitespace()` maps to ASCII space; test `chakma_sharada_and_khojki_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D93 | HTML `clean_html`: Brahmi U+11047–U+1104D + Kaithi U+110BB U+110BC U+110BE–U+110C1 as word separators | Brahmi danda through punctuation lotus (Po); Kaithi abbreviation / enumeration / section marks and dandas (Po). U+110BD / U+110CD (Cf) remain on the existing Kaithi number-sign arm. `collapse_whitespace()` maps to ASCII space; test `brahmi_danda_through_lotus_and_kaithi_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D92 | HTML `clean_html`: Adlam U+1E95E–U+1E95F + Medefaidrin U+16E97 U+16E98 U+16E9A as word separators | Adlam initial exclamation / question (Po); Medefaidrin comma / full stop / exclamation oh (Po). (U+16E99 SYMBOL AIVA was later added as `Po` in FEAT-D204—`U+16E97..=U+16E9A`.) `collapse_whitespace()` maps to ASCII space; test `adlam_initial_punctuation_and_medefaidrin_sentence_marks_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D91 | HTML `clean_html`: Wancho U+1E2FE–U+1E2FF + Nyiakeng Puachue Hmong U+16FE2–U+16FE3 as word separators | Nyiakeng exclamation / question (Po). **Superseded for Wancho:** UnicodeData assigns digits `Nd` U+1E2F0–U+1E2F9, unassigned `Cn` U+1E2FA–U+1E2FE, **WANCHO NGUN SIGN** `Sc` U+1E2FF; FEAT-D214 maps `U+1E2FF` only (historical “comma / full stop Po” pair was wrong). U+16FE0–U+16FE1 (Lo/Lm) and U+16FE4 (Lo) omitted. `collapse_whitespace()` maps Ngün + Nyiakeng marks; tests `wancho_comma_full_stop_and_nyiakeng_sentence_punctuation_separate_words`, `wancho_digit_nd_and_unassigned_gap_before_ngun_stay_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D90 | HTML `clean_html`: NKo U+07F7–U+07F9 + Samaritan U+0830–U+083E as word separators | NKo GBAKURUNEN symbol, comma, exclamation (Po); Samaritan punctuation marks (Po). U+07FA LAJANYALAN (Lm) omitted; U+083F unassigned. `collapse_whitespace()` maps to ASCII space; test `nko_sentence_punctuation_and_samaritan_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D89 | HTML `clean_html`: Syriac U+0700 (Bk) + U+0701–U+070D (Po) as word separators | End of paragraph + supralinear/sublinear stops, colons, dots, Harklean marks; not Rust whitespace. U+070E unassigned (not in range); U+070F Cf remains with Arabic edition arm. `collapse_whitespace()` maps to ASCII space; test `syriac_paragraph_end_and_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D88 | HTML `clean_html`: Canadian U+166E + Bamum U+A6F2–U+A6F7 + Mro U+16A6E–U+16A6F + New Tai Lue U+19DE–U+19DF as word separators | Syllabics full stop (Po); Bamum sentence marks (Po); Mro danda / double danda (Po); New Tai Lue lae / laev (So). U+166D CHI SIGN (So) omitted. `collapse_whitespace()` maps to ASCII space; test `canadian_syllabics_full_stop_bamum_mro_and_new_tai_lue_signs_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D87 | HTML `clean_html`: Lisu U+A4FE–U+A4FF + Vai U+A60C–U+A60F as word separators | Lisu punctuation comma / full stop + Vai comma / full stop / question / exclamation (Po); not Rust whitespace. `collapse_whitespace()` maps to ASCII space; test `lisu_comma_full_stop_and_vai_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D86 | HTML `clean_html`: Kayah Li U+A92E–U+A92F + Rejang U+A95F as word separators | Sign cwi / sign shya + section mark (Po); not Rust whitespace. `collapse_whitespace()` maps to ASCII space; test `kayah_li_cwi_shya_and_rejang_section_mark_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D85 | HTML `clean_html`: Buginese U+1A1E–U+1A1F + Tai Tham U+1AA0–U+1AA6 / U+1AA8–U+1AAD as word separators | Pallawa + end of section (Po); Tai Tham sentence/section signs (Po). U+1AA7 MAI YAMOK (Lm) omitted (word-internal risk, like Thai U+0E46). `collapse_whitespace()` maps to ASCII space; test `buginese_pallawa_end_section_and_tai_tham_signs_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D84 | HTML `clean_html`: Sundanese Supplement U+1CC0–U+1CC7 as word separators | Bindu surya through bindu pameneng (Po); not Rust whitespace. `collapse_whitespace()` maps to ASCII space; test `sundanese_supplement_bindu_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D83 | HTML `clean_html`: Philippine U+1734–U+1736 as word separators | Historical row text assumed three `Po` scalars. **Superseded for U+1734:** Unicode assigns **HANUNOO SIGN PAMUDPOD** as `Mn`; **FEAT-D253** removed it from the arm. **PHILIPPINE SINGLE / DOUBLE PUNCTUATION** U+1735–U+1736 (`Po`) remain word separators. Test `philippine_single_double_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D82 | HTML `clean_html`: Tai Viet U+AADE–U+AADF as word separators | SYMBOL HO HOI and SYMBOL KOI KOI (Po); not Rust whitespace. U+AADB–U+AADD (Lo/Lm) omitted. `collapse_whitespace()` maps to ASCII space; test `tai_viet_ho_hoi_koi_koi_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D81 | HTML `clean_html`: Cham U+AA5C–U+AA5F as word separators | Spiral and danda / double / triple danda (Po); not Rust whitespace. `collapse_whitespace()` maps to ASCII space; test `cham_spiral_danda_through_triple_danda_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D80 | HTML `clean_html`: Batak U+1BFC–U+1BFF, Lepcha U+1C3B–U+1C3F, Ol Chiki U+1C7E–U+1C7F as word separators | Bindu symbols, Lepcha ta-rol through tshook, Ol Chiki mucaad (all Po); not Rust whitespace. `collapse_whitespace()` maps to ASCII space; test `batak_bindu_lepcha_punctuation_ol_chiki_mucaad_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D79 | HTML `clean_html`: Balinese U+1B5A–U+1B60 as word separators | Carik siki through pamengkeb (Po); not Rust whitespace. `collapse_whitespace()` maps to ASCII space; test `balinese_carik_through_pamengkeb_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D78 | HTML `clean_html`: Khmer U+17D4–U+17DA + Javanese pada U+A9C1–U+A9CF as word separators | Khan through koomuut (Po); Javanese rerenggan / pada / section (Po). U+A9C0 PANGKON (Cf) omitted. `collapse_whitespace()` maps to ASCII space; test `khmer_sentence_signs_and_javanese_pada_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D77 | HTML `clean_html`: Sinhala U+0DF4, Limbu U+1940 U+1944 U+1945, Meetei Mayek U+AAF0 U+AAF1 as word separators | Kunddaliya; tokma + exclamation + question; cheikhei + ahang khuda—all Po, not Rust whitespace. `collapse_whitespace()` maps to ASCII space; test `sinhala_limbu_meetei_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D76 | HTML `clean_html`: Tibetan U+0F04–U+0F12, U+0F14, U+0F3A–U+0F3D, U+0F85, U+0FD0–U+0FD4, U+0FD9–U+0FDA as word separators | Yig mgo / tsheg / shad stack, gter tsheg, corner brackets (Ps/Pe), paluta, astrological marks—all non–Rust-whitespace Po/Ps/Pe. U+0F13 (caret, So) omitted. `collapse_whitespace()` maps to ASCII space; test `tibetan_shad_head_marks_brackets_astro_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D75 | HTML `clean_html`: Thai U+0E2F U+0E4F U+0E5A U+0E5B, Lao U+0EAF, Myanmar U+104A–U+104B as word separators | All Po; not Rust whitespace. Southeast Asian–Latin bilingual or Unicode-sample HTML can glue Latin tokens without ASCII space. Thai MAIYAMOK U+0E46 (Lm) omitted (word-internal risk). `collapse_whitespace()` maps to ASCII space; test `thai_lao_myanmar_sentence_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D74 | HTML `clean_html`: Devanagari danda U+0964 + double danda U+0965 as word separators | Both Po; not Rust whitespace. Mixed Latin–Devanagari or Unicode-sample HTML can glue tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `devanagari_danda_double_danda_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D73 | HTML `clean_html`: Hebrew sof pasuq U+05C3 + Georgian paragraph separator U+10FB as word separators | Both Po; not Rust whitespace. Mixed Latin–Hebrew / Latin–Georgian or Unicode-sample HTML can glue tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `hebrew_sof_pasuq_georgian_paragraph_separator_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D72 | HTML `clean_html`: Armenian script punctuation U+055C–U+055F and U+0589–U+058A as word separators | Exclamation / comma / question / abbreviation (Po) and full stop / hyphen (Po/Pd); not Rust whitespace. U+055A apostrophe and U+055B emphasis mark omitted (word-internal risk, like U+2019). `collapse_whitespace()` maps to ASCII space; test `armenian_script_punctuation_separates_words` in `commands/html_cleaning.rs`. |
| FEAT-D71 | HTML `clean_html`: Latin-1 section sign U+00A7 and pilcrow U+00B6 as word separators | Both Po; not Rust whitespace. Legal or editorial HTML can place them between Latin tokens without ASCII space. `collapse_whitespace()` maps to ASCII space; test `latin1_section_sign_and_pilcrow_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D70 | HTML `clean_html`: CJK Symbols U+3003 U+3004 U+3006, brackets + postal/geta U+3008–U+301B, wave dash U+301C, double-prime quotes U+301D–U+301F, postal face U+3020, vertical kana repeat U+3031–U+3036, masu U+303C, variation indicator + half fill U+303E U+303F as word separators | Skips iteration marks U+3005 / U+303B, ideographic zero U+3007, Hangzhou numerals U+3021–U+3029 / U+3038–U+303A, tone marks U+302A–U+302F. `collapse_whitespace()` maps to ASCII space; test `cjk_symbols_brackets_ditto_wave_vertical_repeat_masu_half_fill_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D69 | HTML `clean_html`: Latin-1 inverted !/? + guillemets U+00A1 U+00BF U+00AB U+00BB, Greek question mark U+037E, Arabic sentence + numeric punctuation U+060C U+061B U+061F U+06D4 U+066A–U+066C as word separators | All Po / Pi / Pf; none are Rust whitespace. U+037E is distinct from ano teleia U+0387 (middle-dot arm). Arabic comma/semicolon/question/full stop and percent/decimal/thousands separators sit beside edition format Cf chars U+0600–U+0605 without overlapping them. `collapse_whitespace()` maps to ASCII space; test `latin1_greek_and_arabic_script_punctuation_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D68 | HTML `clean_html`: Small Form Variants U+FE50–U+FE52, U+FE54–U+FE66, U+FE68–U+FE6B + CJK compat U+3030 / U+3037 / U+303D / U+30A0 / U+FF3F as word separators | Small forms: Po/Pd/Ps/Pe/Sm/Sc as assigned; skips unassigned U+FE53, U+FE67, U+FE6C–U+FE6F. CJK: wavy dash, ideographic telegraph line-feed sep. symbol, part alternation mark, Katakana-Hiragana double hyphen, fullwidth low line—none are Rust whitespace. `collapse_whitespace()` maps them to ASCII space; tests `small_form_variants_fe50_fe52_fe54_fe66_fe68_fe6b_separate_words`, `cjk_compat_wavy_dash_telegraph_sep_pam_double_hyphen_fullwidth_low_line_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D67 | HTML `clean_html`: General Punctuation U+2020–U+2023 (dagger, double dagger, bullet, triangular bullet) as word separators | All Po; none are Rust whitespace. They sit between the U+201A–U+201F arm and U+2024–U+2027 (U+2019 apostrophe remains unmapped). `collapse_whitespace()` maps `U+2020..=U+2023` to ASCII space (subsumes prior standalone U+2022); tests `general_punctuation_u2020_through_u2023_separate_words`, `bullet_and_dot_operators_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D66 | HTML `clean_html`: General Punctuation U+2016–U+2018 and U+201A–U+201F (double vertical line, double low line, curly single/double quotes except U+2019) as word separators | Pi / Pf / Ps / Po; none are Rust whitespace. U+2019 omitted: typographic apostrophe inside words (`don't`). `collapse_whitespace()` maps `U+2016..=U+2018` and `U+201A..=U+201F` to ASCII space; tests `general_punctuation_u2016_u2018_and_u201a_through_u201f_separate_words`, `typographic_apostrophe_u2019_does_not_split_contractions` in `commands/html_cleaning.rs`. |
| FEAT-D65 | HTML `clean_html`: General Punctuation U+203C–U+205E (double exclamation through vertical four dots; includes U+2044 fraction slash) as word separators | Po / Pc / Ps / Pe / Sm; none are Rust whitespace (U+205F is Rust whitespace, so range stops before U+2061). Interrobang, undertie, Tironian et, four-dot punctuation, etc. in pasted HTML can glue Latin tokens. `collapse_whitespace()` maps `U+203C..=U+205E` to ASCII space (subsumes prior explicit U+2044 arm); test `general_punctuation_u203c_through_u205e_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D64 | HTML `clean_html`: General Punctuation U+2030–U+203B (per mille, per ten thousand, prime / double / triple / reversed primes, caret, single guillemets, reference mark) as word separators | Po / Pi / Pf; none are Rust whitespace. Measurement, foot/inch-style, or European-typography HTML can place them between Latin tokens without ASCII space. `collapse_whitespace()` maps `U+2030..=U+203B` to ASCII space; test `general_punctuation_u2030_through_u203b_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D63 | HTML `clean_html`: Unicode dash punctuation U+2010–U+2015 (hyphen, NB hyphen, figure dash, en dash, em dash, horizontal bar) as word separators | All Pd; none are Rust whitespace. Typographic or pasted Office-style HTML can place them between Latin tokens without ASCII space. `collapse_whitespace()` maps `U+2010..=U+2015` to ASCII space; test `unicode_dash_punctuation_u2010_through_u2015_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D62 | HTML `clean_html`: ideographic comma/stop U+3001–U+3002, fullwidth `,;:!?` U+FF0C/FF1A/FF1B/FF01/FF1F, Vertical Forms U+FE10–U+FE19, Hebrew maqaf U+05BE + paseq U+05C0, Tibetan tsheg U+0F0B, Ethiopic full stop U+1362 as word separators | All Po/Pd/Ps/Pe/Pc (assigned in FE10–FE19); none are Rust whitespace. CJK, vertical compatibility, RTL Hebrew, Tibetan, or Ethiopic HTML can glue Latin tokens without ASCII space. `collapse_whitespace()` maps them to ASCII space; tests `cjk_fullwidth_and_vertical_forms_punctuation_separate_words`, `hebrew_maqaf_paseq_tibetan_tsheg_ethiopic_stop_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D61 | HTML `clean_html`: fraction slash U+2044, division slash U+2215, fullwidth solidus U+FF0F as word separators | U+2044 / U+2215 are Sm; U+FF0F is Po; none are Rust whitespace. Math fractions, MathML, or CJK fullwidth typography can sit between Latin tokens without ASCII space. `collapse_whitespace()` maps all three to ASCII space; test `fraction_division_and_fullwidth_solidus_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D60 | HTML `clean_html`: Supplemental Punctuation full assigned block U+2E00–U+2E5D as word separators | Prior arms covered only U+2E31–U+2E3B; U+2E00–U+2E30 (editorial/transcription marks, brackets) and U+2E3C–U+2E5D (stenographic full stop through oblique hyphen) were gaps. **Superseded for U+2E2F:** Unicode assigns **VERTICAL TILDE** as `Lm`; **FEAT-D255** splits the arm to `U+2E00..=U+2E2E` + `U+2E30..=U+2E5D`. Tests `supplemental_punctuation_u2e00_through_u2e5d_separate_words`, `supplemental_punctuation_vertical_tilde_lm_u2e2f_stays_unmapped` in `commands/html_cleaning.rs`. |
| FEAT-D59 | HTML `clean_html`: Supplemental Punctuation U+2E31–U+2E3B + Runic U+16EB–U+16ED as word separators | Stepped in before FEAT-D60: U+2E31–U+2E3B plus runic Po. Superseded for supplemental subrange by FEAT-D60 (`U+2E00..=U+2E5D`). `runic_word_punctuation_separates_words` remains in `commands/html_cleaning.rs`. |
| FEAT-D58 | HTML `clean_html`: Supplemental Punctuation U+2E31–U+2E33 + Aegean U+10100–U+10101 + Phoenician U+1091F as word separators | All are Po and not Rust whitespace. U+2E31–U+2E33 now live under FEAT-D60 `U+2E00..=U+2E5D`; Aegean/Phoenician remain explicit in the match. `aegean_and_phoenician_word_separator_marks_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D57 | HTML `clean_html`: one dot leader U+2024, two dot leader U+2025, horizontal ellipsis U+2026, hyphenation point U+2027 as word separators | All are Po and not Rust whitespace. TOC-style leaders or UI copy (e.g. "more…") can glue Latin tokens without ASCII space. `collapse_whitespace()` maps `U+2024..=U+2027` to ASCII space; test `dot_leaders_ellipsis_and_hyphenation_point_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D56 | HTML `clean_html`: bullet U+2022, bullet operator U+2219, dot operator U+22C5 as word separators | U+2022 is Po; U+2219 and U+22C5 are Sm; none are Rust whitespace. List- or MathML-style HTML can place them between Latin tokens without ASCII space. `collapse_whitespace()` maps them to ASCII space; test `bullet_and_dot_operators_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D55 | HTML `clean_html`: middle dot U+00B7, Greek ano teleia U+0387, Katakana middle dot U+30FB / U+FF65 as word separators | All are Po and not Rust whitespace; typography and mixed-script HTML can use them between Latin tokens without ASCII space. `collapse_whitespace()` maps them to ASCII space; test `middle_dot_punctuation_separates_words` in `commands/html_cleaning.rs`. |
| FEAT-D54 | HTML `clean_html`: Egyptian hieroglyph extended format U+13439–U+13455 as word separators | Unicode extended the hieroglyph format block past U+13438 (quadrat / area / insert controls, all Cf); they are not Rust whitespace. `collapse_whitespace()` maps `U+13430..=U+13455` to ASCII space; `egyptian_hieroglyph_format_controls_separate_words` loops the full range in `commands/html_cleaning.rs`. |
| FEAT-D53 | HTML `clean_html`: Brahmi U+1107F + Ideographic Description Characters U+2FF0–U+2FFB as word separators | Brahmi number joiner (Mn) and IDC block (So) are not Rust whitespace; Indic numeral or Han-component notation HTML can glue Latin tokens. `collapse_whitespace()` maps them to ASCII space; tests `brahmi_number_joiner_separates_words`, `ideographic_description_characters_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D52 | HTML `clean_html`: Duployan U+1BC9D–U+1BC9E + Kaithi U+110BD / U+110CD as word separators | Thick letter selector / double mark (Mn) sit before U+1BCA0–U+1BCA3 shorthand format; Kaithi number sign and number sign above (Cf) are not Rust whitespace. `collapse_whitespace()` maps them to ASCII space; tests `duployan_selectors_and_shorthand_format_separate_words` (also covers U+1BCA0–U+1BCA3; skips visible U+1BC9F Po), `kaithi_number_format_signs_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D51 | HTML `clean_html`: Egyptian hieroglyph format U+13430–U+13438 + musical symbol format U+1D173–U+1D17A as word separators | Cf joiners/segment markers and begin/end beam–tie–slur–phrase controls are not Rust whitespace; scholarly or MusicXML-style HTML could glue Latin tokens. `collapse_whitespace()` maps both ranges to ASCII space; tests `egyptian_hieroglyph_format_controls_separate_words`, `musical_symbol_format_controls_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D50 | HTML `clean_html`: Mongolian U+1800–U+1805 / U+1807–U+1809 (sentence punctuation) as word separators | Po/Pd marks between Latin tokens were not Rust whitespace (only U+1806 / U+180A / U+180B–U+180E were mapped before). `collapse_whitespace()` now uses `U+1800..=U+180E` (one arm). Tests: `mongolian_punctuation_separates_words`; `invisible_fillers_separate_words` covers U+180B–U+180E + Hangul fillers only. |
| FEAT-D49 | HTML `clean_html`: Arabic Extended-A U+0890–U+0891 (pound / piastre mark above) as word separators | Both are Cf and not Rust whitespace; they were the remaining BMP format controls in that script range alongside U+0600–U+0605 / U+06DD / U+08E2. `collapse_whitespace()` maps them to ASCII space; test `arabic_extended_a_currency_format_marks_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D48 | HTML `clean_html`: Ethiopic wordspace (U+1361), Braille blank (U+2800), Duployan shorthand format (U+1BCA0–U+1BCA3) as word separators | U+1361 is Po, U+2800 is So, U+1BCA0–3 are Cf; none are Rust whitespace, so Ethiopic/Braille/Duployan HTML or pasted text could glue Latin tokens. `collapse_whitespace()` maps them to ASCII space; Ethiopic primary punctuation extended to `U+1360..=U+1368` in FEAT-D206 (`ethiopic_section_wordspace_and_sentence_punctuation_u1360_through_u1368_separate_words`). Tests `braille_patterns_separate_words` (full block U+2800–U+28FF since FEAT-D136), `duployan_selectors_and_shorthand_format_separate_words` (U+1BCA0–3; FEAT-D52 extends with U+1BC9D–U+1BC9E). |
| FEAT-D47 | HTML `clean_html`: `<img>` `alt` / `title` as `[Image: …]` text | Previously images were dropped silently (022 HTML-cleaning observation). `walk_node` emits inline `[Image: description]` when `alt` (trimmed) is non-empty, else `title`; no placeholder when both are empty. Tests: `img_alt_emitted_as_readable_text`, `img_title_used_when_alt_empty`, `img_alt_preferred_over_title`, `img_without_alt_or_title_emits_nothing` in `commands/html_cleaning.rs`. |
| FEAT-D46 | HTML `clean_html`: Mongolian U+1806 (TODO SOFT HYPHEN) + U+180A (NIRUGU) as word separators | Both are Cf and not Rust whitespace; they sit outside the existing U+180B–U+180E arm (FVS / vowel separator). `collapse_whitespace()` maps them to ASCII space; `invisible_fillers_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D45 | HTML `clean_html`: Arabic / Syriac koranic & edition format chars (U+0600–U+0605, U+06DD, U+070F, U+08E2) as word separators | Arabic number sign / safha / ayah frames and Syriac abbreviation mark (Cf) are not Rust whitespace, so RTL scholarly HTML could glue Latin tokens. `collapse_whitespace()` maps them to ASCII space; `arabic_and_syriac_edition_format_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D44 | HTML `clean_html`: Khmer inherent vowel format chars (U+17B4, U+17B5) as word separators | Khmer vowel inherent AQ/AA (Cf) are not Rust whitespace, so `hello\u{17B4}world` stayed one token for `split_whitespace()`. `collapse_whitespace()` maps both to ASCII space with other format controls; `khmer_inherent_vowel_format_separates_words` in `commands/html_cleaning.rs`. |
| FEAT-D43 | HTML `clean_html`: Unicode Tags block (U+E0000–U+E007F) as word separators | Deprecated language-tag / tag-id characters (Cf) are not Rust whitespace, so `hello\u{E0020}world` stayed one token for `split_whitespace()`. `collapse_whitespace()` maps `U+E0000..=U+E007F` to ASCII space; `unicode_language_tag_characters_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D42 | HTML `clean_html`: variation selectors (U+FE00–U+FE0F, U+E0100–U+E01EF) + Mongolian free variation selectors (U+180B–U+180D) as word separators | `collapse_whitespace()` maps VS / VS supplement and U+180B–U+180D to ASCII space (U+180E Mongolian vowel separator folded into `U+180B..=U+180E`); Rust `split_whitespace()` does not treat them as whitespace, so emoji- or IVS-heavy FETCH_URL text could glue Latin tokens. `variation_selectors_separate_words` and extended `invisible_fillers_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D41 | HTML `clean_html`: U+202A–U+202E bidi embedding / override / pop as word separators | `collapse_whitespace()` maps U+202A–U+202E (LRE/RLE/PDF/LRO/RLO) to ASCII space with existing bidi/Cf controls; Rust `split_whitespace()` does not treat them as whitespace, so copied RTL/LTR HTML could glue tokens. `bidi_embedding_and_override_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D40 | HTML `clean_html`: U+2061 function application + interlinear / object replacement (U+FFF9–U+FFFC) as word separators | `collapse_whitespace()` maps U+2061 (MathML invisible function application; previously outside the U+2062–U+206F arm) via `U+2061..=U+206F`, and U+FFF9–U+FFFC (interlinear annotation anchors + object replacement) to ASCII space with existing controls. `invisible_math_and_bidi_format_separate_words` now covers U+2061..=U+206F; `interlinear_annotation_and_object_replacement_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D39 | HTML `clean_html`: Mongolian vowel separator + Hangul / halfwidth fillers as word separators | `collapse_whitespace()` maps U+180E, U+115F, U+1160, U+3164, U+FFA0 to ASCII space with existing ZWSP/bidi/math controls; Rust `split_whitespace()` does not treat these as whitespace, so copied or CJK-layout HTML could glue Latin tokens. `invisible_fillers_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D38 | HTML `clean_html`: U+2062–U+206F invisible math + bidi/shaping as word separators | `collapse_whitespace()` maps U+2062–U+206F (invisible times/separator/plus, isolates 2066–9, symmetric-swapping / Arabic shaping / digit-shape Cf chars) to ASCII space with existing controls so MathML- or RTL-heavy FETCH_URL bodies do not glue tokens. Range arm subsumes prior explicit U+2066–U+2069 match arms. `invisible_math_and_bidi_format_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D37 | HTML `clean_html`: CGJ / ALM / LRM / RLM / directional isolates as word separators | `collapse_whitespace()` maps U+034F, U+061C, U+200E, U+200F, U+2066–U+2069 to ASCII space with existing ZWSP/ZWNJ/ZWJ/WJ/BOM/SHY/NBSP so bidi and grapheme-joiner controls from copied HTML do not glue tokens (`split_whitespace` does not treat them as whitespace). `bidi_and_grapheme_joiner_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D36 | HTML `clean_html`: ZWNJ / ZWJ / word joiner as word separators | `collapse_whitespace()` maps U+200C, U+200D, U+2060 to ASCII space with ZWSP/SHY/BOM/NBSP so copied HTML / emoji-adjacent text does not glue tokens (`split_whitespace` does not treat them as whitespace). `zero_width_joiners_separate_words` in `commands/html_cleaning.rs`. |
| FEAT-D35 | HTML `clean_html`: NBSP (U+00A0) as word separator | `collapse_whitespace()` maps no-break space to ASCII space with ZWSP/SHY/BOM so `&nbsp;` does not glue tokens (`split_whitespace` does not treat U+00A0 as whitespace). `nbsp_separates_words` in `commands/html_cleaning.rs`. Closes 022 §8 HTML-cleaning observation (NBSP). |
| FEAT-D34 | HTML `clean_html`: soft hyphen (U+00AD) as word separator | `collapse_whitespace()` maps SHY to ASCII space with ZWSP/BOM so `&shy;`-style breaks do not glue tokens (`split_whitespace` does not treat U+00AD as whitespace). `soft_hyphen_separates_words` in `commands/html_cleaning.rs`. |
| FEAT-D33 | HTML `clean_html`: ZWSP / BOM-in-text as word separators | `collapse_whitespace()` maps U+200B and U+FEFF to ASCII space before `split_whitespace()` so FETCH_URL body text does not glue words (Rust does not classify those as whitespace). `zero_width_space_separates_words` in `commands/html_cleaning.rs`. Follow-up to 022 HTML-cleaning review (NBSP is already whitespace; ZWSP was the sticky case). |
| FEAT-D32 | FETCH_URL oversized body: ellipse + explicit suffix (022 §F7) | `truncate_fetch_body_ellipse_then_explicit_suffix_for_llm` in `commands/browser.rs` asserts ellipsed payload contains `...` and output ends with ` [content truncated]` within `MAX_BODY_CHARS`. Closes the remaining automated slice of the F7 FETCH_URL truncation checklist row in `022_feature_review_plan.md`. No runtime behaviour change. |
| FEAT-D31 | `toggle_cpu_window` from CPU window chat (022 §F8) | Rustdoc on `commands/window.rs` and `ui/status_bar.rs` states that `--cpu` from inside the CPU window uses the same close-then-recreate path as the menu bar (WebView torn down, fresh window)—intentional UX, not in-place hide. `022_feature_review_plan.md` F8 checklist row closed. No runtime behaviour change. |
| FEAT-D30 | `toggle_cpu_window` + `run_on_main_thread` threading (022 §F9) | Rustdoc on `commands/window.rs` documents Tauri 1 behaviour: command runs off main thread, `run_on_main_thread` blocks the command thread until the short main-thread close/create finishes; UI loop unchanged. `022_feature_review_plan.md` F9 checklist row closed. No code change. |
| FEAT-D29 | `toggle_cpu_window` rustdoc ↔ 022 §F9 behaviour | Doc comments on `commands/window.rs` and `ui/status_bar.rs` now state that any existing `cpu` window is closed then recreated so the call always ends with a visible window—not a strict open/close toggle. No code change. |
| FEAT-D28 | Context overflow → user sanitize (022 §F2) | Three new unit tests in `content_reduction.rs` for `sanitize_ollama_error_for_user` (`maximum context length … tokens`, `context length exceeded`, `exceeds the model's context window`) so common Ollama overflow strings stay tied to the same “new topic / larger context” guidance as `is_context_overflow_error`. No runtime behaviour change. |
| FEAT-D27 | `load_agents` always logs shared soul presence (022 §F3) | `log_shared_soul_presence()` + `shared_soul_file_nonempty()` in `agents/mod.rs`. Shared `soul.md` status is logged when the agents directory is missing, when no agents are enabled, and when agents load (previously only the non-empty load path). Three mutex-serialized tests override `HOME` to assert nonempty / whitespace-only / missing file. Operator-visible log change only. |
| FEAT-D26 | `run_due_monitor_checks` wiring contract + 022 checklist triage | Unit test `lib_rs_invokes_run_due_monitor_checks_in_background_loop` in `commands/monitors.rs` asserts `lib.rs` still calls `run_due_monitor_checks()` from the monitor thread. `docs/022_feature_review_plan.md` §3–§4: checklist items marked complete where covered by existing tests/docs (F1/F2/F3/F4/F6/F7/F8/F10); manual-only rows left open. No runtime behaviour change. |
| FEAT-D25 | `prepare_conversation_history` tests: const assert vs clippy | `const _: () = assert!(COMPACTION_THRESHOLD > 2)` in `session_history` test module documents that below-threshold cases use 1–2 turns; removes two runtime `assert!` that triggered `clippy::assertions_on_constants`. No runtime behaviour change. |
| FEAT-D24 | `prepare_conversation_history` below-compaction tests | Two `#[tokio::test]` cases in `session_history.rs`: `is_new_topic` with history shorter than `COMPACTION_THRESHOLD` yields empty prior; non–new-topic path appends the Discord/FETCH_URL 401 system correction to confused assistant text (022 §F1 new-topic clearing + `annotate_discord_401` wiring). No runtime behaviour change. |
| FEAT-D23 | `cap_tail_chronological` ↔ `CONVERSATION_HISTORY_CAP` + `ellipse` max_len 2 | `session_history` cap-tail test uses `CONVERSATION_HISTORY_CAP` and `total = cap + 5` so the “last N in order” contract tracks the shared router/Discord cap (022 §F1). `ellipse_max_len_two_clamped` in `logging/mod.rs` documents F7: `max_len` below `sep_len + 1` clamps to `sep_len + 1`. No runtime behaviour change. |
| FEAT-D22 | `shorten_file_path_internal` unit tests | Five tests in `logging/legacy.rs`: strip `src-tauri/src/` and `src/` prefixes; last `src/` fallback; unchanged when no match; empty string. Documents 022 §F7 logging path display for `debug!` macros. No runtime behaviour change. |
| FEAT-D21 | `looks_like_discord_401_confusion` unit tests | Four tests in `reply_helpers.rs`: true when 401/unauthorized + token/credential/auth + discord/guild/channel; false when generic 401+token (no Discord context) or Discord text without auth signal. Documents the gate for `annotate_discord_401` in `session_history::prepare_conversation_history`. No runtime behaviour change. |
| FEAT-D20 | `session_history` cap ordering: const assert + clippy | `const _: () = assert!(HAVING_FUN_IDLE_HISTORY_CAP < CONVERSATION_HISTORY_CAP)` replaces runtime `assert!` in `conversation_history_caps_match_discord_contract` (clippy `assertions_on_constants`). Test still asserts numeric caps (20 / 10). No runtime behaviour change. |
| FEAT-D19 | Discord having_fun history caps ↔ `session_history` | `HAVING_FUN_IDLE_HISTORY_CAP` (10); Discord `having_fun` reply path uses `CONVERSATION_HISTORY_CAP` instead of literal `20` so router and Discord share one constant (022 §F1). Idle thought path uses the new constant. One contract test. No behaviour change. |
| FEAT-D18 | `is_monitor_due_for_background` + F10 due-logic tests | Pure helper extracted from `run_due_monitor_checks` filter; five tests (never checked; elapsed below vs equals interval; missing config → 60s default; stored `0` clamps to 1 so same-second not due). Background caller remains `lib.rs` (30s loop). No runtime change. |
| FEAT-D17 | `extract_first_url` unit tests (FETCH_URL arg parsing) | Five tests in `commands/browser.rs`: no scheme / empty; trim + trailing `.,;:` strip (022 / AGENTS FETCH_URL note); first token only; `https://` sought before `http://` so a later https URL beats an earlier http token; `http://` with port. No runtime change. |
| FEAT-D16 | Chat reserved words: no user bubble (022 §F8) | `sendChatMessage()` in `src/ollama.js` clears input then runs `--cpu` / `-v*` handling before `addChatMessage('user')` / `addToHistory`, so meta-commands are not shown as user turns (history was already skipped). Synced via `scripts/sync-dist.sh`. |
| FEAT-D15 | Agent soul vs shared soul helper + F3 tests | `agent_soul_or_shared()` in `agents/mod.rs` centralizes per-agent `soul.md` vs `Config::load_soul_content()` fallback; three unit tests lock 022 §F3 (non-empty per-agent wins; empty/missing → shared; both empty → None). `load_one_agent` unchanged behaviour; debug logs preserved. |
| FEAT-D14 | `MAC_STATS_SESSION_DIR` + session disk-resume tests | `Config::session_dir()` honors optional env (like `MAC_STATS_TASK_DIR`). Mutex-serialized tests call `load_messages_from_latest_session_file` against a temp dir: new layout, legacy `session-memory-{topic}-{id}-{ts}.md`, and newest `modified()` when two files match the id. Locks 022 §3 F1 “resume from disk” without writing under `~/.mac-stats/session`. |
| FEAT-D13 | Router soul prefix helper + F4 tests | `format_router_soul_block()` in `ollama_memory.rs` builds the planning-system soul + `You are mac-stats v…` prefix; `answer_with_ollama_and_fetch` calls it only when `skill_content` is `None` (agent `combined_prompt` still uses `skill_content` Some → empty soul). Three unit tests lock `docs/022_feature_review_plan.md` §F4 (no double voice when skill/agent prompt active; empty vs non-empty soul formatting). No API or runtime behaviour change. |
| FEAT-D12 | Execution message stack helper + F2 tests | `build_execution_message_stack()` in `session_history.rs` assembles system → capped history → current user (same order as both agent-router execution paths in `ollama.rs`); two unit tests lock `docs/022_feature_review_plan.md` §F2 ordering. No API or runtime behaviour change. |
| FEAT-E1 | Persist monitor stats after `check_monitor` | Each successful check updates `monitors.json` via `save_monitors()` so `last_check` / `last_status` survive restart and match background-thread behaviour (022 §3 F10). Inner block drops `get_monitors()` lock before save to avoid deadlock with `save_monitors`’ lock order; `save_monitors` errors ignored (busy locks). |
| FEAT-D11 | Chat verbosity command ↔ `VERBOSITY` atomic | Unit tests in `commands/logging.rs`: `set_chat_verbosity` updates the same `legacy::VERBOSITY` used by `ellipse()` / Ollama log truncation; values above 3 clamp. Mutex-serialized; restores prior level. Locks 022 §F8 checklist (reserved words path matches CLI atomic). No runtime behaviour change. |
| FEAT-D10 | TASK prompt F6 contract tests | `format_task_agent_description()` extracts the **TASK** paragraph from `build_agent_descriptions`; unit tests lock 022 §F6 strings (orchestrator for agent chats, `TASK_APPEND`/`TASK_STATUS` when topic+id exists). No runtime behaviour change. |
| FEAT-D9 | TASK_CREATE dedup tests + `MAC_STATS_TASK_DIR` | Optional env override in `Config::task_dir()` so unit tests use a temp dir (no writes under `~/.mac-stats/task`). `test_slug_deterministic` + `create_task_duplicate_topic_id_errors_with_task_append_hint` lock F5 slug/dedup/error-text contract (022 §3). Mutex-serialized tests restore env on drop. |
| FEAT-D8 | Conversation history cap helper + F1 ordering test | `cap_tail_chronological` in `commands/session_history.rs` (same semantics as `rev().take(N).rev()`); four unit tests. Used by `answer_with_ollama_and_fetch` and Discord having_fun (20 / 10 caps). `get_messages_before_add_user_excludes_current_turn` in `session_memory.rs` documents F1: fetch prior before `add_message` for the current user turn. Closes 022 §3 F1/F2 checklist automation. |
| FEAT-D7 | Session file parse: headings in body | `load_messages_from_latest_session_file` / `parse_session_markdown`: only full-line `## User` / `## Assistant` start a block (line-oriented parser). Lines like `## Notes` inside a message stay in the body; old `split("\n## ")` dropped those turns. Four unit tests. Closes 022 F1 checklist edge case (`## ` in content). |
| FEAT-D6 | Monitor check interval minimum | `check_interval_secs` clamped to ≥1 on add, load, and in `run_due_monitor_checks` so `0` cannot make a monitor due every pass (`elapsed >= 0`). Three unit tests. Closes review doc F10 edge case (022 §3). |
| FEAT-D5 | SSRF redirect DNS / empty resolve | `check_redirect_target_ssrf()`: redirect hop must resolve like the initial URL; DNS failure or zero addresses → `PermissionDenied` (no silent `follow`). Extracted from `ssrf_redirect_policy`; 4 unit tests (private IP, allowlist, public, `.invalid` DNS fail). Closes 022 §9 nit (redirect DNS-fail previously followed). |
| FEAT-D4 | FETCH_URL truncation marker | Oversized bodies: `truncate_fetch_body_if_needed()` ellipses with budget `MAX_BODY_CHARS - len(suffix)` and appends ` [content truncated]`; unit tests for short/long. Resolves review doc D3 (explicit LLM hint). |
| FEAT-D3 | SSRF: IPv4-mapped broadcast | `is_blocked_ip` IPv6 branch: `to_ipv4_mapped()` closure now includes `is_broadcast()` (parity with IPv4); test for `::ffff:255.255.255.255`. |
| FEAT-D2 | Clippy `items_after_test_module` | `logging/subsystem.rs`: `mod tests` moved after exported `mac_stats_*` macros. |
| FEAT-D1 | Session file resume: legacy filename layout | `load_messages_from_latest_session_file` matches new `session-memory-{id}-{ts}-{topic}.md` and legacy `session-memory-{topic}-{id}-{ts}.md` via `session_filename_matches_id` + tests. |

## Open / deferred (no owner)

Large integrations and vague items stay in **docs/006_roadmap_ai_tasks.md** and per-doc “Future/backlog” sections (Mail, WhatsApp, Google Docs, parallel tool loop, etc.) until scoped.

## When empty

When every FEAT-D* item is only under **Recently closed** (highest id today: **FEAT-D383**), there is **no row to pick**—add a new scoped **FEAT-D383+** bullet or table under **Open / deferred** before implementing, or triage the paths below.

Triage **docs/022_feature_review_plan.md** unchecked checklist items (several are **manual Discord** checks only), **docs/035_memory_and_topic_handling.md**, or run `cargo clippy` / `cargo test` for actionable issues.
